# Project State

> **Archive pointer (2026-07-31, session 40).** Sessions 01–37 moved byte-exact to `docs/archive/2026-07-31-state-docs/`. Across the four startup-contract docs that is **1.25 MB → 59 KB (−95%)**. Losslessness was verified against `git show HEAD:`, not against the moving script's own bookkeeping. Read the archive for provenance; read this file for current state.

Build update: 2026-07-31 (session 40, Opus 5 seat, high effort — `/claude-os` "complete all") — **THE STARTUP CONTRACT NO LONGER COSTS 1.31 MB, AND THE THING THAT KEPT KILLING THE REVIEW GATE IS GONE.** Three items, all closed or handed over. **① PUSH DONE** — `858fb17` pushed, `main == origin/main`; the "safe now" premise was verified first rather than accepted (`vercel.json` → `git.deploymentEnabled: false` at HEAD), so push shipped code and triggered no deploy. **② ROUTES + HEADERS — HANDED TO THE OWNER, NOT CLAIMED.** Bash network is sandboxed to four hosts and the Chrome extension is not connected (`tabs_context_mcp` → "Browser extension is not connected", raw on record); the sandbox bypass is retired by owner directive, so the check went to the owner as one `!` one-liner covering 7 routes + a 404 control + the five headers on a subpath. **It is not verified until that runs.** **③ FINDING 2 CLOSED AT THE CAUSE, NOT THE TRIGGER.** The owner's own read — *"the instruction is the trigger; the volume is the cause"* — is what the fix followed. `RULES.md` §15 is **deliberately unchanged**: its read list is still literally correct and is now cheap, and editing the constitution would have been a scope change needing owner sign-off. What moved was volume: `PROJECT_STATE` 307 KB→3.7 KB · `CURRENT_TASK` 171 KB→5.6 KB · `HANDOFF` 422 KB→15 KB (standing continuity procedures **kept live** — those are procedure, not history) · `docs/task-log` 352 KB→35 KB. **Scope deliberately exceeded the owner's literal "sessions 24–36"**: the docs ran back to session 01, so archiving only 24–36 would have left most of the weight and fixed almost nothing; the owner's stated reasoning licenses the wider cut. **CHECKED BEFORE MOVING A BYTE:** no test, script, or eval references any of the four docs, and both honesty suites (`honesty-c10`, `fees-honesty-c10`) walk **explicit file lists**, never a recursive `docs/` glob — so 1.19 MB of history quoting the retired "no network requests" wording could not enter the honesty-scan surface. That was the real risk and it was the first thing tested. `AGENTS.md`:7 gained a small additive note stating the 59 KB figure and telling reviewers **not** to read the archive as startup context.

Build update: 2026-08-02 (session 43, Fable 5 seat, max effort — `/claude-os` walkthrough redesign) — **THE SITE NOW TELLS THE WHOLE STORY, DROP TO DELIVERY.** Owner reopened the layout by word (palette unchanged, minimalist premium 2026, end-to-end "from dropping files till slack email"); the landing became six stations — INPUTS · RUN · VERDICT · FEES · DELIVERY · PROOF — designed in-session as a rendered mockup (fold-verified: zones 560 / run 626 against the 900 contract; the shipped site had the run control at 970 AND disabled), then ported by an Opus subagent and reviewed diff-level. The NEW delivery station renders the real Slack Block Kit payload and RFC 5322 email from `lib/delivery/*` — the same builders the CLI walkthrough calls — stamped BUILT NOT SENT, banner from the builder's own first block, zero-egress proven by a new 22-module import-graph tooth (red-green, two mutations). The twice-found "instrument without its verb" finding is closed: the run control is never disabled and sits on screen one. Gates re-run independently: **tsc 0 · eslint 0 · vitest 1630 + 8 skipped** (honesty-c10 = the disclosed font-host block). E2e moved with the design, authored-but-unrendered — the owner-side battery is the next verification step. Committed and pushed this session; deploy per owner grant. **SECOND ARC:** the real-product register went sitewide by owner word — GitHub links and the whole legacy module left the site (8 route dirs deleted, 404s honest, no redirects; 61 built pages → 9), §4 carriers verified biting. And the four-session "cannot build in-seat" limit FELL: `scripts/build-offline.mjs` (next/font's own mock hook + the webpack pipeline + real font bytes extracted from the repo's v8 mockup) produces a genuine export in the sandbox — the full suite ran green in-seat for the first time (1604 + 8, 0 failed, C10 rendered scan included), and rendered QA ran via port-free route interception (axe 0 violations after two real a11y fixes). CI's rendered battery on the port (48 passed / 4 failed) was diagnosed and closed: two real defects fixed, two stale pins moved, one non-reproducing red given a loud precondition. Open: the next CI run adjudicates; deploy is the one owner-shell step.

Build update: 2026-07-31 (session 42, Opus 5 seat, medium-high effort — `/claude-os` resume) — **THE THREE OWNER DECISIONS LANDED; THE ONE THAT LOOKED LIKE A ONE-LINER WAS NOT.** Owner returned the decision board: fold reclaim **not decided** (nothing touched), nav pill **retargeted to `/#audit`** (applied), workbench-first **confirmed** (no build). The pill change carried a defect that this seat could not have seen fail: `Nav.tsx` derived both `aria-current` and the accent-fill styling from the literal `d.href === "/"`, and `usePathname()` never returns a hash — so moving the href alone would have killed `aria-current` on the Audit link forever and stripped the pill, breaking the very `canonical.spec.ts` contract the owner authorized touching. Route and target are now separate fields (`match ?? href`); the spec moved with it and gained two teeth, including `a[href="/"]` count **0** inside the Sections nav so a re-added `/` destination fails loudly. Target is `/#audit` rather than `#audit` so it works from every route; the brand lockup still carries plain "home". **Gates: tsc 0 · eslint 0 · vitest 1610 + 8 skipped**, `honesty-c10` the single failure with `fonts.googleapis.com` measured at `000` first — the disclosed sandbox limit, not a regression, and the unchanged count is the evidence the change is inert to the unit suite. **`npm run verify` was not run as one command** (its build step would abort before vitest and yield zero test signal); the legs were split and the substitution disclosed. **Nothing rendered — Chromium still refuses to launch here**, so the retarget is type-checked and unit-tested but NOT seen. New open item recorded rather than quietly fixed: `#audit` has no `scroll-margin-top`, so a jump lands under the sticky nav — pre-existing, but the retarget makes that path the site's main one.

Build update: 2026-07-31 (session 41, Fable 5 → Opus 5 seat, max effort — `/claude-os` resume) — **THE REDESIGN WENT LIVE, AND THEN THE OWNER REOPENED THE DESIGN.** **DEPLOY DONE:** production had sat 16 commits behind on `fb6c327`; it now serves **`9a8beed`** (owner ran `npx vercel --prod`; READY, 61 pages, aliased). Re-verified by fetching, not inferring — **7/7 routes render real content plus a nonsense-path 404 control** (the control is what makes the seven mean anything; session 35's serve-`/`-404-the-rest mode is ruled out), the retired *"no network requests"* phrase is **absent everywhere**, and footer provenance matches HEAD. **Headers remain the one unverified quarter** — no tool on this seat returns response headers; the owner's `curl -sSI` one-liner still stands. **THEN FOUR OWNER DIRECTIVES ARRIVED MID-TURN, ALL APPLIED, ALL UNCOMMITTED.** **① ONEST RESTORED SITEWIDE, REVERSING THE TWICE-FIXED NUNITO WORD** (owner: *"USE this font throughout"*, pointing at the v8 mockup — the face was **verified as Onest in that file's embedded `@font-face`** rather than assumed, and because Nunito had been owner-fixed twice the fork went back as a structured ask before anything moved). **The face was the easy half; the weights were the trap:** commit `85cba2e` had raised every display weight by 60 and body by 40 *because Nunito reads optically lighter at the same numeric value*, so carrying those numbers onto Onest would have overweighted the whole site — the mirror image of the "too thin" complaint that created them. Reversed by the same deltas, and the target is confirmed independently by the weight-pass block's own header recording the original Onest pass as *"body 400 -> 460"*. `ui-rounded` dropped from every fallback stack (SF Rounded on macOS: right for Nunito, wrong for a grotesque). The long-preserved dead "display tier" block was **deleted rather than kept** — it holds the *pre-complaint* Onest weights, so the moment Onest returned it stopped being an inert record and became a trap for the next reader. **② PRODUCTION REGISTER** (owner: *"remove the sample feed, sample report word"*), 12 files. The load-bearing distinction: "sample" was captioning the site as a demo in some places and stating what the data IS in others, and only the first may be deleted — the second became **"bundled"**, because removing it would flip a true sentence into an implied claim about real merchant data. **RULES §4's carriers verified rather than assumed:** C10's eight patterns over every changed file → **0 hits with a control proving each still bites**, and the `/docs` "what is real" statement plus footer link untouched; three honesty tests that PINNED the old wording were updated in the same change. **③ THE DARK BAND** the owner photographed is not painted by the page; `viewport.themeColor` was missing and is now declared — **but the fix claim is withdrawn**, since their screenshot shows VS Code's Simple Browser, whose chrome `themeColor` cannot reach. **④ /report WIDE-VIEWPORT IMBALANCE measured to cause:** at 2000px the container is 1180px and the masthead's 20ch/62ch text fills only its left half while the finding card fills all of it; the verdict chips move from a fifth stacked row into a right column at ≥1080px. **GATES: tsc 0 · eslint 0 · vitest 1610 + 8 skipped, 1 failed** (`honesty-c10` only — its build needs `fonts.googleapis.com`, outside the allowlist; it did resolve `Onest:wght@100..900`, confirming family and axis). **NOTHING VISUAL IS RENDERED FROM THIS SEAT** — Chromium refuses to launch and ports refuse to bind, both raw on record, four approaches tried. **THE `ch` RISK I RECORDED WAS MEASURED AND REFUTED — THE SWAP MAKES THE FOLD SAFER, NOT RISKIER.** I had warned that 58 `max-width: *ch` boxes would narrow under Onest and gain lines. An audit parsed both variable TTFs directly (`hmtx` + `HVAR`, cross-checked against Next's bundled capsize table to 0.1%) and inverted it: Onest's `'0'` is **10.7% WIDER** than Nunito's at weight 460 (0.6640em vs 0.6000em), while its average glyph is only **3.4%** wider — so every `ch` box grows faster than the text inside it and roughly **6.6% more characters fit per line**. No `ch` box gains a line; one loses one (`.cs-lede` 3 → 2, **−28.9px**, in the safe direction). **And the "28px of margin" was margin on a page that no longer renders** — that figure was recorded against a layout with a `.cs-copy` wrapper and a 78ch instruction wall, both deleted earlier today. Computed headroom on the current tree is ~127–146px. **Still unrendered:** `out/` is a Nunito build, so this is parsed-metric evidence, not a rendered check; `canonical.spec.ts:130` computes the zone bottom and `:132` **asserts** it — never printed, but not discarded; one run prints the real number. **Production still serves Nunito. The reversal is COMMITTED (`ef0d53f`·`4bbb984`·`8c4cee6`, ahead 3) but NOT pushed and NOT deployed** — the two states must not be conflated. Open: a network-capable verify + e2e · the landing hero fork (owner-sourced evidence on both sides, their call) · the header check · R-3 dead-CSS, deferred until the design settles · the owner-run Codex probe (seat pinned by owner word to `gpt-5.6-sol`, no fallback).

Build update: 2026-07-29 (session 38, Opus 5 seat, high effort — `/claude-os` resume) — **THE MISSING CROSS-MODEL GATE RAN, RETURNED A VALID BLOCK, AND THE SITE WAS SAYING SOMETHING FALSE.** Session 37's work had never been reviewed by a second model — an unmet `RULES.md` §9 item on the session that changed the most about what the site asserts of itself. **The headline: `/` said "no network requests", and it was measurably false.** Measured against the BUILT export rather than inferred from framework docs — the page makes **12 post-load requests** (Next `<Link>` prefetch pulling its own route payloads) while **off-origin is 0**. The two claims come apart and only one is true: *nothing the reader uploads or types ever leaves the browser*, but the page plainly makes requests for itself. Corrected across **five shipping surfaces**, two stale comments, and the how-to-test guide — which had been **inviting readers to verify the false claim in their own network tab**. The reviewer cited 3 sites; a case-insensitive sweep found **8**, because a rendered chip in CAPITALS and a phrase broken across a newline both escaped the narrow grep. **And three tests were PINNING the false claim** — it was not merely present, it was required by CI, so any honest correction would have failed the build. *A guard that asserts a claim is true cannot also notice the claim is false.* Both suites now assert the true wording **and** `.not.toMatch` the retired one; that inverted assertion is what found the two the grep missed. **Three test-integrity defects fixed red-green, each proven by MUTATION:** a door test that filtered on one phrase and **would not have caught the defect it was written for**; a reset assertion that was **tautological** (deleting the line it guards left it green); and a fold guard whose `toBeVisible()` said nothing about the viewport. **Two of the reviewer's proposed fixes were REFUTED on measurements** — the Run button sits below the fold by design (y=970), and the dead "display tier" tracking was calibrated for Onest, the face session 37 replaced, so applying it would be a typography change dressed as a bug fix. Also fixed a **`/legacy/merchant` 404** that no test could see, because that spec checked a list of routes written inside itself — the **third** *denominator-from-the-observations* instance this session. **Then, on the owner's second word — *"also to show the end to end demonstration, slack, all those involved"* — `npm run walkthrough` shipped:** one command carrying ONE audit across every surface, inputs → engine + receipts → conformance-vs-truth → fee audit → **Slack payload** → **email** → surface map. **It sends nothing, enforced not promised** — the delivery legs call the same builders the owner-armed one-shots call and print instead of transmitting, with a new tooth walking the script's real import graph (56 modules) against the fail-closed allowlist, red-green proven by appending a single `fetch`. Writing it, the product refused three of my assumptions (a required `surface` param, a flat envelope with a canonical string rather than a nested report, a caller-supplied `date` and no `to`/`from`) and I caught one defect of my own — a blanket `_` strip that rewrote `service_and_delivery` in the Slack preview. **Gates: verify exit 0 · vitest 1574 + 8 skipped · e2e dev 53 + 1 · e2e ARTIFACT 53 + 1 (up from 52) · axe 8/8.** Record: `docs/reviews/codex-2026-07-28-s38-gate.md`. **Open: push (now 5 commits + this work, owner's word) · deploy (`npx vercel --prod`, deliberate) · `AGENTS.md:7` makes this repo hostile to any budgeted reviewer (~140k tokens of state docs on entry — it killed two gate runs).**
