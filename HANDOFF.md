# HANDOFF.md

Point-in-time handoff for the next session. Overwrite the top block each time a session ends or pauses; keep the standing procedures at the bottom.

## Latest handoff

- **Last session tool:** Claude Code (Opus 4.8)
- **Account / session:** account 1 (mithulshyam4@gmail.com)
- **Task ID:** T-001.7 — Post-Playbook Alignment Audit (**complete; T-001 = closed with minor follow-ups**) **+ Source Openness Clarification Pass** (lightweight, wording-only — **complete**) **+ Codex review of the pass + Codex-flagged continuity doc-sync correction** (**complete; pending owner commit**).
- **What was done (this session = checkpoint audit, full mode under the Startup Contract):** ran the prescribed commands; verified standards alignment; created `docs/audits/post-playbook-alignment-audit.md`; fixed the known-stale `docs/v1-slice-plan.md` (test list → T1–T18 + P2-1..P2-5 = 23; added `--fresh` vs preserve note; status → implemented) and the state-doc git-state lines. Read the vault read-only. **No** product code/tests/CSV edits; nothing installed/adopted.
- **Audit verdict:** still on the right path. **T-001 holds under the updated standards — 23/23 pass; all guarantees intact** (CSV `43fb21f6…` unchanged; offline; no Gemini/API/sends; `contact_eligible`≠`send_eligible`; T17 send-gate; idempotency via `run.py`; unique `model_run_id`; guardrail over+under flagging). The new playbook/contract/intake rule break nothing retroactively. **No blockers.**
- **Obsidian vault: exemplary & separate.** README + CLAUDE.md both say personal-knowledge-layer / not-source-of-truth / repo-wins / stop-and-ask / no-global-config-changes; Tool/Model Usage Note template present; **global `~/.claude/CLAUDE.md` has no vault link**; runtime independent of Obsidian.
- **Source Openness Clarification Pass (this session, lightweight, wording-only):** added an **Open Source Discovery** rule — *named sources/frameworks/repos/vendors/communities/examples are candidates and seeds, not boundaries*; search broadly and task-specifically (official/vendor/standards docs, mature OSS, GitHub issues/PRs, eng blogs, **Reddit/forums/YouTube/SO as field-signals**, changelogs); **tiers judge quality, not restrict discovery**; *maximum useful research ≠ endless* (stop at sufficiency, document uncertainty); source-use weighting kept (official = source of truth; community = signal, not proof unless corroborated). Added to `docs/enterprise-delivery-playbook.md` + pointers in `RULES.md` §14, `CLAUDE.md`, `CODEX.md` (8 flag conditions), `docs/prompts/claude-task-template.md`, both Codex review templates, and `prevent-repeat-checklist.md`. **No restrictive wording existed to remove** (grep found only false positives) — the pass makes existing openness explicit. **No** code/tests/CSV/integration/scope change; **no new standing files; no decision-log entry** (no decision made).
- **Codex review + continuity correction (this session):** ran the plugin `adversarial-review` (review-only, `sandbox: read-only`) on the working tree. Verdict: **needs-revision** — but the **Open Source Discovery rule itself was approved** (treats named sources as seeds, preserves official-source authority, demotes community content to field-signals, proportional stop conditions; scope-safety PASS; no process bloat). The needs-revision was only about **two stale continuity docs** riding along in the tree. Both now fixed: `PROJECT_STATE.md` (refreshed Current Readiness / Blockers / Next Step / Decision Status / Open Questions / Handoff Notes from the old pre-build framing to current reality) and `CURRENT_TASK.md` (rewritten to docs-only scope; T-001 implementation facts moved to a labeled completed-stage summary). Source-openness wording left intact; no product/test/CSV/`out`/integration change.
- **Git state (re-derived 2026-06-02):** `HEAD = 63e3332 "Complete T-001.6 source intake correction"`; tree was clean before the audit. **Uncommitted now:** the T-001.7 audit + doc-sync fixes (`v1-slice-plan`, state docs); **the Source Openness Clarification wording edits** (playbook, `RULES.md`, `CLAUDE.md`, `CODEX.md`, 3 prompt templates, prevent-repeat-checklist, state docs); **the Codex-flagged continuity correction** (`PROJECT_STATE.md` + `CURRENT_TASK.md` refreshed); **and** `out/audit_log.csv`/`out/model_runs.csv` (regenerated by the prescribed `run.py` — restore with `git checkout -- out/`). Product code/tests/CSV unchanged.
- **What not to do:** Do not start T-002 or the roadmap without owner approval; do not jump to Gemini before an offline eval harness + secrets + offline mock; do not add integrations/plugins/hooks; do not link the vault globally; do not add more governance; do not commit unless the owner says so.
- **Next step:** owner reviews + commits this audit + the Source Openness clarification + this continuity correction; restores `out/` (or sets a log policy); then the **roadmap / lifecycle applicability review**, then **ratify eval-first T-002** in `docs/decision-log.md` before scoping **T-002 = offline evaluation harness**. Tests: `python3 -m unittest tests.test_t001 -v`.
- **If unsure, stop and ask.**

## Standing continuity procedures

### If Claude Code account 1 hits usage mid-task

1. Stop.
2. Update `CURRENT_TASK.md`.
3. Update this `HANDOFF.md`.
4. Update `PROJECT_STATE.md`.
5. Update `docs/task-log.md`.
6. List uncommitted changes (`git status`).
7. Do not start a new task.

### When Claude Code account 2 (or the CLI) starts

1. Read `RULES.md`.
2. Read `PROJECT_STATE.md`.
3. Read `CURRENT_TASK.md`.
4. Read `HANDOFF.md`.
5. Read `docs/task-log.md`.
6. Run `git status`.
7. Summarize current phase, active task, changed files, unfinished work, risks, and the next safest step.
8. Wait for human approval before continuing.

### Background Codex jobs

When a Codex job runs in the background, record its purpose and whether its result was checked here or in `docs/task-log.md`. Poll with `/codex:status`; fetch with `/codex:result`; cancel with `/codex:cancel`.
