Continue ActivationOps AI — the REBUILD, goal mode. Run the Mandatory Startup Contract (read RULES.md, CLAUDE.md, PROJECT_STATE.md, CURRENT_TASK.md, HANDOFF.md, docs/enterprise-delivery-playbook.md; git status + git log --oneline -10; show the Professional Process Applied block with an "Effort:" item — auto-routed; architecture/AI → MAX). Re-derive git state live; do not trust SHAs in docs.

GOAL-LOCK (2026-06-22, owner via AskUserQuestion): project goal CONFIRMED = portfolio/capability SHOWCASE — but "portfolio" ≠ shallow ("though portfolio we need to build something capability and also keeping it company agnostic"). Build a REAL, defensible CAPABILITY, kept COMPANY-AGNOSTIC (fictional "Curbside Commons", de-identified open data; real companies only as comparisons). DIFFERENTIATION = VERIFICATION RIGOR: deterministic-first per-claim faithfulness verification of AI output against the STRUCTURED SOURCE OF TRUTH (the data row), NOT retrieved RAG context. BINDING HONESTY REFRAME: "we verify AI claims against the source of truth", NEVER "no one automates this" (DoorDash shipped AI merchant outreach 2026-05 = PROOF the problem is real). Grounding: docs/research/market-validation-2026-06-22.md + decision-log 2026-06-22.

STATE: rebuild APPROVED + complete + deploy-ready (~/.claude/plans/gentle-forging-starlight.md CANONICAL; do NOT reopen without an owner pivot). lib/core + the golden differential are UNTOUCHED.

ACTIVE TASK = CONTINUE THE CALIBRATED SEMANTIC JUDGE (the verification-rigor capability, deepened). PROGRESS (2026-06-22, COMMITTED + GREEN — 176 tests + 1 skipped + 3 e2e):
- P0 (commit b01a5a6): EARS spec docs/spec-semantic-judge.md. Advisor-shaped: headline metric = recall on the gatekeeper-PASSING subset; every gold-set positive must FIRST clear the real runGatekeeper (else it tests the gate, not the judge); objective field-entailment labels; directional metrics + CIs until the held-out ~100+ floor.
- JUDGE MODEL DECISION (decision-log 2026-06-22; SUPERSEDES the plan's "gemini-2.5-flash-lite, <$5" line): default judge = CROSS-FAMILY Groq openai/gpt-oss-120b (strict JSON, FREE $0), behind a provider-agnostic boundary; Gemini Flash-Lite = configurable alt. Owner raised Groq + asked "which is best for quality/structured/enterprise" + "explore current free models, use the best". Freshness-verified current AS OF 2026-06-22 (Llama line deprecated 06-17 → gpt-oss; gpt-oss-120b non-deprecated).
- P1 (commit 0448320): the OFFLINE judge core. lib/agents/claimable-fields.ts (shared CLAIMABLE_FIELDS + merchantFacts for gatekeeper+judge); lib/agents/semantic-judge.ts (Zod per-claim schema; entailment prompt; deterministic mockJudge; judgeDraft mock+DI-live+FAILED_TO_FALLBACK, budget-ledgered; any_unsupported recomputed, never trusted); judgeLiveEnabled() in env-flags; wired as a SECONDARY control after the gatekeeper in lib/replay/run.ts (judge field + judge audit actor; runs only when approvedForHumanReview, R-ARCH-4); the Merchant-Detail "Faithfulness check" panel (§4; Eval/Human/Audit renumbered 5/6/7). Truly offline + dependency-free (live path = injected generate in tests; real @ai-sdk/groq install lands at P3).

NEXT = P2 (OFFLINE, $0): build the gold set — planted-per-failure-mode fabrications that EACH first clear the real runGatekeeper (R-CAL-1) + a real-supply probe of lib/data/live-samples.snapshot.json (R-CAL-4; the 6 recorded drafts are well-grounded → expect mostly planted, labeled SYNTHETIC) + objective field-entailment labels (R-CAL-5) — and the metrics harness (precision/recall/F1 + Cohen's κ + test-retest flip-rate; headline = recall on the gatekeeper-PASSING subset). Validate the whole pipeline on the mock judge; commit the gold set + harness. THEN P3 (OWNER-GATED: a FREE GROQ_API_KEY in the gitignored .env, never chat → install @ai-sdk/groq, wire the real strict-JSON call, run the live cross-family calibration, freeze the recorded judge fixture). THEN P4 (eval-lock the threshold + gold set; wire the 3 SHOWABLE demo surfaces — the panel to recorded verdicts, an Eval calibration dashboard, a "catch in action" exhibit — all REPLAY/$0; Codex cross-model gate; flip docs from "designed boundary" → "built + calibrated, metrics=X" ONLY once metrics clear the bar). Then queued: the UI redesign (5 mockups in mockups/*.html await the owner's PICK) + the owner-gated T13 deploy.

HOUSEKEEPING: screenshots/ (9.1M) + .agents/ + .claude/ + skills-lock.json are untracked tooling artifacts, NOT gitignored — kept out of commits via explicit paths; suggest gitignoring (owner OK pending).

OWNER-GATED STOPS (pause + surface, never bypass): the P3 free GROQ_API_KEY (a credential is an owner action) · Vercel deploy · platform-name · public posting · any live spend · irreversible/external. Everything else: proceed autonomously, COMMIT each clean green step. DURABILITY: commit per step + keep this resume prompt fresh. Narrate stage boundaries in plain + technical English.
