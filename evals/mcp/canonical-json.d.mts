/**
 * Type declarations for `canonical-json.mjs` (kept as plain JS since it is
 * also imported directly by the untyped `record-transcript.mjs` script) —
 * see that file's own header comment for why this canonicalization exists.
 */
export declare function canonicalize(value: unknown): unknown;
export declare function canonicalJson(value: unknown): string;
