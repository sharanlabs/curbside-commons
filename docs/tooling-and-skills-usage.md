# Tooling, Skills & Agents — Usage Ledger

> **Purpose:** a queryable record of *how each tool, skill, and agent was used and how useful it was* on this project — so anyone (especially the owner) can ask "how was [graphify / mem / caveman / Codex / X] utilized?" and get a grounded answer. Complements the chronological `docs/task-log.md` (RULES §5) with a **tool-indexed** view.
>
> **Maintenance rule (standing):** whenever a tool/skill/agent is used — *or evaluated and deliberately not adopted* — add/append a row with **what for · how useful · basis**. Keep it lightweight (one row per tool; append notes). Owner-requested 2026-06-09.

| Tool / Skill / Agent | Status | Where / when | What for | How useful (verdict + basis) |
|---|---|---|---|---|
| **claude-os** (skill) | Used | Session boot, 2026-06-09 | Entry ritual / project framing | Useful as a **lens**, not a scaffolder — the project has its own OS, so applied preflight→sense→gate as a way of seeing; did *not* scaffold competing ledgers (would breach the project's anti-drift rules). |
| **advisor** (tool) | Used (several) | Before each consequential interpretation/decision | Pressure-test analysis before committing | **High value** — affirmed "don't scaffold," pushed me to *verify* the eval-coverage crux instead of punting it, and sharpened the live-API pushback framing. |
| **Codex CLI** (gpt-5.5, xhigh, read-only) | Used | Data-sourcing review, session `019eac95`, 2026-06-09 | Independent cross-model adversarial review of live-API vs synthetic | **High value** — neutral fork, reached the same no-ship-on-live-API verdict independently (high-confidence signal); also surfaced the exact de-brand string locations + the contact-safety data gap. |
| **WebSearch** | Used | 2026-06-09 | Verify the nested-subagent claim; platform-name collision checks | Useful — caught "Forkful"/"Stoop" as real companies; surfaced that `fork` is anti-recursion (contradicting the pasted tweet). |
| **WebFetch** | Used | 2026-06-09 | Official changelog verification; intake of `ai-intel` + `agent-findings` repos | Useful for authoritative verification (changelog) + read-only repo evaluation (Law 11 quarantine). |
| **memory / mem** (system) | Used | 2026-06-09 | Persist non-obvious facts + owner preferences across sessions | Useful — saved the eval-coverage crux (project), the always-give-resume-prompt rule (feedback), and this ledger habit. |
| **caveman** (skill) | **Evaluated → not adopted** | 2026-06-09 (read SKILL.md) | Considered for token-efficient output | **Rejected for deliverables/public docs** — its terse/compressed style conflicts with the project's "Professional Experience Quality" bar (clear, layperson-readable). Available for *internal* terse scratch only. |
| **graphify** (skill) | Not used (yet) | — | — | No knowledge-graph need has arisen. Candidate later if we visualize the decision/architecture graph. |
| **project-advisor / acceptance-gate / discovery-scout** (claude-os agents) | **Not used** (deliberate) | — | — | The project has its own OS + Codex review; spawning these would duplicate/bloat (ADR-0005, anti-bloat). |
| **Subagents** (general) | **Not spawned** (deliberate) | — | — | Orchestration kept on the main thread; multi-agent ≠ better and costs ~7× tokens. Would reconsider only for a heavy isolated read/research step (fork-for-context-isolation). |
| **Codex CLI** (gpt-5.5, xhigh, read-only) — T-003 plan review | Used | Plan review, job `bvympilb4`, 2026-06-09 | Adversarial review of `docs/phase3-prep-slice-plan.md` before coding | **High value** — independent **NO-SHIP**; converged on the add-alongside v1/v2 architecture, caught the pipeline-schema limit (hardcoded contact fields, `pipeline.py:268`), the dual-platform guardrail need, the draft-contract stub-leak risk, and the regression-vs-adversarial-probe scoring conflict. Findings fold into the agentic foundation layer. |
| **WebSearch + WebFetch** (multi-source discovery) | Used | Agentic architecture blueprint, 2026-06-09 | 7-domain discovery: agent patterns, guardrails, agentic security, evals, governance, HITL, observability | **High value** — current Tier-1 sources (Anthropic *Building Effective Agents*, OpenAI agents/guardrails, OWASP LLM 2025 + Agentic 2026, NIST AI RMF + GenAI Profile, 12-Factor-Agents, OTel GenAI semconv, DeepEval/promptfoo/RAGAS) grounded every blueprint choice; `awesome-*` lists used as seeds; fetched content treated as untrusted reference (Law 11). |
| **Codex CLI** (gpt-5.5, xhigh, read-only) — blueprint completeness review | Used | Blueprint review, job `bm0i9bxpy`, 2026-06-09 | Independent completeness/correctness check of the agentic blueprint package | **High value** — **NO-SHIP (no P0)**; 10 findings: T-003 plan still says rename SOURCE_CSV (must be add-alongside), pipeline can't ingest v2 yet, approval-gate overclaim, autonomy gates not auditable, visuals overstate eval, source traceability + built-vs-target overclaims. Honesty fixes applied; deeper acceptance-criteria deferred to build-time. |

| **claude-os** (skill) — goal reassessment | Used | Session boot, 2026-06-11 | Front-door triage of the owner's "reassess the goal" ask (half-done route: reassess the core from the repo's own files; owner fixes the new direction) | Useful — again as a **lens**: routed the ask to evidence-first analysis + cross-model gate instead of a fresh brainstorm; no scaffolding (project has its own OS). |
| **Explore subagent** (read-only) | Used | Repo inventory, 2026-06-11 | Fan-out factual inventory (code vs docs footprint, blueprint/case-study/narrative summaries, purpose quotes) feeding the goal reassessment | Useful — kept ~40 file reads out of the main context; surfaced the 6,037-doc-lines vs 1,364-code-lines ratio and the "public portfolio piece" purpose quotes with file refs. |
| **Codex CLI** (gpt-5.5, xhigh, read-only) — goal-reassessment cross-check | Used | Background `codex exec`, 2026-06-11 | Adversarial cross-check of the consequential goal recommendation (per operating doctrine) before presenting it to the owner | **High value** — AGREE ×4 with substance: Phase-4-as-stretch-proof refinement, "L1 undersells agentic unless the demo shows safety paths," + 3 verified repo contradictions (stale README, blueprint-review status conflict, hybrid-vs-synthetic wording). |

| **Codex CLI** (gpt-5.5, xhigh, read-only) — T-003 draft-2 plan review | **Attempted → quota-blocked** | 2026-06-12 | Mandatory adversarial review of the revised `docs/phase3-prep-slice-plan.md` (draft 2) | **Did not run** — the `codex exec` process orphaned (1h36m, zero output, no session rollout ever created); root cause verified from the 09:45 smoke-test rollout: seat credits `balance: "0"` (capped until 2026-06-14 ~09:56 per `~/claude-os` STATE). Killed; deferred with written reason (RULES §9 — not skipped). *Same-day update (owner-directed): cap-tracking removed per the claude-os order — status reframed to **pending a working seat** (smoke-test first; raw errors verbatim; seat/credits = owner action); two live smoke tests still errored with the usage-limit message.* |
| **claude-os PROJECT-CONSTRAINTS intake** | Used | 2026-06-12 | Owner directed this repo to take the new standing constraints (`~/claude-os/docs/PROJECT-CONSTRAINTS.md`) | Useful — tooling ladder, Gemini routing-in-cap, data-source biases (documented as not reopening the no-live-API decision), date-anchored vetting, the Phase-7 expansion/adoption deliverable, and the product-target-market intake question; encoded via a decision-log row + `CLAUDE.md` addendum + roadmap/plan/open-questions syncs. |
| **memory / mem** (system) | Used | 2026-06-12 | Encoded the owner's new session-management rule (proactively prompt a fresh session when long; stage-boundary cuts; Claude's call) | Useful — extended the existing resume-prompt memory + added the rule to `CLAUDE.md` (Handoff doctrine) so it survives across accounts. |

| **grill-me-codex** (skill) — Act 1 | Used | 2026-06-12, second session | Owner-chosen review flow for the T-003 plan: Act 1 grill (4 open questions, one at a time, with recommendations) before the Act 2 Codex adversarial loop | **High value** — locked all 4 owner decisions (OQ-1 rename override · OQ-2 commit-fresh · Curbside Commons · US) and surfaced one live follow-up (the residual-content/frozen-proof tension) the written plan had not separated from the filename question; produced `PLAN.md` + `PLAN-REVIEW-LOG.md` so Act 2 can start cold next session. Live grep during the grill (filename literal only at `scripts/config.py:10`) turned OQ-1 from "rename breaks the proof" into a proof-safe 1-line re-pin — explore-the-codebase-instead-of-asking, as the skill instructs. |
| **Codex CLI** — T-003 Act-2 smoke test | **Attempted → seat dead** | 2026-06-12, second session | Seat smoke test before the mandatory Act-2 plan review | **Did not run** — usage-limit error, exit 1 (raw text verbatim in `PLAN-REVIEW-LOG.md`); per owner standing order: surfaced verbatim, stopped, no retry/switch/cap-tracking. Act 2 = RULES §9 deferral with written reason. |
| **advisor** (tool) | **Unavailable** | 2026-06-12, second session | Attempted pre-decision pressure-test (whether to proceed with Act 1 despite the dead seat) | Tool returned "unavailable" — proceeded on own judgment (owner inputs are Codex-independent and were pre-authorized); noted for transparency. |

| **codex-guarded** (`~/claude-os/bin/codex-guarded`) | **Built + verified** | 2026-06-13 | Cross-project Codex queue/mutex wrapper — serializes the one shared Codex seat across all the owner's projects so concurrent runs queue instead of colliding/contaminating | **High value** — built in response to the 2026-06-12 contamination near-miss (ActivationOps read resilix's stale verdict). Atomic-`mkdir` lock (flock absent on macOS) + dead-holder/stale reclaim + namespaced outputs + arg/exit passthrough. Verified: fake-codex harness (serialize/isolate/reclaim/passthrough/release PASS) + real-codex smoke. The T-003 build's per-slice Codex reviews go through it. |
| **grill-me-codex** (skill) — hardening | Updated | 2026-06-13 | Closed the contamination vector at the source | Mandated project-namespaced `VERDICT_FILE`/`STREAM_FILE` + routed Codex through `codex-guarded`, so **every** project using the skill auto-coordinates (not just ActivationOps). Fixed the stale `/tmp/codex-verdict.txt` example that contradicted the earlier What-NOT-to-do bullet. |

| **Explore subagents ×3** (read-only) | Used | 2026-06-19 | Parallel deep-dive: (1) current-project product/UI/roadmap gap, (2) resilix lessons/patterns, (3) owner house-style across sibling projects | **High value** — established the no-UI gap, resilix's design-system + safety patterns + hybrid-data approach, and that procureguard (live on Vercel) is the house standard; grounded the rebuild direction. |
| **Specialized research agents** (market-strategist · opportunity-finder · research-specialist · devils-advocate) | Used | 2026-06-19 | 2026 US AI-demand scan; merchant-activation competitive-gap; last-30-days recency; 4-agent positioning validation | **High value** — "evals are the breakout skill"; "AI for merchants" is table-stakes (DoorDash AI Marketer May 2026 etc.); the governed stalled-long-tail-activation vertical is the wedge; **caught real positioning errors** (EU AI Act high-risk→Art 50; FTC *Air AI*≠hallucination→*Moffatt v. Air Canada*; governance crowded→lead with domain). All cited + dated. |
| **advisor** (tool, ~5×) | Used | 2026-06-19 | Pressure-test each major commitment incl. the keep-Python→rebuild reversal under owner full-liberty + the final anti-scope-balloon check | **High value** — reversed the keep-engine steer correctly; forced "port not from-scratch", eval-spine-before-UI, walking-skeleton-first, dataset-safe-default, why-chain-as-decision-records; flagged the plan-scope-balloon risk. |
| **plan mode + ExitPlanMode** | Used | 2026-06-19 | Gated goal finalization; the plan iterated through several owner refinements before approval | Useful — kept the reanalysis read-only until the goal+DoD were owner-approved; the approved plan (`~/.claude/plans/gentle-forging-starlight.md`) is the canonical execution contract. |
| **autopilot** (skill) | Engaged | 2026-06-19 | Goal-mode driver for the approved rebuild (owner delegated engagement) | Engaged post-approval to drive the pipeline to DONE with gates + owner-gated stops; main-session-only. Build continues in a fresh session (context hygiene). |
| **competitor-analysis** (browserbase skill) | **Installed** (owner-authorized) | 2026-06-19 | Owner asked to add it to the armoury; flagged candidate for Phase-C positioning checks | Installed to `.agents/skills/competitor-analysis` (symlinked → Claude Code) after explicit owner authorization (the earlier auto-classifier denial was "not user-authorized"). **Security at install:** Gen Safe · Socket 0 alerts · **Snyk Med Risk** → *review before first use* (runs with full agent permissions; Law 11). **Not yet used / not yet reviewed** — evaluate + review at Phase C. New untracked path in the repo (`.agents/`); commit-vs-gitignore is the owner's call. |
| **DesignSync / claude-design** (UI design-sync feature) | **Flagged → evaluate at Phase C** | 2026-06-19 | Owner-flagged candidate accelerator for the desktop-console UI build ("your call") | Not yet evaluated — Source-Intake at Phase C; use if it fits, with resilix "Quiet Instrument" tokens as the design language; don't adopt blindly. |

| **advisor** (stronger-model reviewer) | Used ×3 | 2026-06-19 (build session 2) | Cross-checked the slice approach before building, the dataset-sourcing branch, and the finished slice before the gate | High value at the approach + pre-gate boundaries: caught the git-drift (session-1 commit already done), the untracked-files-invisible-to-a-diff-review footgun (→ `git add -N` before Codex), the live-path prompt-injection Phase-B item, and an "independent oracle" overclaim in a comment (fixed). |
| **codex-guarded** (shared-seat wrapper) | Used | 2026-06-19 | Ship-gating changed-files Codex review of the slice (read-only, namespaced output) | Routed the mandatory cross-model review through the shared-seat mutex per doctrine; verdict at `/tmp/codex-verdict-activationops.md`. |
| **DataSF Socrata API** (open data) | Used | 2026-06-19 | Build-time one-time fetch of real SF business entities → frozen snapshot (no runtime dependency) | Public-domain (PDDL 1.0); only `dba_name`+category fetched (PII-safe); measured NAICS coverage live to set the honest sector-level crosswalk (Restaurant/Retail). |
| **TaskCreate / TaskUpdate** (harness) | Used | 2026-06-19 | Tracked the 7 slice stages for visibility + resumability | Lightweight session-scoped tracking alongside the durable repo state docs. |
| **node --experimental-strip-types** | Used | 2026-06-19 | Ran the zero-dep snapshot generator (`scripts-ts/build-hybrid-snapshot.mjs`) | Avoided adding a `tsx` dev dependency for a one-time generator. |

| **research-specialist** (subagent) | Used | 2026-06-19 (goal mode) | Cited domain research (delivery-marketplace merchant-onboarding blocker taxonomy + reactivation plays + 2026 incumbents/failure frontier) for the Phase-B diagnosis layer | High value — structured, cited, honestly-flagged digest (the discrimination test; the actively-stuck/ghosted discriminator; platform-side blockers as the under-modeled category). Saved to `docs/research/merchant-activation-domain-2026-06-19.md`; drove `lib/domain/diagnosis.ts`. Read-only + quarantined. |

*Rows are appended as work proceeds (e.g., during the rebuild: which skills/tools the build actually used).*

### A3-7 (live cross-family run, 2026-06-28)

| **advisor** (stronger-model reviewer) | Used ×3 | 2026-06-28 | (1) before the run — reshaped the approach (three different mechanisms per agent, not one ablation; reuse the existing tune/test partition for K; the snapshot-overwrite + arm-check blindspots); (2) after run #1 — proved the determined-fallback, decoupled it from the Router regression-risk, named the pre-reg-vs-measured gap + the Codex-currency process gap; (3) after run #3 — the decisive **two-deliverable** reframe (labels CLEAN + run-independent vs convergence INCOMPLETE/K-vacuous), elevated the parse-failure as a drafter finding, sequenced drafter-fix-before-rerun. | Highest-leverage at every approach boundary; each call caught a real structural/honesty risk before it shipped. |
| **WebSearch / WebFetch** (live freshness) | Used | 2026-06-28 | RULES §6 freshness for `gemini-2.5-flash` — availability + the 2026-10-16 shutdown + pricing ($0.30/$2.50 per M) vs the official ai.google.dev pricing + deprecations pages + a corroborating source. | Confirmed the pinned `pricing.ts` table; date-anchored; surfaced the shutdown as an upgrade trigger. |
| **codex-guarded** (shared seat → gpt-5.5 @ xhigh) | Used ×2 | 2026-06-28 | The mandatory cross-model review (BLOCK 1 P1 + 3 P2) + a CONFIRMING pass on the final changed artifact (BLOCK 2 P2). | Earned its keep — the P1 (`selfCorrected` `.some()` overcount) was a real metric bug CI couldn't catch; the confirming pass caught the standalone-snapshot K-pass overclaim. Both reconciled primary-model-final. |
| **acceptance-gate** (subagent, ran ×2) | Used | 2026-06-28 | The ship gate (BLOCK on 2 process gates → re-judged with the Codex+verify evidence → SHIP 5/5). | Independent maker≠judge; file-verified the fixes rather than trusting the coordinator; flagged the metric-change-without-its-own-red-green for the next slice. |
| **custom $0 pre-spend gates** (`gemini-preflight.mjs` + a Groq window probe + an arm-check) | Used | 2026-06-28 | Gemini key/model preflight; the `crossFamilyReady` four-boolean arm-check (proved the live suite would RUN, not skip); a 1-call Groq rate-limit-header probe before each re-run. | Kept the live run from mis-arming/mis-spending. Never read/printed the real `.env` (Law 11); live AI armed via `ENABLE_LIVE_AI=true` CLI override only. |
| **the live harness** (`evals/agent-loop.live.test.ts`, 3 runs) | Used | 2026-06-28 | The metered cross-family run (Gemini drafter ⊥ Groq critics). | ~$0.046 total over 3 runs (« $5). Run #2 surfaced an over-strict assert; run #3 (corrected metric) surfaced the ~75% Gemini structured-output parse-failure finding. |

### SLICE 1 (drafter-reliability fix, 2026-06-29)

| Tool / skill | Status | Date | What for | Value |
| --- | --- | --- | --- | --- |
| **codex-guarded** (shared seat → gpt-5.5 @ xhigh) | Used ×5 | 2026-06-29 | The mandatory cross-model gate: changed-files review (BLOCK 4) → confirm-1 (BLOCK 1) → confirm-2 (BLOCK 1+P3) → confirm-3 interrupted (seat-limited, flagged 3 comments) → confirm-3 final (BLOCK 1 P2). | Earned its keep heavily — each pass found a DISTINCT real budget-integrity issue a green CI cannot catch (SDK `maxRetries:2` escape, unpriced reasoning tokens, soft-vs-hard thinking-budget overclaim → post-call overflow stop, residual "true upper bound" comments). The final pass CLEAN-confirmed the mechanism (bounded, fail-closed, differential untouched). All reconciled primary-model-final. |
| **acceptance-gate** (subagent) | Used | 2026-06-29 | The ship gate re-stamp after gate-2 cleared. | Independent maker≠judge; gates 1-5 PASS; its only BLOCK was a stale-state-doc defect (docs still said gate-2 NAMED-OPEN + sent the next session to re-run a discharged Codex pass) — caught a real ~200k-token waste + a self-contradicting record before commit. It file-verified that the gate-2 rewordings landed ("true upper bound" gone from all lib/evals .ts) rather than trusting the coordinator. |
| **Monitor / background Bash** | Used | 2026-06-29 | Awaited the codex seat smoke-test, the confirming pass, and `npm run verify`/vitest without blocking. | Kept the long-running cross-model + verify steps off the critical path; surfaced raw seat output verbatim. |

