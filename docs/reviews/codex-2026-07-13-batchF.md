# Batch F — changed-files review (2026-07-13)

- **Reviewer:** ONE gpt-5.6-sol@high via `~/claude-os/bin/codex-guarded`, probe-first (probe SEAT_OK, 12,104 tok; one `Not inside a trusted directory` arg retry raw on record — the recorded batch-B/D CLI class; one transient MCP transport reconnect error raw in the log). Review run: 1,79,396 tokens. Raw: `codex-2026-07-13-batchF-raw.log` (2,063 lines).
- **Scope:** the committed range `f1c0139..4253057` — `a8f6c5f` (B-2 mockup, previously unbatched) · `36f488c` (session-8 decision rows) · `4246091` (B-2 adoption site slice) · `4253057` (playground commission slice, **built inline after a builder seat-death — reviewed at maximum skepticism per the maker≠judge brief**). The parallel sol lane's uncommitted worktree files were explicitly out of scope.
- **Reviewer's own validation:** clean committed-snapshot typecheck + lint + packs + legacy + full vitest re-run (its packs count 495 = the committed snapshot WITHOUT the sol lane's two untracked mockups — consistent with our dirty-tree 497). Frozen B/B-2 blocks, protected paths, the evals/ allowlist, the exact CLI seam composition, `simulated:true`, and C10 inclusion all independently CONFIRMED. Full build/e2e not repeated in its sandbox (pre-existing `next/font/google` fetch blocked there); our own e2e runs stand (16/16 both modes, re-run post-fix).

## VERDICT: REVISE — 0 P1 · 6 P2 · 0 P3 → reconciliation: ALL 6 ACCEPTED-FIXED, 0 refuted

| # | Finding (sol) | Adjudication | Fix |
|---|---|---|---|
| 1 | `.lp-btn` 8px radius + decorative normal/hover shadows violate the stated ≤6px/no-shadow contract the B-2 surfaces claim | CONFIRMED (globals.css:952-981) | Radius → 6px; both box-shadows removed; box-shadow dropped from the transition list; comment names the fix |
| 2 | The site-slice commit's "ONE prose change" claim is inaccurate — the catch section added new visible prose | CONFIRMED — the claim was true only of PRE-EXISTING prose | Correction recorded in the journal, task-log, PROJECT_STATE, and this record (commit messages are never rewritten) |
| 3 | The import-graph browser-safety "proof" is FAIL-OPEN: the regex misses `import("x")`, `require("x")`, TS import-equals; bare/unresolved specifiers silently skipped | CONFIRMED — sol's direct probes showed zero matches on all three hidden forms | Scanner rebuilt fail-closed: four import-form regexes; the COMPLETE `builtinModules` list (+`node:` prefixes); unresolved relative/alias imports FAIL; non-allowlisted bare specifiers FAIL; a scanner-bites self-test asserts every hidden form is detected (12/12 unit green) |
| 4 | `{"items":[null]}` escapes the promised "No verdict" error path — the adapter throws on property access, the client has no catch | CONFIRMED | `parseAcpFeedText` now validates every row (non-null object + string `item_id`) with the row index named; the client wraps the engine call in a defensive catch; unit tests (null row, array row, id-less row, numeric id) + the wrong-FIELD-VALUES-still-verify distinction + an e2e null-row case, all green |
| 5 | The edited-feed e2e overclaims "proof that replay is impossible" — one known edit + a generic assertion could be canned | CONFIRMED as a claim-strength defect | Test renamed to "input-sensitivity evidence"; two distinct edits; a planted arbitrary price (8642.31) asserted via its engine-derived receipt (with the corrected understanding that a REPLACED price keeps the tally at 16 — the receipt, not the tally, is the evidence); the byte-level proof is delegated to the golden-equality vitest suite, stated in the test comment |
| 6 | Continuity records stale at committed HEAD (CURRENT_TASK/HANDOFF/PROJECT_STATE/task-log said the picks were still awaited; no journal entries for two meaningful slices) | CONFIRMED — a real contract violation | Full state sync in the reconciliation commit: all five docs carry the session-8 primary-pipeline record (alongside the parallel sol lane's blocks, preserved verbatim) |

**Post-fix gates (this reconciliation, dirty tree incl. the sol lane's 2 on-disk mockups):** verify exit 0 = vitest 1170+7 · legacy 306+5 · e2e 16/16 dev + 16/16 artifact · packs 497/497 · playground unit suite 12/12 · playground e2e 3/3.
