# Grill — A2 single-agent verify-and-self-correct loop (gate 1)

- **Date:** 2026-06-26
- **Target:** the A2 loop (`lib/agents/loop/*`, `lib/agents/groq*.ts`) + its GO/NO-GO evidence (`lib/data/agent-loop.snapshot.json`, the self-correction claim).
- **Method:** adversarial probe for hand-waving / overclaim / weak evidence. Each probe → finding → disposition. (Complements the independent cross-model review in `codex-2026-06-26-a1-a2.md` and the acceptance-gate, which gate 1 is distinct from.)

| # | Probe (adversarial) | Finding | Disposition |
|---|---|---|---|
| 1 | **"You claim the agent catches its own lies — but the drafter AND judge are the same model (Groq gpt-oss-120b). Isn't that circular?"** | Legitimate. Same-family verify (R-LOOP-5). | **Not hidden — it is the central caveat.** A2 asserts loop **CONVERGENCE/machinery**, NOT calibrated cross-family faithfulness. The honesty note is stamped in `trajectory.ts` (`A2_HONESTY_NOTE`), the orchestrator, and the snapshot provenance. Cross-family (Gemini drafts → Groq judges) is restored at A3; the calibrated faithfulness number is the P3 judge, not this. The claim is "the integrated loop converges," never "calibrated faithfulness." |
| 2 | **"Is K=7 a real bar or a number you picked to pass?"** | K=7 (≥7/9) is a **pre-registered** floor, set BEFORE reading any run, grounded as "a clear majority + margin" (spec §6.2). | **Defensible, but it is the OWNER's bar to ratify at the GO/NO-GO** (flagged as such). The recorded operating performance is **7/9 then 8/9** across runs — both clear the floor. Surface K to the owner for confirmation, not asserted as settled. |
| 3 | **"Does one un-reproduced run carry the headline?"** | No. | The live R-LOOP-10 has now been run **three times** (7/9, 8/9, + this re-run under the tightened metric). Run-variance is real (Groq temp-0 is not bit-deterministic) and **disclosed**; the claim is "≥7/9 across runs, directional," not a single number. |
| 4 | **"`8/9` sounds like 89% faithfulness. Overclaim?"** | Risk if quoted bare. | Framed as **convergence + same-family detection, directional, n=9** — never a faithfulness percentage. n=9 held-out is small (R-HON-1); the ~100+ floor is the eventual validation target; no public "calibrated" claim ships off n=9. |
| 5 | **"What about the fabrications it can't fix?"** | 1–2 per run (timeline / invented-specific) hit max-iterations. | **The safety property is the answer:** every un-fixed item is **HELD** (`drafted`), **never `simulated_sent`** — verified in the snapshot (`P-specific-4`, `P-timeline-*`) and locked by the fail-closed + recommend-not-decide tests. The loop is allowed to fail; it is NOT allowed to send a fabrication. |
| 6 | **"Could the agent's recommendation sneak a send through?"** | No. | `assertEligibilityUntouched()` hard-throws on any eligibility mutation; the only send path is the deterministic `simulate_send` (recomputes `computeSendEligible`); the recommendation is never an input. Independently affirmed by Codex + the acceptance-gate; test-locked (R-LOOP-8b). |
| 7 | **"Did you quietly change the deterministic core to make this work?"** | No. | `git` shows 0 changed files under `lib/core`/`scripts`/oracle/gold; the byte-for-byte differential (`tools-differential` + `core-differential`) stays green. Add-alongside only. |

## Grill verdict
**No overclaim or hand-waving found.** The headline ("the agent self-corrects most of its own fabrications and never sends the ones it can't fix") is **honestly bounded** by the stated caveats (same-family = convergence-not-faithfulness; directional n=9; held-not-sent on misses). **One genuine owner action:** ratify the operating **K** at the GO/NO-GO (it is the owner's bar, not mine to settle). Everything else is recorded and reproduced.
