# Batch-B evidence record (plan v3.3 S4+S4b + reconciliation) — 2026-07-10/11

The committed evidence batch-B finding #6 required: SHA boundaries, exact commands, outputs/exit codes, scan results, and the red-green chronology. Finding #8's process record is §0.

## 0. Professional Process Applied (the slice-group-B execution session's block, preserved per finding #8)

- **Task type:** implementation (de-brand sweep + golden/template identity migration) · **Stage:** execution, slice group B (plan v3.3 §S4/§S4b) · **Risk:** HIGH (public-facing claims + freeze-boundary golden migration) → **FULL mode**
- **Effort:** high (plan route tags `fable(main)@high` both slices); review at the owner-sanctioned sol@high
- **Basis:** plan v3.3 (S0-APPROVED, 4 rounds); unified-review findings F-16/AP-08, F-13/Gate-P2, F-03/AP-09; SPEC-ADHERENCE mode per the owner's resume word
- **Validation:** inventory-first git-provable ordering · zero-brand grep vs inventory · out/ scan vs declared allowlist · verify floor 1005+7 · legacy 306+5 · C10 · protected-path allowlisted diff · red-green on the golden migration
- **Artifact policy:** inventory + mockups/README = commit; regenerated goldens = commit (sanctioned, allowlisted); scratch regen script = scratchpad only, never committed; L-2 send record = never touched
- **Codex review:** batch B, dedicated freeze-boundary, closed before push · **Human approval:** the owner's recorded resume directive (HANDOFF top block) is the authorization; push only after batch B reconciles; NO DEPLOY (owner word)
- Ceremony: harness advisor() down (24th consecutive session, surfaced); frontier-advisor consult at the pre-approach boundary = **PROCEED-WITH-CONSTRAINTS** (①  GitHub repo slug checked, not just About — done, clean; ② per-edited-line no-citation assertions in the inventory — done; ③ the S4b allowlist enumerated at 7 paths incl. expected-demo.json — done).

## 1. SHA boundaries

| Point | SHA |
|---|---|
| Batch base (= batch-A wrap, pushed) | `d83df29` |
| S4 inventory (committed FIRST, own commit) | `859c070` |
| S4 sweep (6 edits) | `2d67cb0` |
| S4b decision-log row (own commit, precedes edits) | `3135d86` |
| S4b migration + golden regen | `96d1cf7` |
| Batch-B reconciliation (all 8 findings fixed) | the commit carrying this file |

## 2. Gate runs (exact commands; all exit codes 0; logs quoted from live runs)

| When | Command | Result |
|---|---|---|
| post-S4 sweep | `npm run verify` | exit 0 — `Tests 1005 passed \| 7 skipped (1012)` (floor 1005+7 held) |
| post-S4 sweep | `npm run test:legacy` | exit 0 — `Tests 306 passed \| 5 skipped (311)` |
| post-S4 sweep | `npm run build` | exit 0 (static export prerendered) |
| S4b RED (post-template-edit, pre-regen) | `npx vitest run evals/delivery/delivery.test.ts` | **`Tests 4 failed \| 12 passed (16)`** — exactly the 3 slack + 1 email golden byte-identity tests failed; all 12 invariants passed |
| S4b regen | scratch script through the REAL builders (`callTool` → `buildSlackReportPayload`/`serializeSlackPayload`, `buildEmailReportMessage` with the test's exact fixtures + `FIXED_DATE`) + `npm run fixtures:demo` | 4 gold files rewritten; `expected-demo.json` regenerated **byte-identical** (absent from `git status`), `expected-demo.txt` changed on the title line only |
| S4b GREEN | `npx vitest run evals/delivery/delivery.test.ts` | `Tests 16 passed (16)` |
| post-S4b | `npm run verify` / `npm run test:legacy` | exit 0 — 1005+7 / exit 0 — 306+5 |
| post-reconciliation | `npx vitest run evals/n8n/` | `Tests 6 passed (6)` (workflow display-name migration re-tested) |
| post-reconciliation | `npm run verify` / `npm run test:legacy` / `npm run build` | exit 0 — **1005+7** / exit 0 — **306+5** / exit 0 |

## 3. Scan results

- **Source scan (corrected method — see the inventory §Method for the `\b` defect batch B caught):** 178 files carry ≥1 brand hit; every file maps to an inventory class; the EDIT class total is 6 lines (all applied at `2d67cb0`); the 16 files the defective scan missed are all keep-class (inventory §3b).
- **`out/` scan (fresh build, post-reconciliation; html+txt):** DataSF 267 · DoorDash 158 · Grubhub 158 · Uber Eats 157 · Stripe 47 · Square 7 · Google 3 · OpenAI 1 — every hit maps to the inventory §6 allowlist (S2 footer disclaimer on every page, frozen SIMULATED banner, ReportView protocol labels, DataSF provenance, legacy diagnosis strings rendered on merchant surfaces, Next.js vendored bot-regex). **"Commerce Truth Audit" residue in `out/`: ZERO.**
- **Protected paths:** `git diff --name-only 3135d86..96d1cf7 -- legacy/ evals/ fixtures/` = exactly the 6 recorded allowlist files (4× `evals/delivery/gold/*` + `fixtures/synthetic-restaurant/expected-demo.txt` + `evals/delivery/delivery.test.ts`); the 7th allowlisted path (`expected-demo.json`) regenerated byte-identical. S4's range: EMPTY. The L-2 send record: untouched across the whole batch (`git diff d83df29..HEAD -- docs/reviews/l2-slack-one-shot-2026-07-09T15-06-01-054Z.md` empty).
- **Golden byte proof:** `git diff -U0` over the 5 changed goldens shows every changed line is the `Commerce Truth Audit` → `Curbside Commons` swap and nothing else (no QP reflow in the .eml; the base64 report.json attachment unchanged — the canonical payload carries no product name).
- **GitHub surfaces (live `gh repo view`, 2026-07-10):** About description clean, slug `sharanlabs/curbside-commons` clean, topics none.

## 4. Batch-B review + reconciliation chronology

Probe (`gpt-5.6-sol` low) → SEAT_OK → heavy run (ONE `gpt-5.6-sol@high` via `~/claude-os/bin/codex-guarded`, read-only, session `019f4ebf-ca00-7b22-ac6d-9f23c55e78b6`) → stopped at the repo's startup checkpoint (correctly flagged the packet's "5 commits" as 4) → first `exec resume` died on a CLI arg error (`--sandbox` not accepted by `resume`; raw error preserved in the raw log; zero tokens spent) → corrected resume completed. **VERDICT: REVISE — 0 P1 + 7 P2 + 1 P3 → ALL 8 ACCEPTED, 0 refuted** (each verified against the repo before acceptance; the reviewer independently confirmed typecheck, lint, 1005+7, legacy 306+5, 16/16 delivery tests, exact golden substitutions, and unchanged protected blobs). Fix details: `docs/reviews/codex-2026-07-10-batchB.md`. Raw stream: `docs/reviews/codex-2026-07-10-batchB-raw.log`.

Note on the reviewer's build attempt: its sandboxed build failed on three Google-Fonts fetches (its network is restricted); the builds in §2 ran in the normal environment and passed. The Google-Fonts-at-build dependency is the known AP-14 item — the S6b fonts decision (self-host or recorded exception) covers it.
