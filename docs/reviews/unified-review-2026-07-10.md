# Unified fresh review — 2026-07-10 (dual-flagship; absorbs the 26 plan-v2 findings)

**Trigger:** owner directive 2026-07-10 (decision-log rows this date) — fresh whole-project review under the updated doctrine (MODEL-ROUTING 2026-07-10: Codex seat `gpt-5.6-sol` @ `high` sanctioned; Haiku OFF) + newly-released models, before deploy. Review scope fork answer: ABSORB (the 26 unreconciled 2026-07-09 findings are input, re-validated here).
**HEAD at review:** `fbb4f67` (tree clean except state-doc appends). **Floors re-verified live this session:** `npm run verify` exit 0 = **961 passed + 7 skipped** · `npm run test:legacy` exit 0 = **306 passed + 5 skipped**.
**Seat events (honest record):** all six Phase-1 subagent legs died TWICE on the shared subagent seat limit (raw verbatim: "You've hit your session limit · resets 3:30pm (America/New_York)", then "…resets 4:30pm (America/New_York)"); one launch additionally blocked by a transient harness-classifier outage. Owner-confirmed retry consumed → **Fable-side review converted INLINE on the main seat per the NO-WAIT precedent** (maker≠judge mitigation = the independent Sol cross-model run below + the Phase-3/4 gates). Harness advisor() down, 22nd consecutive session.

## Side 1 — Fable (inline, this session; every claim verified against the working tree)

### Integrity backbone (R2) — ALL PASS
- HEAD `fbb4f67`; only `docs/decision-log.md` dirty (this session's authorized rows).
- verify 961+7 exit 0 · test:legacy 306+5 exit 0 (raw tails in /tmp/inline-verify.log, /tmp/inline-legacy.log this session).
- **Protected paths FROZEN:** `git diff 906b522..HEAD --stat -- legacy/ evals/ fixtures/` = EMPTY.
- Eval-locks present: `evals/gold/fee-classifier-calibration.lock.test.ts` + `fee-classifier-recalibration.lock.test.ts` + `evals/crew/l1-live-lock.test.ts`. Frozen artifacts present: both calibration snapshots, L-2 send record, all three l1-live gold files.

### Findings ledger (F-xx; maps to the 2026-07-09 AP/GATE findings)
| id | maps | verdict | severity | evidence (this session) | fix sketch |
|---|---|---|---|---|---|
| F-01 | AP-01 | **CONFIRMED** | **P1** | README.md:42 "947 passed + 6 skipped" (true: 961+7); PLAIN-ENGLISH.md status block ends 2026-07-07 "Still true: nothing can send a message" + classifier "deferred" (send DELIVERED 07-09; label EARNED 21/21); PUBLICATION.md:33/:46 "20/21…DEFERRED" + "743 tests"; WHY.md:5 "the public deploy is the remaining owner-gated step" (deployed 07-08) | Public-doc reconciliation slice BEFORE/WITH de-brand; update or clearly date-stamp as historical |
| F-02 | AP-02 | **CONFIRMED + ESCALATED** | **P1** | app/layout.tsx:50-70 footer describes the LEGACY activation product entirely ("synthetic activation state", DataSF adapter, recorded Gemini fixture) — the wrong product generation for the truth-audit site — AND says "not … real sends" (false since 07-09) | Rewrite footer for the CURRENT platform, scoped to the web replay ("this site initiates no sends"); keep non-affiliation sentence (e2e asserts it); route through the F-10 freeze-reversal row |
| F-03 | AP-09 | CONFIRMED | P2 | "Commerce Truth Audit" baked in: render-text.ts:47, slack.ts:49/:132, email.ts:129/:143, expected-demo.txt:2 (golden-frozen) | Owner/orchestrator decision: historical artifact vs current output → if current, versioned template change + sanctioned golden regen; never touch the actual send record |
| F-04 | AP-10 | CONFIRMED | P2 | honesty-c10.test.ts scans fixed arrays (demoScanned/publicProse/scannedFiles) — landing page, layout footer, mockups outside the scan set; report/demo banners independently maintained | Extend C10 scan set over landing+footer (+ mockup claim test); single-source or parity-test the banner |
| F-05 | AP-11 | CONFIRMED | P2 | test:legacy pins the oracle differential, not arbitrary legacy/ edits; only an empty git diff proves the freeze (run + EMPTY this session) | Slice-end gate = empty diff on legacy/** evals/** fixtures/** vs slice-start SHA |
| F-06 | AP-12 | CONFIRMED | P2 | package.json:18 verify = typecheck+lint+test+build; e2e ONLY in verify:full; playwright.config.ts:24 `reuseExistingServer: !process.env.CI` (stale local server risk); no e2e against out/ | Deploy gate: build once → hash out/ → e2e with a fresh server (or CI=1) → deploy that exact artifact |
| F-07 | AP-04 | **CONFIRMED** | **P1** | No wrangler dep/pin anywhere (npx-fetched); Cloudflare docs (fetched 2026-07-10): deploy WITHOUT --branch = production by default; --branch targets a branch env (production-ness = cached project setting). The recorded recipe's `--branch main` semantics are UNPROVEN from the repo | Preflight `wrangler pages project` state → preview deploy first → confirmed-production deploy → smoke; record wrangler version |
| F-08 | AP-13 | PARTIAL | P2 | 9 page.tsx sources incl. dynamic merchant/[id]; exported-file manifest must be counted from the actual out/ build at deploy time | Deploy smoke = route manifest from out/ + 404 probe + rollback note |
| F-09 | AP-17 | **CONFIRMED** | **P1 (ordering)** | Emil repo NOT vendored: no `~/claude-os/library/_external-skills/emilkowalski-*`, no registry line in source-registry/design.md; only the thin local `~/.claude/skills/emilkowalski-motion` exists | VET+PIN (commit SHA, MIT notice, neutralize the scripted-greeting payload) BEFORE the design brief consumes any of it |
| F-10 | AP-05, AP-03 | **MOOTED-with-row / STANDS** | P2 | AP-05 mooted: the 2026-07-10 owner design direction is recorded (decision-log this date) — new-lane exception explicit (pure white + micro/macro motion supersede the Oxblood bans FOR THE SAMPLE LANE only; shipped site spec unchanged). AP-03 STANDS: plan-whole-site-redesign.md byte-freezes the SIMULATED banner + footer; F-02's footer rewrite REQUIRES a recorded freeze-reversal + a semantic disclosure contract + parity test first | Freeze-reversal decision-log row + semantic contract + parity test precede any footer/banner edit |
| F-11 | AP-14 | STANDS | P2 | Zero-external-requests + system-fonts precedent (ultra-modern README); any web font in the new sample needs self-hosting or a recorded exception | Decide in the design brief |
| F-12 | AP-15 | STANDS | P3 | Parent mockups' anchor ids incompatible (#main/#top/#verify) | Canonical fragment matrix in the brief |
| F-13 | GATE-P2 | CONFIRMED | P2 | README.md:59, WHY.md:11/:15, PUBLICATION.md:15 dated factual market claims naming companies | Third de-brand class: genericize prose, keep named citations in research digests |
| F-14 | NEW | — | P2 | The owner-liked "hold and release" interaction exists NOWHERE in repo/mockups/Emil-local-skill (searched) | Confirm the reference with the owner at design-brief time, or design our own within the micro-interaction vocabulary |
| F-15 | AP-06 | STANDS | P2 | S-11 closure wording must be "final non-commercial display brand, no clearance claimed"; TESS retired → current USPTO Trademark Search wording | Fold into Slice-A wording |
| F-16 | AP-08 | STANDS | P2 | Five-name grep misses OpenAI/Stripe/Google/DataSF (DataSF literally in the live footer, seen this session), `Uber&nbsp;Eats` encodings, mockup parents (swiss/premium/story-flow.html carry names) | Tracked inventory: edit / factual-attribution / frozen-archive / historical + out/ scan |
| F-17 | AP-07 | STANDS | P2 | Plan v2 lacks route tags on slices 0/A/B/D/E + RULES §9 closure steps; also now stale vs the 07-10 Sol@high doctrine | Plan v3 tags every step `[route | verify]` with current seats |
| F-18 | AP-16/18/19/20 | STAND (minor) | P3 | Receipts-block prerequisites/exit-semantics; dated source matrix for the design sweep; attribution precision; dual-flagship-before-deploy ordering | Fold into plan v3 |

### Unknowns map (blindspot leg — converted inline after 2× scout seat-death; condensed)
- **B-01 (git history):** de-brand edits surfaces, not history — brand names remain in commits forever; a rewrite would break every SHA-anchored gate record. Honest framing ("names removed from current surfaces; history preserved for provenance") beats scrubbing. OWNER should consciously accept this.
- **B-02 (README first-90-seconds):** 2026 hiring sources agree reviewers open the README and bounce fast; the strongest rare signal is a visible EVALS HARNESS + one-command demo. The current README leads with stale counts (F-01). The doc-reconciliation slice should restructure the top: one-command demo + eval-locks story first.
- **B-03 (site↔repo drift class):** with site live + repo public, every future repo change opens a NEW honesty gap class (deployed site says X, repo says Y). Cheap structural fix: a "deployed at commit <SHA>, <date>" line in the site footer, updated per deploy.
- **B-04 (the "run it again" question):** both classifier splits are EXPOSED; a hiring reviewer asking "re-run it live" must get the honest prepared answer (fresh pre-registration required, why that's the point). Add one FAQ line to SHOWCASE.
- **B-05 (vendored-content licensing):** anything vendored into the PUBLIC repo (Emil skills, fonts) carries license/notice obligations (MIT retention; font licenses if self-hosted per F-11).

### Research digest (inline; live-sourced, dated 2026-07-10; reuse-first honored)
- **Design references:** Anthropic re-release pages — anthropic.com/news/redeploying-fable-5 + anthropic.com/news/claude-fable-5-mythos-5; OpenAI — openai.com/index/previewing-gpt-5-6-sol. **Motion/icon specifics are UNVERIFIABLE from text fetch** — the builder must study these pages LIVE at build time (brief carries the URLs). 2026 premium-motion genre markers (search sweep): icon-driven vertical scroll, pure-white grounds, restraint + one signature interaction — consistent with the owner's direction.
- **RAG $0 stack (candidate #1 facts):** small-corpus practitioner consensus (rxdb.info article, redhopai.com Node-RAG guide, github.com/Stevenic/vectra, freeapihub 2026 roundup): local embeddings via Transformers.js-class models (e.g. nomic-embed) run $0 offline in Node; stores for <1M chunks: sqlite-vec / Vectra (pure JS, file-based) / LanceDB; for a corpus this small, BM25 or exact in-memory cosine is legitimately enough — no external service needed. Fits the $0/offline/import-graph-proof pattern exactly.
- **Hiring signal (candidate ranking input; ≥3 sources, ≥2 platforms):** dev.to, aitechconnect.in, dataskew.io, agenticcareers.co (all 2026): the EVALS HARNESS is the rarest/strongest signal ("eval literacy — the single biggest signal"); RAG the most in-demand skill, differentiated by its EVAL layer; agent reliability + MCP integration + cost optimization + observability on the checklist. This repo already over-delivers on evals/agents/MCP/cost — the gaps are RAG and visible observability.
- **Slack free-tier / Resend limits:** NOT fetched this pass (seat economy) — **UNVERIFIED; fetch at Phase 3 if the owner picks those candidates.**

### Enhancement ranking (Fable @ xhigh judgment; Sol second opinion in Side 2)
1. **RAG lane with a pre-registered retrieval eval** over the committed regulatory/spec corpus — fills the one missing floor capability; top 2026 hiring signal; $0 offline; maximally on-brand (the repo's eval discipline applied to RAG).
2. **MCP-Slack interactive approvals** — live human-gate surface; owner already floated; needs free Slack app + owner-gated arming.
3. **Eval/observability dashboard page** — renders the existing eval wealth (locks, matrices, snapshots, $0-enforcement) on the site; cheap, pure offline, visualizes the strongest signal. Can carry B-03's deployed-SHA line.
4. **Email one-shot leg** — builders ready; marginal over the earned Slack send.

## Side 2 — Sol (gpt-5.6-sol @ high, codex-guarded, read-only): **VERDICT: REVISE**
Raw preserved: `docs/reviews/codex-2026-07-10-unified-review-raw.md`. Ops note: the first run stopped at the repo's startup-contract approval gate (it read RULES.md — correct behavior); approved via `codex exec resume` on the same thread (one arg-order retry, exit 2, $0). Usage: run-1 in 242,958 (192,000 cached) / out 2,183; run-2 ~298k thread total.
- **Section A (the 26 prior findings): 17 STANDS/STANDS-MODIFIED · 3 MOOTED (AP-05/19/20) · 0 WRONG** — near-exact agreement with the Fable side (F-01..F-18); the cross-seat overlap is high-confidence signal.
- **Section B: 15 NEW findings** — NEW-01 **two products under one identity** (landing + console/eval/metrics/audit/cost/merchant present the LEGACY activation product while README/report/demo present the truth-audit engine) · NEW-02 full-claim-coverage overclaim on the landing · NEW-03 "machine-checked in CI" with no CI workflow · NEW-04 "empty seat / nobody built it" exclusivity claims vs the binding 2026-06-22 positioning decision (all four **P1, independently CONFIRMED by Fable this session** with fresh file evidence) · NEW-05..09 P2 (provenance scoping, AI-inventory accuracy, e2e locks the obsolete landing, no deployed-SHA on site [= Fable B-03], CLI classifier-label clarity) · NEW-10..15 P3 (ARIA tabs, package name, mockup inventory, npm ci, header posture, glossary freshness).
- **Section C (ranking second opinion):** dashboard→#1 (the /eval,/metrics,/cost surfaces currently render LEGACY records — replacing them fixes part of NEW-01 AND surfaces the earned evidence), RAG #2-with-conditions (citation-required · abstaining · advisory · pre-registered floors), NEW candidate adversarial entity-resolution, MCP-Slack demoted (interactive ingress ≠ prototype identity; offline signed-action simulator first), email #5.

## Reconciliation (primary-model-final, Fable — 2026-07-10)
- **Section A adopted wholesale** — zero cross-seat disagreements on the 26; the three MOOTED calls match the Fable side exactly (owner's 07-10 design row).
- **NEW-01..04 CONFIRMED** by direct inspection (page.tsx hero = activation story; "machine-checked in CI" vs no .github/workflows; PUBLICATION.md:9 + PLAIN-ENGLISH.md:35/:56 exclusivity claims vs decision-log:57). **NEW-05..15 ACCEPTED-PLAUSIBLE** (evidence-cited, consistent with confirmed findings; each re-verified red-green at its fix slice — none is load-bearing for the menu shape).
- **Ranking reconciled (Fable final):** ① truth-engine eval/observability dashboard (Sol's promotion accepted — it double-counts: fixes part of the identity split AND surfaces the strongest hiring signal) · ② RAG lane with Sol's conditions folded in (the pre-registered-floors design was already the Fable framing; conditions accepted) · ③ MCP-Slack as an OFFLINE signed-approval simulator first (Sol's infra objection accepted; the live lane stays a future owner word) · ④ adversarial entity-resolution (real capability, heavier, less showcase-aligned than ①②) · ⑤ email one-shot. **Real CI (GitHub Actions, free)** promoted OUT of the ranking into the Tier-1 fix set — it converts the false NEW-03 claim into a true one and adds the "regression gate in CI" hiring signal at ~zero cost.
- **Unified verdict: REVISE confirmed on both seats** → the fix set becomes the Phase-2 options menu (Tier 1 must-do honesty/integrity · Tier 2 corrected planned slices · Tier 3 ranked enhancements); plan v3 folds the owner's picks; the Codex approval loop re-runs over v3 before execution.
