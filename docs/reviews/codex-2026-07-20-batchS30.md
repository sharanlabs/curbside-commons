# Covering sol batch — session 30 (2026-07-20)

**Dispatch:** gpt-5.6-sol via codex-guarded, read-only sandbox, language/narrative/
blindspots lanes (design excluded — owner standing rule). One startup-gate resume
(sol's own protocol; per the owner's 2026-07-20 word, sol's process was otherwise
untouched). Raw: `scratchpad/s30/sol-batch-out.md` + `sol-batch-out2.md`.
**Adjudication:** Fable, primary-model-final. 29 language findings + 6
counterfactuals + 16 capability verifications.

## Verdict: REVISE → reconciled

**Accepted / accepted-modified (applied this session):**
- #5 trust-fact tests sentence smoothed (metaphor → function).
- #6 /report "each with its receipt" scoped to what the Ledger renders (index rows + the one full receipt).
- #8 /fees "the caps are the whole test" oversimplification dropped (the 11/6 boundary section carries the truth).
- #9 months foot now names the four outcomes explicitly (fail / pass / cured / conditional).
- #10-adjacent: felt note now names the statement shape hint up front (modified — sol's "simulated/illustrative" qualifiers stripped per the real-product voice directive; the meta gate still explains itself on error).
- #11 "STATEMENT READ IN FULL" → "STATEMENT AUDIT" (six rules need outside evidence; "in full" overstated).
- #12 FeeJewel close trimmed to auditable fact (refund window closed, none recorded).
- #13 playground lede scoped: "No network requests during a run" (page load is network; the run is the boundary). Chips = adopted chrome, unchanged.
- #15 "only the words you supply" → what is actually examined (the pasted document, locally, vs the pinned catalog).
- #16 receipt-anatomy sentence unpacked (claim / asserted value / reference row / rule).
- #17 duplicate print sentence removed ("The receipt is the product" stays — spec motto).
- #19 **"Replay the calibration" → "Replay the count-up"** (the button animates stored figures; it does not rerun calibration — honesty catch).
- #20 proof consoles: "is a replay of that run" → "carries the recorded result" (REPLAY chrome unchanged — the accepted console deviation).
- #21 retrieval rail sentence untangled.
- #25 **/docs get_rule label corrected** — returns the codified rule + source-clause citation, not statutory text.
- #27 /docs what-is-real split: fee rules codified from real published law; conformance uses real protocol schemas (the listings pack's own rules are neither — sol's precision catch; pack binding /real published law/ preserved).
- #29 Nav /report readout gains FILE A (one-case grammar symmetry with FILE B).
- **#93 wrong-state print bug FIXED:** with an invalid price the receipt no longer falls back to the reference findings under a NO RUN stamp — it renders the awaiting state only.

**Refuted (with grounds):**
- #1/#2/#28 hero/metadata/empty-seat register — the owner-adopted W3 register and the empty-seat wedge (as-of-dated, research-verified) stand; the session-28 inspection refuted the same class (quiet honesty carries the boundary on /docs). 
- #3/#4/#7/#14/#18/#22/#24 — adopted design headings/chrome (v9/02/03/04 gated mockup language; design authority = Fable/owner; #22's foot text already states precisely "the record holds a single outbound send").
- #23 /docs "the reference" — the next sentence scopes it; REFERENCE is the chapter chrome.
- #26 "proves no send path is reachable" — a static import-closure check IS a proof over the committed artifact; the sentence scopes itself to the code.

**Recorded, no action (owner's standing choices):**
- #101 direct-entry readers can miss /docs "What is real" — background placement is the owner's explicit directive.
- #102 the "1,200+" tests figure is a pinned conservative floor (true at 1263); computing it at build is a recorded watch item.
- Counterfactuals (§2) — filed as input for future content iterations; no reflow of the adopted arc without an owner word.

Capabilities table (§3): 16 claims verified against code; the two deliverable
defects above were the only code-level catches; the rest confirmed the shipped
behavior or restated known register choices.
