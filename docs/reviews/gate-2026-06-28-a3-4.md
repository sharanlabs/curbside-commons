# Acceptance gate — A3-4 (Domain Critic as the 2nd VERIFY-phase critic, advisory)

**Date:** 2026-06-28 · **Judge:** `acceptance-gate` subagent (independent, read-only) · **Target:** the uncommitted A3-4 working-tree diff (5 modified + 2 new docs/eval) · **Decision authority:** primary-model-final (Opus 4.8).

## Verdict: BLOCK — gates 1/3/4/5 PASS; gate-2 (Codex) NAMED-OPEN until the round-3 re-confirm returns

The acceptance-gate confirmed the **substance is sound** and verified the three load-bearing claims against source: (a) the advisory invariant is structural (`verifyPassed` finalized before the domain critic; route uses `if (verifyPassed)`; the advisory test fails if domain gates the send); (b) cross-family is enforced by construction (`crossFamilyReady` four-conjunct gate incl. `resolvedDomainJudgeProvider()==="groq"`, the fail-closed throw, the per-item harness assert); (c) the anti-theater DEFER is honest (the mock ties the live judge's frozen F1; `domain_critic` correctly stays "tool").

**Gate panel:**
1. **grill — PASS** (basis on disk: §11 R-A3-1..9 + the advisor's anti-theater-eval-decides-the-label discipline + the Codex BLOCK that probed the joints; the A3-3 "DONE-while-pending" honesty defect was pre-empted).
2. **codex cross-model — FAIL / PENDING-HANDOFF (THE BLOCKER).** The load-bearing P1 round-2 fix (`fullyInjectedDI`, `||`→`&&` at `orchestrator.ts`) has **never been seen by Codex** — round-3 is seat-blocked. Decisive point: on THIS artifact the cross-model pass already earned its keep once (round-1 reconciliation *missed* the P1 that round-2 caught), so a maker-written regression is not a substitute for the cross-model judge seeing the patched logic. This is the **B2 pattern** (gate-2 named-open, flips to SHIP when the dated Codex re-confirm returns) — NOT the A1/A2 "proceed test-verified" pattern, which applied to *commits/build progress*, never to the acceptance-gate stamp.
3. **verify — PASS** *(verified from handed-off evidence — the gate read `/tmp/a3-4-verify-r2.log` directly: typecheck/lint clean, 285 passed | 5 skipped, build prerendered 28 pages; differential lane untouched; the anti-theater tie-claim is internally consistent with the frozen snapshot it reads). The two red-green cycles are described in the records, not pasted in that log; the gate holds no Bash.*
4. **enterprise+taste — PASS.**
5. **anti-slop — PASS** (prose; code-slop handoff owed for a formal score but nothing flagged on read).

## Flip condition (the gate's own pre-committed terms)

**Flips to SHIP 5/5 when the round-3 Codex re-confirm returns SHIP on the current diff** (seat resets ~7:25 PM ET). The re-confirm must see the `fullyInjectedDI` partial-DI fix, the per-iteration `lastDomain = null` reset, and the `crossFamilyReady` predicate, and confirm the residual P1 is resolved + the advisory invariant holds + the anti-theater DEFER is honest.

## Disposition (primary-model-final)

**Committed TEST-VERIFIED with gate-2 named-open** — the gate explicitly states this BLOCK does NOT undo the RESUME-DIRECTIVE-authorized commit or stop the build ("a legitimate owner choice with gate-2 named-open — just not 'gates passed'"). The **Codex round-3 re-confirm is a DATED OBLIGATION** before any irreversible step (push / the A3-7 live run). What this does NOT authorize: **push** (HELD — no remote), **deploy**, **public posting**, the **A3-7 live Gemini run** (owner-gated). All remain owner-gated.

Records: `docs/reviews/codex-2026-06-28-a3-4-domain-critic.md` (the 3-round Codex history).
