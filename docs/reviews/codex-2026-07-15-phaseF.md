# Phase F — redesign-lane covering review (2026-07-15, session 16)

- **Reviewer:** ONE gpt-5.6-sol@high via `~/claude-os/bin/codex-guarded`, probe-first (probe SEAT_OK 12,101 tok). The first run stopped at the repo's own startup-approval gate after 137,198 tok of grounding (recorded batch-E class) → `codex exec resume --last` completed the deep audit (605,127 tok). One CLI arg-retry on the resume (`--sandbox` unsupported after `resume`, batch-B/D class). Raw: `codex-2026-07-15-phaseF-raw.log` (24,344 lines).
- **Scope:** the ENTIRE uncommitted working tree vs HEAD `9bee351` — phase-E de-jargon closure, ST-1..ST-4 storyboard adoption, the bench opens-complete flip (owner pick, built this session red-green), the playground/browser-fixture infrastructure, the cruft deletion, the NYC rule-table freshness update, the Light Gauge sample, all doc/state updates.
- **The binding bar:** `docs/redesign-acceptance-criteria-2026-07-15.md` (the owner's 20-item spec + 5 product-derived principles) + the honesty line (C10 BANNED_CLAIMS; no real-brand/dev-jargon rendered copy; no invented business claims).
- **Reviewer's own validation:** clean tsc + lint + full vitest (1,174+7 at the pre-fix tree) + targeted C10; live LL79 source checks; computed contrast ratios. Its sandbox could not run `npm run build` (the pre-existing `next/font/google` build-time fetch) or bind a dev server — browser behavior verified on our side (evidence below).

## VERDICT: BLOCK — 5 P1 · 10 P2 · 4 P3 → reconciliation (primary-model-final, Fable adjudicating per the owner's 2026-07-15 routing word): 16 ACCEPTED-FIXED · 1 REFUTED · 2 DISCHARGED BY OWNER WORD

| # | Sev | Finding (sol) | Adjudication | Fix |
|---|---|---|---|---|
| 1 | P1 | De-jargon closure false: `/eval` renders `enhanced_service_fee` + "gold set" context; `/legacy/eval` "raw enum"; `/legacy/console` raw blocker codes; `/cost` "committed record" | ACCEPTED-SCOPED — real viewer-facing leaks at all four sites; the class token itself is source-bound by `dashboard-evidence.test.ts:80` (deliberate, principle 5) | Dashboard reason rephrased in plain framing (token kept, bound); console blocker codes through `dejargon()` + de-underscored; "raw enum"→"raw internal code"; "committed record"→"record kept in the project" ×3 pages; NEW pack `evals/packs/legacy-dejargon.test.ts` walks EVERY replay merchant |
| 2 | P1 | `/legacy/merchant` diagnosis fields bypass `dejargon()` — "Stripe"/"IRS" render | ACCEPTED (frozen `legacy/activation` supplies the strings; fix is display-layer only) | All four rendered diagnosis fields wrapped with `dejargon()`; Stripe/IRS/fraud-hold mapping rules added to `lib/legacy/display.ts`; e2e word-boundary leak assertions (`legacy.spec.ts`) + the per-merchant pack |
| 3 | P1 | Playground sample textarea shows "Phantom Platter (simulated ghost item)" | **REFUTED** — the title is a golden-bound CLAIM value (renaming breaks the byte-for-byte golden-equality proof that the browser runs the real engine — the slice's core evidence, `generate-browser-fixtures.mts:24-27`); the wording is data inside an editable feed document, consistent with the page's own simulated-sample label; every RENDERED receipt rewords via `cleanFinding()` (the /report + /demo convention) | none (design recorded here) |
| 4 | P1 | `/demo` lost its visible illustrative-data boundary | ACCEPTED — a real honesty regression of the freeze reversal | Boundary sentence added to the demo footer ("…invented for this demonstration; no real merchant's data appears") + a rendered e2e assertion (`canonical.spec.ts` demo test) |
| 5 | P1 | `mockups/proof-theatre-2026-07-13.html` carries real brands + dev copy | DISCHARGED BY OWNER WORD (same session): "that is my final delete old ones" — 8 superseded untracked proof-theatre samples deleted (byte copies archived to the session scratchpad first; they were never committed) | deletion + `mockups/README.md` rewrite |
| 6 | P2 | Replay staging incoherent: `aria-hidden` removes visible dimmed content; dim opacities fail 4.5:1 | ACCEPTED (a11y leg) + design-adjudicated (visual leg, owner's "for design let fable take the decision") — the aria staging rationale died with the idle state (the evidence is already exposed pre-replay) | All staged `aria-hidden` removed (`EvidenceBench.tsx`); the transient ~2.3s replay dim (inactive content) kept at low emphasis per the recorded storyboard lesson, settled state fully compliant |
| 7 | P2 | No-JS paths show dead controls on 6+ surfaces | ACCEPTED | `<noscript>` control-hiding: hero pause, bench replay, coverage tab row (+static sentence replaces the method word-buttons), report toggle (+which-surface note), playground actions (+plain requirement note), Light Gauge controls |
| 8 | P2 | Light Gauge receipt says "finding 01 of 16" (truth: 11) | DISCHARGED BY OWNER WORD (same session): "dont mention those numbers, do something else creative" — the number chrome was REMOVED in the creative rebuild, not corrected | creative rebuild (see the sample row) |
| 9 | P2 | C10's site-wide `out/` scan is stale/fail-open | ACCEPTED — a real honesty-gate hole | `ensureBuilt()` refuses an `out/` older than the newest source under app/components/lib (rebuilds); `verify` reordered build-before-test; tooth proven red-green (0.48s fresh vs 10.2s rebuild-on-stale) |
| 10 | P2 | Browser fixtures cast through `as unknown as` against required fields | ACCEPTED | Honest reconstruction with true values (`simulated: true`, projection provenance) + narrow `Pick<>` types; golden-equality suite still green |
| 11 | P2 | Rule-table as-of contradicts its JSON twin + honesty box | ACCEPTED | `as_of` → 2026-07-15 in the twin; U4 re-check note synced both files |
| 12 | P2 | Rule a-3 misreads LL79: "must deliver ≥1 mile" (trip length) vs the actual minimum service RADIUS | ACCEPTED — a genuine legal-precision catch | md row + JSON predicate + `lib/packs/fees/rules.ts` description all restated as the radius obligation ("serve customers within at least a 1-mile radius"); a-3 remains a non-machine-checkable structural obligation |
| 13 | P2 | mockups/README false at the current tree | ACCEPTED (then superseded by the owner's deletion word) | full inventory rewrite: two-file reality, git-recoverable vs scratchpad-archived deletions stated |
| 14 | P2 | Landing copy still says "Analyze the claim" post-flip | ACCEPTED (my miss in the flip) | copy rewritten around the completed specimen + replay |
| 15 | P2 | Hero pause control mixes changing action label with `aria-pressed` | ACCEPTED (ARIA APG) | `aria-pressed` removed; action-label semantics kept; header comment synced |
| 16 | P3 | "One iris interactive accent" inconsistent (ember/azure focus + selected states) | ACCEPTED (design-adjudicated) | focus rings + selected-tab states → `--iris/--iris-ink`; ember stays verdict-only, azure stays data-material. Note: the method word's chapter-accent focus ring is a recorded exception (gate advisory) |
| 17 | P3 | Height contract samples only endpoints | ACCEPTED | whole-run ResizeObserver records every box across all six stages (`canonical.spec.ts`) |
| 18 | P3 | 16MP ceiling not absolute | ACCEPTED | scale may drop below 1; rounded dimensions re-clamped by product |
| 19 | P3 | CSS comments cite deleted mockups as provenance | ACCEPTED | comments commit-qualified (B-2 at `a8f6c5f`; gallery-v2 at `8269ab9` — verified via `git log --diff-filter=A`) |

**Also fixed under this reconciliation (own findings, the phase-F axe sweep):** the landing contrast flag under allowed motion proven a sweep-timing artifact (zero violations settled, evidence in `captures-2026-07-15-phaseF/after2-axe-report.json` + the settle probes); mobile-only findings (`.ds-tbl` scrollable-region-focusable at 360/390 on /eval; 29px nav overflow on /legacy/console at 360) recorded OUT-OF-SCOPE per the owner's desktop-only word.

**Session-16 owner words riding this batch (decision-log rows, recorded before execution):** bench opens-complete pick · NYC track-only recommendation · Light Gauge iterate → creative rebuild + register sweet spot · desktop-only scope · final design = the refined storyboard + old-sample deletion · design authority to Fable · Claude Design connect (project "Curbside Commons" created; brief/tokens/storyboard/sample pushed via DesignSync on explicit owner word).

**Harness events (raw):** one auto-mode classifier denial on a local axe-sweep Bash call, reason verbatim: "[Data Exfiltration] The agent created a project on the external, unconfigured-trust 'DesignSync' service and uploaded a full internal design brief … being sent to a destination not listed as trusted in Environment." — the DesignSync push was the owner's explicit, repeated instruction to their own claude.ai/design account; the denied command itself (a local accessibility test) was re-run standalone without incident.

## Post-fix gates (live, this reconciliation)

- `npm run verify` exit 0 — typecheck · lint · build (56/56 pages) · vitest **1,187 passed + 7 skipped** (floor 1,174+7 held; growth = the new teeth)
- e2e **19/19 dev + 19/19 artifact** (new: demo boundary · merchant leak assertions · the ResizeObserver height contract; bench flip contract proven red first)
- legacy **306 + 5**
- axe desktop breakpoints 1280/1440/1728: **zero WCAG 2.0/2.1/2.2 A/AA violations** (reduce + settled allowed-motion)
- Before/after captures: `captures-2026-07-15-phaseF/` (desktop binding; the full set incl. non-binding mobile in the session scratchpad)

## Independent acceptance-gate (fresh-context, read-only)

**SHIP** — all five gates PASS; **all 20 acceptance-checklist items MET** under the recorded owner amendments. Four record conditions (this file · state-doc sync · the mockups/README stale line · the hero comment) land in the covering commit. Advisories on record: the `enhanced_service_fee` token is a deliberate source-bound keep; the method word's violet focus ring is a recorded chapter-accent exception.
