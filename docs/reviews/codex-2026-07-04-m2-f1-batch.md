# M2 batched Codex review — F1 fee-audit module (F1a `896ab59` + F1b `bda6314`)

**Gate:** the M2 module-boundary cross-model review (plan §5 gate policy S-4; the tenth-session wrap + frontier-advisor ruling made the owner-gated live classifier run conditional on this ceremony). **Seat:** smoke-tested `SEAT_OK`; run read-only via `~/claude-os/bin/codex-guarded` (config default `gpt-5.5` @ `xhigh`). **Scope:** the complete `git diff c864618..bda6314` (the whole F1 module), with four advisor-ruled mandatory items enumerated: (a) the `ClaimSource` `"classifier"` shared-core touch, (b) the reviewer-executed F1b red-greens, (c) statutory logic vs `docs/research/uc1-rule-table.md`, (d) the leak-free classifier seam + pre-registered floors. **Raw verdict:** `codex-2026-07-04-m2-f1-batch-raw.md`. **Date:** 2026-07-04 (eleventh session).

**Verdict: BLOCK — 2 P1 + 2 P2 + 1 P3.** Scope items: (a) **CONFIRMED** (additive-only; no exhaustive source switch; listings render/pass-through only), (b) **CONFIRMED** ("the reviewer-executed red-greens have real teeth by code structure… the first RG-A mutation was correctly recorded as too weak"), (c) **REFUTED on one point** (monthly-average ∨, e-1 states, c-exclusion, U1 markers, 17-rule set coverage all consistent — but c-2 pass-through did not match the table), (d) **REFUTED on one point** (leak-free input, split locks, baseline pin, documented misses, no live path all confirmed — but the accuracy floor admitted a baseline tie).

**Boundary consults (routing doctrine 2026-07-03):** harness `advisor()` DOWN — 11th consecutive session (raw: "The advisor tool is unavailable"). `frontier-advisor` consulted at the pre-verdict boundary (first launch died on the subagent seat limit — raw: "You've hit your session limit · resets 11:30pm (America/New_York)"; owner-confirmed retry succeeded): **PROCEED, no overturns**, three rulings — (i) finding #1 takes the advisory-warn shape, NOT a schema `actualProcessorChargeCents` field ("a stronger-*sounding* check that's actually fake verifiability"); (ii) finding #2 amends the floor upward pre-run (leaving "≥ 0.90" is a latent trap for a future session); (iii) finding #4 takes full lineId + conscious golden regeneration, with the regen diff checked to be claim-id-only deltas.

## Reconciliation — primary-model-final (Fable seat). ALL FIVE ACCEPTED + FIXED RED-GREEN.

| # | Sev | Finding | Disposition | Fix | Red-green |
| --- | --- | --- | --- | --- | --- |
| 1 | P1 | c-2 pass-through was a boolean escape hatch: `passthroughDocumented===true` silently cleared an over-3% transaction fee, though §20-563.3(c)(i)–(ii) requires the fee EQUAL the actual processor charge with proof — self-labeled pass-throughs could hide base inflation | **ACCEPTED** (advisor ruling i: advisory-warn shape) | New `FeeVerdict` state `asserted-passthrough-unverified` (never counts toward `ok:false`); the c-1 branch now emits a **warn** finding (ruleId `NYC-563.3-c-2`, `claim.field: passthroughDocumented`, U1-provisional, both registers) instead of `continue`; `verdictPhrase`/`plainVerdict`/CLI `VERDICT_TAG` + tally line extended (tsc exhaustiveness caught the CLI record); audit header comment rewritten | RG-1: mutation back to silent `continue` → `fees-audit-e1.test.ts` **1 failed** ("SURFACED as a non-gating warn"); restore → 11/11. Plus a green-boundary test: at/under 3% with the flag → NO finding |
| 2 | P1 | The pre-registered live-classifier accuracy floor "≥ 0.90" admitted a TIE with the pinned 19/21 = 0.905 baseline — contradicting the plan's own "earns only by beating the baseline" (AM-7) | **ACCEPTED** (advisor ruling ii) | `docs/plan-f1b-classifier.md` floors table: accuracy floor is now **≥ 20/21 (≈ 0.952), strictly more than the baseline's 19/21; tie = DEFER** — dated amendment note in the row (2026-07-04, this finding), amended BEFORE any live run, strengthened-only | Doc-only (no test encodes the floor — verified by grep); the change is itself the record |
| 3 | P2 | Parser accepted lines whose `month` differs from `meta.month`; the audit sums all non-refund lines against `meta.month`'s caps + refund window → mixed-month statements corrupt monthly averages and e-1 cure logic | **ACCEPTED** | `parseStatement` now throws `StatementParseError` on any `line.month !== meta.month` (loud, never coerces) | RG-2: disable the check → `fees-parser.test.ts` **1 failed** (the new mixed-month rejection case); restore → 21/21 |
| 4 | P2 | Per-line claim ids (`ORD-1#transaction_fee`, `ORD-x#<category>`, advisory `…#classifier`) were non-unique for repeated same-order/same-category lines — weakens C2 traceability | **ACCEPTED** (advisor ruling iii) | Statement-position tag `L<index>` appended to every per-line claim id in `audit.ts` (d-1 + c-1/c-2 branches) and `classified-audit.ts` (advisory); monthly aggregate ids unchanged; `generate.ts` answer-key `expectedClaimId`s suffixed to match (L1/L5/L7); **goldens consciously regenerated via the seeded generator** | RG-3: drop the tags → **4 failed** across `fees-audit-e1` (2 uniqueness tests) + `fees-classifier` (advisory dup test + golden byte re-assertion); restore → 26/26. Freeze teeth independently demonstrated pre-regen: the F1a-golden byte assertion fired on the id change (1 failed) before the sanctioned regeneration |
| 5 | P3 | The drift-lock asserted `cap_pct`/`base`/`drift_classes` vs the JSON twin but not `kind`/`source_clause`, despite claiming field-for-field mirroring | **ACCEPTED** | `fees-drift-lock.test.ts` per-rule assertion extended to `sourceClause` + `kind` (twin values verified to match the TS table exactly, annotations included) | RG-4: mutate a-1's `kind` in `rules.ts` → drift-lock **1 failed**; restore → 19/19 |

**Golden-regeneration diff scope (advisor directive iii honored):** the ONLY byte deltas across the 4 expected reports + answer key are (1) three per-line claim ids gaining `#L1`/`#L5`/`#L7`, (2) the new `asserted-passthrough-unverified: 0` tally key in all four reports, (3) the three matching answer-key `expectedClaimId`s. Statement JSONs byte-unchanged; no verdict, amount, rule id, or ordering changed. No fixture exercises `passthroughDocumented: true`, so finding #1 adds zero golden bytes.

**Gate after reconciliation:** `npm run verify` **exit 0 — 720 passed + 5 skipped** (715→720: +5 tests); `npm run test:legacy` **306 passed + 5 skipped** unchanged; differential-oracle semantics untouched (no `lib/core`/gold/legacy path in the diff). Red-green evidence: `docs/reviews/m2-reconcile-evidence.log`.

**Process-adherence note (Codex, verbatim):** "slice records/logs exist and the F1b inline-tail deviation is recorded; I did not run tests because this session is read-only, so this verdict is based on static diff/code/doc verification." The live test execution Codex could not do is supplied by this reconciliation's executed red-greens + the verify runs above.

## Mapped confirming pass (same seat, read-only) — ALL FIVE DISCHARGED

Raw: `codex-2026-07-04-m2-f1-confirm-raw.md`. Verdict: **F1–F5 all "DISCHARGED"**; one **new residual P3** → BLOCK on it alone: the E-1 design-escalation comment in `classified-audit.ts` (and the `finding.ts` header) still described `FeeVerdict` as three-membered with "adding a fourth would force tally/golden changes / finding.ts is untouched" — made materially false by finding #1's sanctioned fourth state. **ACCEPTED + fixed primary-model-final** (comment-only): the E-1 comment now records the original F1b-time rationale AND the M2 addition explicitly (the resolution stands — the advisory lane remains outside `FeeVerdict`); the `finding.ts` header now names both verdict families. No behavior change; `npm run verify` re-run **exit 0 — 720 + 5** after the comment fixes. The cross-model gate over the reconciliation is DISCHARGED.

**Test-count honesty note (acceptance-gate item, resolved 2026-07-04):** the F1a evidence log's RG-3 GREEN line reads "Tests 10 passed (10)" for `fees-audit-e1.test.ts`, but the file as COMMITTED carries 8 tests at `896ab59` and is byte-identical through `bda6314` (`git diff 896ab59 bda6314 -- <file>` EMPTY). The two extra tests existed only in the F1a builder's pre-commit working tree during that RG run and never reached any commit — **no committed test was ever deleted**. The live-verified runtime chain is 715 (pre-M2) → 720 (post-M2), exactly +5: e1 +3 (one in-place rewrite + three net-new), parser +1, classifier +1, drift-lock +0 (extended in place).

**Next leg:** independent acceptance-gate over the F1 module (F1a + F1b + this reconciliation diff).
