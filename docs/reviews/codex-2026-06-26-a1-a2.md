# Codex Changed-Files Review — A1 (tool-ify) + A2 (single-agent loop)

- **Dates:** 2026-06-25 → 2026-06-26
- **Reviewer:** Codex `gpt-5.5` @ `xhigh`, read-only, via `~/claude-os/bin/codex-guarded` (`exec review --commit <sha>`)
- **Subject:** the A1 tool-ify slice (`f521b5c`) + the A2 single-agent verify-and-self-correct loop (`5a1f86a`) + the reconciliation (`37116f5`).
- **Raw verdicts (ephemeral):** `/tmp/codex-a1-review-activationops.md`, `/tmp/codex-a2-review-activationops.md`, `/tmp/codex-reconcile-confirm-activationops.md`. **This file is the durable record** (closes the documented "/tmp-only gate evidence" repeat-failure, which recurred here and is fixed by recording it).
- **Method note:** the A1 review was first attempted with a custom prompt + `--commit` (rejected by the CLI: those flags conflict) — that produced no model call; re-run cleanly as `exec review --commit`. The seat hit its usage limit on the first clean A1 attempt (2026-06-25 ~7:49 PM) → A1/A2 proceeded **test-verified** as a dated obligation; the reviews completed on the seat reset (2026-06-26).

## Findings → reconciliation (primary-model-final)

| # | Sev | Finding | Disposition | Where fixed |
|---|---|---|---|---|
| A1-1 | **P2** | `simulate_send` clears `idempotency_key` to `""` when re-called on an already-`simulated_sent` merchant → a retry/resume can wipe the duplicate-send guard. | **ACCEPT → FIX.** Preserve (recompute if absent) the key for the already-sent case. Locked by a re-call test. | `lib/agents/tools/send.ts`; `evals/tools-differential.test.ts`; `37116f5` |
| A2-1 | **P1** | **Fail-open:** a `FAILED_TO_FALLBACK` judge verdict (live reverse-faithfulness call failed/rate-limited → keyword mock) still counted as verification — an unsupported draft the mock misses could reach `simulate_send` without the live check ever running. | **ACCEPT → FIX (safety).** `verifyPassed` now excludes `FAILED_TO_FALLBACK` → the draft is re-drafted or **held**, never sent on the mock alone. Locked by a fail-closed test (clean draft + throwing judge → held, not sent). | `lib/agents/loop/orchestrator.ts`; `evals/agent-loop.test.ts`; `37116f5` |
| A2-2 | P2 | **Evidence rigor:** the live R-LOOP-10 `selfCorrected` could count an item "fixed" by a deterministic fallback stub, not a genuine live redraft. | **ACCEPT → FIX.** Self-correction requires a genuine `LIVE_AI` redraft + `LIVE_JUDGE` final. | `evals/agent-loop.live.test.ts`; `37116f5` |
| A2-3 | P2 | **Confirming pass:** the A2-2 fix was incomplete — a `verify=FAIL` on iteration-0 can mean the judge *fell back*, not a genuine live catch; `.some` could also tie a live redraft to a different iteration than the final. | **ACCEPT → FIX.** Tied the seed-catch + **every** verify to genuine `LIVE_JUDGE`; per-step modes recorded in the snapshot for auditability. Re-validated by a fresh live run. | `evals/agent-loop.live.test.ts`; this commit |

## Confirming pass (`--commit 37116f5`, 2026-06-26)
Codex **confirmed** the runtime fail-closed (A2-1) + idempotency (A1-1) fixes are "covered by tests," and raised only A2-3 (the metric refinement above). No runtime/safety issue remained.

## Independent acceptance-gate (read-only judge, 2026-06-26)
Verdict: **procedural BLOCK** — the **code and all three headline claims were independently affirmed** (recommend-not-decide "structurally unbreakable," fail-closed "complete; no escape path," 8/9 "framed honestly"), but two gates were **undischarged on the commit**: gate 1 (no recorded A2 grill) and gate 2 (Codex verdicts only in `/tmp`, not `docs/reviews/`). **This file discharges gate 2**; the A2 grill (`docs/reviews/grill-2026-06-26-a2.md`) discharges gate 1; the red-green proof + `verify:full` discharge gate 3.

## Net
**A1 + A2 cross-model gate: BLOCK → all findings reconciled primary-model-final → fixed + test-locked → CONFIRMED** (runtime), with the metric refinement (A2-3) re-validated by a fresh live run. The deterministic core, oracle CSVs, and gold-set labels are UNTOUCHED throughout.
