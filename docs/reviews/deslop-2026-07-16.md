# De-slop gate — rendered surfaces, language + design (2026-07-16, session 22)

**Lane:** the owner's session-21 directive (decision row, verbatim there): anti-AI-slop over language AND design, applied as per-context JUDGMENT with the right toolkit (de-slop / humanizer / vendored better-* references), never blanket. Run AFTER the sol content pass so it grades the finished copy.

**Mode:** de-slop prose mode + design checks + a code-slop leg, graded against `~/.claude/skills/_shared/no-ai-slop.md` (§1 design, §4/§8 + humanizer prose tells, §2 code) . Company-context profiles: none generated — graded against the universal checks + the repo's own recorded register (v8 = owner-final). Findings below rest on exact greps/quotes; the greps are reproducible.

## Scorecard

| Check | Status | What is off | Source | Suggestion |
|---|---|---|---|---|
| AI vocabulary in rendered microcopy (seamless/elevate/delve/robust/leverage/…) | aligned | Zero hits in rendered strings; the only "seamless" hits are code comments in a file removed below | no-ai-slop §1 Copy, §8 | — |
| "Not just X but Y" / negative parallelism | aligned | One hit is a legitimately quoted question heading ("Why not just run it again?" — /eval's one-pass rule) | no-ai-slop §8; humanizer | — |
| Sycophancy / chat artifacts / knowledge-cutoff tells | aligned | Zero hits | no-ai-slop §8 | — |
| Decorative emoji in headings/copy | aligned | Zero hits | no-ai-slop §8 | — |
| Title Case headings | aligned | All H1/H2s sentence-case ("Three moves. Each can be checked.") | no-ai-slop §8 | — |
| Generic CTAs | aligned | CTAs are task-specific ("Read the listings report", "Open the fee-cap audit", "Check a feed in your browser") | no-ai-slop §1 Copy | — |
| Vague attributions / uncited figures | aligned | Every rendered figure derives from committed sources (specimen.ts, ENGINE provenance); no "experts believe" class | no-ai-slop §4 | — |
| Em-dash density | aligned (judgment note) | ~10 em-dashes across the landing's rendered copy — the v8 editorial voice uses them deliberately; sol's content pass did not flag density. Per-context judgment: keep. | no-ai-slop §8; owner's never-blanket word | — |
| Rule of three | aligned (earned) | The method chapter's 3-card grid IS the product's actual triad (claim · record · proof) — semantic, not filler | no-ai-slop §4 | — |
| Typographic quotes/entities | aligned | `&rsquo;`/`&ldquo;` entities are the site's consistent editorial choice, not chat-paste artifacts | no-ai-slop §8 (context read) | — |
| Design: typography | aligned | Onest, self-hosted — not the Inter/Space Grotesk default class | no-ai-slop §1 Typography | — |
| Design: color | aligned | Committed iris/ember brand strategy, every claimed ratio recomputed (session 18); not a category-reflex palette | no-ai-slop §1 Color | — |
| Design: gradient hero text + glass header | skipped (register call) | `background-clip` hero line + white→glass sticky header ARE listed AI-default tells — but both are load-bearing elements of the owner-picked, owner-FINAL v8 language (2026-07-16 v8-final row). The gate does not overrule an owner-fixed design; recorded, not actioned. | no-ai-slop §1 Components; v8-final decision row | — |
| Design: radius/shadow defaults | aligned | The ≤6px/no-shadow button contract is test-enforced (batch-F normalize) | no-ai-slop §1 Layout | — |
| Design: identical card grids | aligned | The two grids on the landing are semantically distinct (method triad; limits notes with indexed structure) | no-ai-slop §1 Components | — |
| Code: dead component | **was drift → FIXED** | `components/landing/CorrespondenceHero.tsx` — 1048 lines, ZERO imports since the v8 CommonsScene replaced it (session 18) — plus its exclusive CSS (`.cc-hero*` copy/canvas/pause/fallback rules + the cc-pause 560px media block, ~240 lines). Classic keep-for-safety residue. | no-ai-slop §2 (backwards-compat shims: delete; rely on git) | Deleted the component + the dead CSS; `.cc-hero-cta` KEPT (live: the landing close section pairs it with `.cc-close-cta`). |

**Verdict: Good to go** (after the one fix, which is applied and verified).

## Fix verification (the gate detected; Fable owned the edit)
- Import proof: zero references to `CorrespondenceHero` across app/, components/, lib/, evals/ before deletion; per-class CSS liveness table run (only `cc-hero-cta` used outside the dead file).
- Gates after: typecheck clean · build green · **vitest 1239 pass + 7 skip** · canonical + layout-sanity e2e 17/17.
- The vitest floor moves 1241→1239 BY DESIGN: `honesty-c10` reads `components/landing/*.tsx` dynamically (one scan case per file), so the dead file's 2 scan cases left with it — the same dynamic-pickup mechanism that grew the count when files were added. No contract was skipped.
