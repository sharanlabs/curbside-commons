# Acceptance gate — A3-5 (Router/Conductor agent — multi-critic revision synthesis + advisory route, recommend-only)

**Date:** 2026-06-28 · **Judge:** `acceptance-gate` subagent (independent, read-only, fresh context — maker≠judge holds) · **Target:** the uncommitted A3-5 working-tree diff (2 modified + 2 new) · **Decision authority:** primary-model-final (Opus 4.8).

## Verdict: BLOCK — gates 1/3/4/5 PASS; gate-2 (Codex) NAMED-OPEN until the batched round-3 re-confirm returns

The acceptance-gate confirmed the **substance is sound** and independently traced the two load-bearing claims it was told to be adversarial on — both hold **against source**, not just the prose:

- **Recommend-only firewall is real + belt-and-suspenders:** the LLM route is clamped to the deterministic envelope (`clampRouteToEnvelope` can only ADD caution), the reflect seam gets a defensive `{ ...merchant }` clone, the send flows ONLY through `simulateSend`, and `assertEligibilityUntouched` hard-throws on any eligibility move. `plan.route`/`holdForHuman` reach the trajectory record ONLY, never the send.
- **The defer is honest, not a dodge:** a genuine RED-GREEN seam-proof exists (domain-blind `buildReflection` vs `strongReflection` as a strict SUPERSET — non-vacuous: the faithfulness text is byte-identical, the domain addendum is appended). Only the LABEL defers, and it defers **by construction** (`routerReflect` recomputes `signals` structurally → it can only tie the deterministic table). Earning genuinely requires a cross-family Gemini judge ⇒ live ⇒ owner-gated A3-7. `router` is ABSENT from the trajectory and the tests assert it.

**Gate panel:**
1. **grill — PASS** (self-satisfied). The judge probed the hardest joint — "why didn't the crux agent earn?" — and found it answered verbatim in `router.ts` + `evals/router.test.ts`: every offline discriminator is a finite axis `strongReflection` reproduces; earning needs an open-ended-quality cross-family judge ⇒ A3-7. Structurally forced, not hand-waving. The recommend-only claim is not waved past; the defer is not used to dodge a real seam-proof (one exists).
2. **codex cross-model — NAMED-OPEN (the blocker).** The mandatory changed-files review is **SEAT-BLOCKED** (`"You've hit your usage limit … try again at 7:25 PM"`, surfaced verbatim, no retry per owner doctrine), recorded in `docs/reviews/codex-2026-06-28-a3-5-router.md`, **batched with the still-open A3-4 round-3 re-confirm**. This is the documented family-bias mitigation — not waivable, not substitutable by the same-family green suite. **B2/A3-4 named-open pattern:** flips SHIP when the dated Codex re-confirm returns clean, reconciled primary-model-final.
3. **verify — PASS** (the judge held no Bash; it judged the LOGIC sound from source and required raw evidence — now captured durably in `docs/reviews/a3-5-verify-evidence.log`): `npm run verify` exit 0, **295 passed | 5 skipped**, typecheck/lint/build green; `git diff --name-only` shows the differential lane (`lib/core` + `out/` oracle + `evals/gold` + frozen `*.snapshot.json`) **UNTOUCHED**; the **clone red-green** is captured raw (RED: dropping `{ ...merchant }` trips `R-LOOP-1b violation: … mutated … "review_required"`; GREEN: restored → 2 passed). The anti-theater RED/GREEN is the eval itself (buildReflection vs strongReflection), non-vacuous by construction.
4. **enterprise+taste — PASS** (full pass — non-trivial, send-path-adjacent). The send firewall is structural (clamp + clone + assertEligibilityUntouched), not convention. Fail-closed honesty taxonomy carries `errorClass`. Prompt withholds the raw merchant_name (no injection surface); unsupported-claim text treated as DATA. `signals` recomputed from inputs; `criticSignals` requires BOTH `domain_defective` AND a failed dimension (defensively does not trust a flag alone). No staff-eng objection.
5. **anti-slop — PASS** (prose/structure). Dense rationale-bearing comments (R-A3-x refs, red-green conditions), no decorative slop. (Code-slop `deslop_code.py` is an advisory handoff the read-only judge can't run; nothing flagged on read.)

## Flip condition (the gate's own pre-committed terms)

**Flips to SHIP 5/5 when the batched Codex re-confirm returns clean** on BOTH the A3-5 diff AND the A3-4 round-2 P1 patch (seat resets ~7:25 PM ET), reconciled primary-model-final. The re-confirm must see: the recommend-only firewall (clamp + clone + `assertEligibilityUntouched`), the honest structurally-forced DEFER (no manufactured win or loss), and the strict-superset seam.

## Disposition (primary-model-final)

**Proceeding TEST-VERIFIED with gate-2 named-open** (the B2/A3-4 precedent, NOT the A1/A2 "proceed test-verified" which applied to commits, never the gate stamp). The gate found **no substantive P0/P1 blocker** independent of the two execution-gated handoffs (Codex + the now-captured raw verify). The **Codex re-confirm is a DATED OBLIGATION** before any irreversible step (push / A3-7 live). What this does NOT authorize: **push** (HELD — no remote), **deploy**, **public posting**, the **A3-7 live Gemini run** — all remain owner-gated.

Records: `docs/reviews/codex-2026-06-28-a3-5-router.md` (the seat-blocked review + targets) · `docs/reviews/a3-5-verify-evidence.log` (raw gate-3 evidence).
