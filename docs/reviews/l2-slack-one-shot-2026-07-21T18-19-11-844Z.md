# L-2 One-Shot Slack Delivery Demo — Run Record (2026-07-21T18:19:11.844Z)

**Arming (this run):** owner word 2026-07-21 — "I need a live integration as well" + "i have already created slack webhook added the key" (the owner created a fresh incoming webhook to their own channel and placed it in the gitignored `.env` as `SLACK_WEBHOOK_URL`, then armed this send). Controls: `docs/plan-a3-delivery-safety.md` — all eight held.
*(The send script hard-codes the ORIGINAL 2026-07-08 L-2 arming line — "complete all step by step." + structured-ask "Slack webhook", authorization commit `eb34bb0`; this line is corrected here for this run's accurate provenance. Follow-up: make the script's arming attribution parameter-driven so future records self-date.)*

| Field | Value |
| --- | --- |
| Sent at | 2026-07-21T18:19:11.844Z → 2026-07-21T18:19:12.270Z |
| Target | the owner's own Slack workspace channel via incoming webhook — host `hooks.slack.com`, URL **REDACTED** (control #5) |
| Payload | `audit_statement` over `fixtures/synthetic-restaurant/fees/statement.drifted.json` → Block Kit via `lib/delivery/slack.ts` (the goldens' exact path) |
| Payload sha256 | `468349c8e75ac404e9e603dac9f5a18732a54ada5b4ed2cec31c75edc2eb2f38` (serialized form — reproducible offline: same builder, same fixture) |
| Banner | SIMULATED banner leads the payload (builder-enforced, throws without it) |
| HTTP result | 200 OK — body (≤200ch): ok |
| Outcome | DELIVERED (one-shot; session ended) |
| Retries | 0 (no retry path exists — control #3) |

One message, one send, session over (control #1/#3). No secret appears in this record or any log.
