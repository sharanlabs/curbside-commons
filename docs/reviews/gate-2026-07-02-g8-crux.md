# G8 — Pre-Build Crux Gate (run inline 2026-07-02 per the owner's NO-WAIT doctrine)

**The two-part crux (Codex amendment 9):** (i) does copy/SOR drift persist behind food's live-query shift? (ii) who has authority/pain/budget to require independent truth evidence?

## Evidence (primary, accessed 2026-07-02)

**(i) TECHNICAL — FAVORABLE. The copy layer is IN-PROTOCOL.** UCP catalog spec (ucp.dev/latest/specification/catalog/): the "Business" role is abstract — **no prohibition on marketplaces/aggregators serving catalog data for merchants**; responses need only "reflect the Business's current terms… not transactional commitments — checkout is authoritative"; **no requirement that responses originate from the merchant's authoritative system-of-record; no sync/accuracy SLA; no upstream-source obligations** (only a session-reuse SHOULD NOT). Verbatim-anchored. Consequence: **live-query ≠ truth-guaranteed** — wherever the serving Business holds a copy (every marketplace context), drift persists BEHIND the live interface. The devils-advocate's Scenario-2 fear (live reads design drift out) is answered by the spec itself: the protocol validates transactions, not truth. The surface-agnostic engine (condition 4) is confirmed as the right shape, not just insurance.

**(ii-a) INCUMBENT DEPTH — FAVORABLE with a named boundary.** Feedonomics ACE (launched 2026-04-27) is **syndication/optimization** — "delivering optimized catalogs to participating agentic surfaces," minute-level sync, delta publishing [feedonomics.com/product/agentic-commerce + commerce.com press]. Their own marketing prescribes "a single source of truth (PIM/catalog master)" — confirming the PROBLEM — but the product is the SYNCER: maker, not judge. No independent feed-vs-SOR audit found at Feedonomics or Deliverect (in-pipe own-sync verification only, per the council pass). **The independent-verifier seat remains unoccupied after this pass.** (Standing caveat: absence-of-evidence after N passes; the seat has shrunk before — the 14-day/30-day tripwires still stand.)

**(ii-b) BUYER/ENFORCEMENT — CLAIM DECLINED, honestly.** No surface-side truth enforcement is named-and-verified today (OpenAI feed-vs-website rejection remains single-sourced). Per the gate's own rule ("name the enforcing surface or decline the claim"): **we DECLINE the buyer claim.** This is a personal/showcase build — the demo + corpus need no buyer; UC-1's validated institutional pain (enforcement-demonstrated, not sold) remains the program's primary evidence track.

## OUTCOME: **PASS — build proceeds exactly as the reshaped plan stands.**
UC-1 = primary evidence/AI-depth track (unchanged) · UC-2 = bounded frontier demo, GREENLIT (the copy layer is in-protocol; the seat is open; the buyer claim is declined not assumed) · surface-agnostic engine (C3) = mandatory, spec-grounded · demo claim stays the Codex-amended sentence. Recorded per plan §5 G8; the 14-day build-slip and 30-day external-signal tripwires are now the live guards.
