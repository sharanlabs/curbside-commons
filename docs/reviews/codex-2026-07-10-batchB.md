# Batch-B Codex review — 2026-07-10/11 (plan v3.3: S4 + S4b, the dedicated freeze-boundary review)

**Run:** ONE `gpt-5.6-sol` @ `high` via `~/claude-os/bin/codex-guarded` (probe-first: SEAT_OK at low effort per the 2026-07-10 quota amendment). Read-only; reviewed range `d83df29..96d1cf7` (4 local commits, unpushed — the reviewer corrected the packet's "5 commits" miscount at its startup summary). Packet cited plan v3.3 (§S4/§S4b/§Gates), the S4 inventory, the unified-review findings (F-16/F-13/F-03), and batch A. Session `019f4ebf-ca00-7b22-ac6d-9f23c55e78b6`; startup checkpoint passed via `exec resume` (one CLI-arg retry — `resume` rejects `--sandbox` — raw on record, $0). Raw stream: `docs/reviews/codex-2026-07-10-batchB-raw.log`. Evidence record: `docs/reviews/batchB-verify-evidence.md`.

**VERDICT: REVISE — 0 P1 + 7 P2 + 1 P3. Reconciliation (primary-model-final, Fable): ALL 8 ACCEPTED, 0 refuted — each verified against the repo, fixed, and re-gated the same session.**

The reviewer independently confirmed the migration core: typecheck, lint, 1005+7, legacy 306+5, 16/16 delivery tests, exact golden substitutions (name-swap-only), and unchanged protected blobs.

| # | Sev | Finding | Disposition + fix |
|---|---|---|---|
| 1 | P2 | The inventory's `\bSquare\b`-style boundaries match nothing in this git-grep build — the "exhaustive" scan missed 16 files (163 vs a correct ~178) | **ACCEPTED — confirmed live** (`git grep -E '\bSquare\b' -- README.md` = no match). Method corrected in the inventory (unboundaried pattern + `git grep -lw` for Square/Stripe/Google/Toast); all 16 omitted files classified in the new §3b — **all keep-class, zero new edit sites** (Square-Catalog schema attribution with its own non-affiliation header, Google-as-Gemini-vendor facts, dated docs, legacy/frozen); counts corrected 163→178. |
| 2 | P2 | `docs/story-arc-content-spec.md` was classed historical while its own header claimed "LOCKED … drives the product surfaces," and the cited digest is not in the repo | **ACCEPTED.** The status line was stale (the 2026-07-08 Oxblood whole-site specs govern the current site; nothing current consumes this spec) — explicit SUPERSEDED marker added naming the replacement, original status line preserved in-place; inventory reclassified with the corrected grounding (the Gate-P2 "linked digest" claim withdrawn — the digest lived in the authoring session only). |
| 3 | P2 | The migration falsified `docs/demo-recording.md`'s claim of byte-identity with the current `expected-demo.txt` | **ACCEPTED.** The recording is preserved verbatim as the historical execution; its provenance note now pins the golden **as recorded** (git blob `49410ece1b8b…`) and states that template v2 renamed the title line in the current golden. |
| 4 | P2 | `workflows/n8n/truth-audit-fees-to-slack.workflow.json` still carried the old display name while invoking the migrated builder (dangling consumer) | **ACCEPTED.** Display name migrated → "Curbside Commons — …"; the structural suite re-run green (`evals/n8n/` 6/6 — it binds structure, not the display name; `workflows/` is not a protected path). |
| 5 | P2 | The pre-edit decision-log row said "EXECUTED" though execution landed in the child commit | **ACCEPTED.** Row reworded to AUTHORIZED (pre-execution row, correction noted in place); a separate post-execution evidence row appended. |
| 6 | P2 | Gates and the red-green chronology were commit-message assertions, not committed evidence | **ACCEPTED.** `docs/reviews/batchB-verify-evidence.md` committed: SHA boundaries, exact commands, exit codes, the 4-fail→16/16 red-green chronology, scan results, golden byte proof. (The reviewer's own sandbox build failed on Google-Fonts fetches — network-restricted; normal-environment builds pass; the fonts dependency is the standing AP-14/S6b item.) |
| 7 | P2 | State docs (PROJECT_STATE/CURRENT_TASK/HANDOFF/task-log/journal) were stale at the freeze boundary | **ACCEPTED.** All synced in the reconciliation commit before push (the sync was scheduled for wrap; the reviewer is right that the freeze boundary is the deadline). |
| 8 | P3 | No Professional Process record inside the reviewed range | **ACCEPTED.** The session's PPA block preserved as §0 of the evidence record. |

**Post-fix gates (all re-run live):** verify exit 0 = **1005+7** · test:legacy **306+5** · `evals/n8n/` 6/6 · fresh build exit 0 + corrected `out/` scan fully allowlist-mapped, zero old-name residue · protected paths = the recorded allowlist exactly · C10 green (inside verify).

**Push policy:** batch B reconciled → push proceeds (S4+S4b+reconciliation). S5/E1a may now consume the new strings per the plan's freeze-boundary rule.
