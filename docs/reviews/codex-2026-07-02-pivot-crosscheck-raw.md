## VERDICT: CONFIRM-WITH-AMENDMENTS

## Findings

1. **P0 — Council Synthesis / missing 8th condition: crux-resolution must be a hard pre-build gate, not a “next test.”**  
   Attack: Synthesis says the crux is unresolved: “who actually serves the catalog in marketplace contexts is the unresolved empirical fact,” then separately lists “The next test… half-day primary-doc teardown.” That teardown is not one of the 7 conditions. This lets planning proceed while the core UC-2 claim remains conditional.  
   Resolve: Add condition 8: **before implementing UC-2, resolve UCP/ACP serving architecture and incumbent verification depth; if direct-SOR/live-read dominates or incumbents already check vs SOR, UC-2 demo is demoted and UC-1 becomes lead.**

2. **P0 — Condition 1 / S-5 close-out FIRST is directionally right but operationally unsafe as written.**  
   Attack: Suggestions ledger S-5 says “commit it as-is with its honest degraded-run labels; declare… CLOSED… drop the clean-K live re-run.” That may convert provenance, but “commit as-is” risks enshrining a degraded artifact as lineage before this pivot is ratified. It also conflicts with the repo’s Definition of Done spirit if docs/tests/handoff are not coherent after the close-out.  
   Resolve: S-5 should be: **freeze or archive the suspended diff only after an explicit owner acceptance record, final `npm run verify`, and a written provenance caveat. Do not call it capability proof beyond what the degraded labels support.**

3. **P0 — Condition 2 / “parallelize UC-1 from week 1” is too weak relative to the evidence.**  
   Attack: Synthesis itself says UC-1 has “four of four load-bearing dimensions” and DA says UC-2 ordering is “evidence-unsupported.” Parallelizing still preserves UC-2 as lead by default. That is not the Bayesian update implied by the council’s own findings.  
   Resolve: Rewrite condition 2 as: **UC-1 is the primary evidence/AI-depth track; UC-2 is a bounded frontier demo unless condition 8 confirms the live-copy drift seat.**

4. **P1 — Condition 3 / July 16 DCWP call is necessary, but the public-comment implication may breach honesty and scope constraints.**  
   Attack: “Owner call THIS WEEK on the July 16 DCWP comment window” is valid if the date/fact is accepted from the package. But the package also says full §20-563.3 text is still unfetched and effectiveness dates conflict: digest says 2025-05-31; addendum flags 2025-06-30. A public regulatory comment before primary text reconciliation risks overclaiming.  
   Resolve: Owner call should be gated by a **one-page source memo**: exact rule text, exact deadline, exact effective dates, what is verified, and what is not.

5. **P0 — Condition 4 / surface-agnostic engine is correct but under-specified.**  
   Attack: “SOR vs static ACP feed AND vs live UCP catalog-capability responses” sounds sufficient, but Build Realist notes real drift is not string equality: identity matching, modifiers, availability semantics, shared IDs, cents/decimal normalization. A synthetic live-read fixture with shared IDs can fake the hard part.  
   Resolve: Acceptance criteria must include at least one **ID-mismatch / entity-resolution drift class**, one modifier/variant ambiguity, and a report label distinguishing synthetic-controlled matching from real-world matching.

6. **P1 — Condition 5 / Slice-C cut is right, but “agent-selects-drifted-item” can still be a puppet show.**  
   Attack: The council correctly cuts fake checkout, but the demo claim remains vulnerable. Sharpener warns “scripted agent failing proves nothing,” then reframes proof as “protocol is truth-blind by design.” If the demo headline still says the agent “gets caught,” it overstates.  
   Resolve: Demo wording must be: **spec-faithful simulated agent follows a spec-valid but false surface; verifier catches the surface/SOR mismatch.** Gemini may be shown only as non-load-bearing color.

7. **P1 — Condition 6 / demote category claim is necessary but not enough; update stale artifacts too.**  
   Attack: `docs/suggestions-ledger.md` still has S-1/S-3 as accepted-for-planning with “conformance seat empty” and “truth layer for agentic commerce” as framing. `use-case-backlog.md` UC-7 still says ACP/UCP have “no independent conformance/verification tooling,” contradicted by the correction.  
   Resolve: Add a docs coherence gate: backlog, suggestions ledger, project state, and decision log must be corrected before plan sign-off.

8. **P1 — Condition 7 / 30-day tripwire is too adoption-weighted and too late for architecture risk.**  
   Attack: Tripwires include “zero external engagement” and “corpus uncited,” but the biggest failure mode is known before launch: live-read/direct-SOR architecture can invalidate the demo premise. Waiting 30 days wastes the wedge.  
   Resolve: Split tripwires: **pre-build crux gate**, **14-day build-slip gate**, and **30-day external-signal gate**.

9. **P0 — Crux formulation is close but not the most decision-changing crux.**  
   Attack: “Does drift survive food’s live-query shift?” matters, but the more decision-changing crux is: **who has authority, pain, and budget to require independent truth evidence?** Pain Validator says nobody is currently asking for feed-vs-POS attestation; Market Strategist says the direct audience is tens of orgs and venture value is trigger-dependent. Even if drift persists technically, UC-2 can still be a no-buyer artifact.  
   Resolve: Reframe crux as two-part: **technical persistence of copy/SOR drift** plus **surface/operator enforcement that creates a reason to run the tool.**

10. **P1 — Probability/confidence assignments are often cleaner than the evidence supports.**  
   Attack: Synthesis says “confidence: medium-high” for RESHAPE-PROCEED while also saying residual seat today is medium, stays-open 12–24 months low, UC-2 ordering evidence is weak, and all proceed-in-some-form consensus is suspicious. That does not add up to medium-high.  
   Resolve: Downgrade to **medium or medium-low** unless condition 8 resolves favorably.

11. **P1 — Showcase-with-venture-optionality is coherent only if venture language stops shaping sequencing.**  
   Attack: The council admits venture is “an OPTION,” but UC-2-leading still appears justified by frontier/category optics. That means the optional venture story is still steering build order.  
   Resolve: Make the build plan optimize first for **craft showcase + verified pain**, not category timing. Venture triggers should influence watchlist and launch timing, not core sequencing.

12. **P2 — Suspicious facts to flag, not disprove here.**  
   Attack: Per user constraint, I cannot verify live facts. Facts that look load-bearing and need current primary re-check: July 16 DCWP deadline; UCP catalog live-query wording; `ucp-schema` release/version; ucptools depth/pricing; UCP Food council participants; OpenAI feed-vs-website rejection; Gemini free-tier details; NYC LL79 effective date.  
   Resolve: Put these in a plan-stage “primary-source lockfile” with source URL, access date, quote/snippet, and status before any public claim.

**Bottom line:** RESHAPE-PROCEED holds only if amended. The missing hard gate is the problem: resolve the live-query/copy-layer and incumbent-depth crux before UC-2 build work, or the evidence points to UC-1 as the honest lead.