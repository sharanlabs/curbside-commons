# Pre-build design + language inspection — five adopted mockups (session 28, 2026-07-20)

**Scope:** owner-worded session-28 stage inserted before build pieces 1–3: full Fable-judged inspection of the five ADOPTED mockups (design, fonts, palette, spec alignment) + sol end-to-end language pass (flow/clarity/nuance/tone/consistency/coherence). Floor-not-ceiling.
**Method (maker≠judge held):** two independent read-only inspection agents (A: static design-system consistency — fonts byte-compare, full color census, type scale, chrome diff, component drift; B: rendered visual + spec alignment — headless Chromium 1440 + 2560, full-page/header/footer captures, runtime DOM probes) + gpt-5.6-sol@high via codex-guarded (probe SEAT_OK; one startup-gate resume; one CLI-arg retry — `exec resume` takes no `--sandbox`; raw on record in scratchpad sol-err2.txt) reviewing the rendered text of all five pages against spec §3. Fable adjudicated every finding primary-model-final, personally verified the headline defects on screenshots, and fact-checked load-bearing claims against the repo (spec §1/§11, SHOWCASE.md L-2 send record, golden fixtures). Codex excluded from design throughout — the sol packet was language-only by construction.
**Targets (SHAs verified = the adopted record):** v9 Home/01 `1a47722c…1d13c8` · 02 `cb1f45fb…0bc42e` · 03 `adaee213…64ea3c35` · 04 `b028d8ee…` · /docs `704a1a69…`.
**Verdict:** the set DOES read as one coherent ultra-premium site (register, chrome, receipt-as-brand geometry hold; fonts byte-identical ×5; §4 NYC-alone clean; §8 replay labels clean; §11 /docs register right). Six defect groups and three normalization packs stand between "gated per page" and "one finished site." NO adopted file was edited this session-stage; all dispositions below are build-input unless the owner words a mockup fix.

## TIER 1 — DEFECTS (Fable-confirmed)

- **D1 · 02 Fees is wholly verdigris/teal — violates the standardized ultramarine law.** 10 defect sites (tokens `--accent #0b6a5c`/`--iris #12897a`/`--iris-ink`, grad-phrase, grad-cta ×2, a:hover `#07463d`, ::selection, eyebrow diamond, ghost-btn hover, active-tab ring); the ONLY ultramarine on the page is the logo. Cause: 02 was adopted session 26, BEFORE the ultramarine standardization landed with the owner's own 03 recolor (session 27); the recolor never propagated back. Fix = the exact 03 pattern (token swap + literal sweep), red-green, new SHA. **Owner pick: recolor the mockup now vs at build piece 3.**
- **D2 · Home JS-mode cascade bug ×2 (rendering defect, Fable-verified on screenshot).** `.nojs-only{display:none}` (L56) is outspecified by later same-specificity display rules: (a) `.cs-status` fallback caption overprints the live status → visibly scrambled glyphs in EVERY JS render (probe: both spans display:flex at x56 y969); (b) the static "SERVED PRICE · 2150.00" `.fixedrow` renders alongside the live editable input and contradicts edited verdicts. The known v9 defect class (display-order leaks), JS-mode mirror — which is why the no-JS batteries never bit. Fix: `.js .nojs-only{display:none!important}` red-green.
- **D3 · Home stale "two-page sample" copy ×7** (nav tooltips ×3, parked note, door sub, footer tooltips ×2) — factually stale on a five-page adopted set; also makes 02 sound unavailable (sol P1 concurs).
- **D4 · Spec-§3 retired vocabulary renders on Home/01 only:** "THE KITCHEN" ×4 (scene station, status rail, sr text), "THE COMMONS" status strings ×3 + FIG caption, "serving copy" ×2 (golden lines 05/08 — the standing sanctioned-regen watch item; goldens NEVER hand-edited). Zero occurrences on the four later pages. Fix at build piece 1: scene/rail chrome relabeled (e.g. THE MENU / THE RECORD), status strings reworded, goldens via the real generator.
- **D5 · 02→03 continue-door mispromise:** "Paste a statement of your own and keep the receipts it earns" → 03 is entirely the FEED bench (zero "statement" occurrences). Statement pasting lives on 02/the app's /fees paste leg. Fix door copy.
- **D6 · Held-lamp semantics are inconsistent within and across pages — needs ONE owner ruling.** Law: ember=violation · gold=held · graphite=pass. Practice: Home header lamp = gold for CLAIM HELD ✓ but `.try-verdict.held`/stamp shadows = ember; 03 `.pcard .p-held` = gold ✓ but `.vpanel.held` = ember; 02 adds gold "cond" meanings (`.vtag.cond`, `.stamp.gold`, `.mdot.gold`); 04 drops ember/gold entirely (graphite `--defer` set — defensible) but introduces a BLUE pulsing header lamp (4th lamp color) and an all-blue logo (other four pages: blue→gold mark). **Recommended ruling:** ember = the violation act/evidence (≠ flash, FAIL, VIOLATION stamps) · gold = held-STATUS chips/lamps/stamps · graphite = pass/neutral/deferred · blue never a lamp · one logo everywhere (blue→gold). Normalize at build after the word.

## TIER 2 — NORMALIZATION PACKS (build piece 1 token sheet; no owner gate)

- **N1 · Three type registers on one site:** HOME lightest (H2 545/-.038, eyebrow 11.5, nav 13/500) → 02 heavier (575 across the register) → 03/04 heaviest (eyebrow 12.5/750-weight d-eyebrows, nav 13.5/600). H3 = 17/19/15.5px. Hero clamp 31→43 (Home) vs 30→44 (04). Normalize to ONE register (recommend the v9/Home base).
- **N2 · Chrome text drift:** case-readout grammar (CASE 001 / CASE 002 / BENCH / LOGBOOK — also: 02 opens "CASE 002" while Home frames the fee file as FILE B of the one continuing case, spec §2; recommend one grammar + the owner's taste on case numbering); footer honesty line ×4 variants (recommend one canonical + 02's NYC addendum); tooltip wording/casing ×3; door eyebrow NEXT vs CONTINUE; door element types (a/div/a-no-href); brand lockup element (button/span/a — on 02/03/04 NO route back exists; app build wires real nav anyway); 04's `.nav-labeltext` wrapper.
- **N3 · Micro-token near-misses:** link-hover ink 3-way (#25329a/#1c2ba8/#1c2aa8); active-ring alpha .2 vs .3; grad-phrase terminal stop (gold on Home/03, blue on 04 — tie to D6 ruling); stamp geometry 02 variant; `.lp-btn`/`.door` padding deltas; docs: token-system absent, unique 13px radius, warm-tinted rules/elev (JUDGED FINE as the §11 background register — keep), dead `.lamp.gold/.ember` CSS (delete), `--ember-ink` #b42318 vs #b71f13.
- **Layout polish:** listings CLAIM/RECORD/RULE/VERDICT strip clips its 4th card at 1440 (+hairline sliver, no scroll affordance); 03 bench left column ends ~1,200px before the receipt (emptiest section in the set); 2560: scene stations hug viewport edges while content stays centered (worse at 3840); 04 centered hero = the only centered chapter page (owner taste: keep or left-align).

## TIER 3 — SOL LANGUAGE PASS (38 findings: 13 P1/22 P2/3 P3 — Fable-adjudicated)

- **ACCEPTED (~24, applied at build pieces):** 02 drama trims ("Nothing came", "closed empty", "not a loophole", "an honest audit names its edge"); 03 metaphor trims (merchant/same/reference "world" ×3 in one section; "one or the other"; LIVE/REPLAY phrasing aligned to the visible labels); 04 slogan trims ("Calibrated the hard way", "ships" ×4, "on the record" ×5, "The tie protected the default" repetition, "A product that reports its own misses"); /docs personification + no-send-repetition consolidation + "honesty label" → concrete output-distinction wording; repeated-phrase dedup (the "same input → same receipt" refrain ×6 site-wide → keep the one precise formulation; "says so" ×5; "real" ×14); **"Binds in the build" ×~21 in source across 02/03/04 → replace with concrete handoffs**; receipt field-name canon (normalize to the actual golden schema); figure-label grammar; 04 fragments (#37, #38); 02 hero-paragraph density (#16, WITHOUT sol's added "example" qualifiers); #10 caps-vs-17-rules tension; #11 "ever being offered" evidence-overreach.
- **ACCEPTED-MODIFIED:** every sol rewrite that injects "simulated / example / in this example" qualifiers is stripped of them — the owner's quiet-honesty word (§0: "you dont want hint or mention anywhere…") overrides sol's caution; the ONE footer line carries the boundary.
- **REFUTED:** #1 (GitHub/n8n/Slack are spec-§1/§11-mandated names + RULES §8 runtime stack); #2/#5 as P1s (live-order framing is the chosen register; quiet-honesty covers it); #8 (misread — home→01 is the forward chain inside one file); #12 (the "single outbound send, eight safety controls" is TRUE: owner-armed L-2 send 2026-07-09, HTTP 200, redacted record committed — SHOWCASE.md §A3); #36 ("Break the feed yourself" = the owner-adopted READER-ENTERS beat).
- **FLAG→BUILD-VERIFY:** #7 empty-seat claim must render its as-of-mid-2026 date (spec §4 requires it — verify present at build); #15 "sixteen KINDS of disagreement" vs the goldens' actual rule diversity (verify, likely "sixteen disagreements"); #13 04→docs "full method and figures" promise vs docs' actual scope (soften); #34 receipt-schema canon from the real golden fields.
- **Cross-page:** sol independently confirmed the 01↔02 chain stale-copy break, the 02→03 paste mispromise (= D5), and that /docs is the calmest page (correct §11 register); page 01 is the register outlier (cinematic + retired vocab — consistent with it being the oldest file, pre-dating the later pages' language discipline).

## Accepted deviations (recorded, no action)

- Dark instrument consoles (Home/03 rails, 02 audit panel, 04 replay consoles, docs excerpt) vs §6 "never darkness": JUDGED a deliberate third "console" material carrying the §8 REPLAY honesty labels; instrument-local, never page ground; consistent ×5. Stands.
- /docs warm-paper narrower register: sanctioned by §11 background-documentation role. Stands.
- Fees "CASE 002" + hero verbatim supersession (spec §3's "$2,150" hero was superseded by the owner's no-numerals lock → W3 hero): recorded, not defects.

## Owner picks surfaced (this stage's new ones first)

1. **D6 held-lamp semantic ruling** (recommended: ember=violation act · gold=held status · graphite=pass/defer · blue never a lamp · one blue→gold logo).
2. **02 recolor timing:** mockup now (red-green, new SHA, re-adopt) vs at build piece 3.
3. **Home mockup defect fixes (D2/D3) now vs at build piece 1.**
4. Case-numbering grammar (one continuing case vs per-chapter case numbers).
5. 04 centered hero: keep (logbook genre) or left-align to the chapter register.
6. Standing: Phantom Platter rename · DCWP junk-fees hearing 2026-08-07 before /fees ships · stale Cloudflare deploy · domain at deploy · OG card · robots · statute-status · d-4 · orphan design project d101e9bf · optional push-back of corrected 04+/docs to 8bed5e72.

**Build-readiness:** nothing above blocks build pieces 1–3. This ledger is BINDING BUILD INPUT: piece 1 consumes D2–D4 + N1–N3 + the sol pack for Home/nav; piece 2 the chrome canon; piece 3 D1 (if not fixed earlier) + D5 + the 02/03/04/docs sol items. Evidence: agent reports + probe scripts + sol raw in the session scratchpads; screenshots ×28 in the session shots/ dir.
