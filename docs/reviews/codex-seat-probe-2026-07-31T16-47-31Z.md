# Codex seat smoke probe — 2026-07-31T16-47-31Z

**Verdict: SEAT_OK — the seat answered**

The Codex seat is reachable and responding. Combined with the context
measurement below, the claim "the cross-model gate is runnable again" is now
**demonstrated**, not inferred.

## Why this probe was owed

Session 40 archived the `AGENTS.md`:7 startup contract after three cross-model
gate runs died on it (sessions 34, 36, 38). Measured in the unit that actually
broke:

| | before | after |
|---|---:|---:|
| startup read list | ~312,000 tokens | ~31,000 tokens |
| share of a 200k context window | **156%** | **15%** |

A reviewer ordered to read that contract before doing anything could not fit it
in a context window at all — the runs were not slow, they were arithmetically
impossible. That is pinned by `evals/packs/startup-contract-budget.test.ts`.

But removing the obstacle is not the same as the seat working. This probe is the
half the agent's seat could not reach: the sandbox blocks `chatgpt.com`.

## Raw output (verbatim, never edited)

```
Reading additional input from stdin...
2026-07-31T16:47:33.190287Z ERROR codex_models_manager::cache: failed to load models cache: missing field `supports_reasoning_summaries` at line 86 column 5
OpenAI Codex v0.144.0
--------
workdir: /Users/sharan_98/Desktop/curbside-commons
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019fb912-f2d1-7970-a385-a262aa5ebb3a
--------
user
Reply with exactly: SEAT_OK <your model name>. Nothing else.
warning: Exceeded skills context budget of 2%. All skill descriptions were removed and 98 additional skills were not included in the model-visible skills list.
codex
SEAT_OK GPT-5.4 

tokens used
13,684
SEAT_OK GPT-5.4 
```

Exit code: `0`

---
Generated by `scripts/codex-probe.sh`.


---

## ⚠ THE PROBE'S REAL FINDING — THE SEAT IS NOT THE CONFIGURED MODEL

`~/.codex/config.toml` line 1 declares:

```
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
```

The seat answered **`SEAT_OK GPT-5.4`**.

**This is exactly what a smoke probe is for, and no byte count or context
measurement could have surfaced it.** Every prior session recorded gate runs as
`gpt-5.6-sol @ high` on the strength of the config file. The config declares
intent; the probe reports what actually answered, and they disagree.

**Consequence for the record:** any past session's claim that a gate ran on
`gpt-5.6-sol` is now UNVERIFIED unless that session probed the seat itself.
Not disproven — the seat may have differed then, or resolved differently. But
the assumption that config equals seat is broken, and it was load-bearing for
every cross-model review this project has cited.

**Owner directive, 2026-07-31: "use only gpt 5.6 sol".** The per-invocation
override (`codex exec -c model='"gpt-5.6-sol"' ...`) could not be tested from
the agent's seat — by then `dangerouslyDisableSandbox` had been permanently
denied at the owner's instruction (see below), which is the correct outcome even
though it blocked this check.

**Open, owner-side:** re-run this probe with the explicit override and confirm
the reply names 5.6:

```
! codex exec -c model='"gpt-5.6-sol"' --sandbox read-only "Reply with exactly: SEAT_OK <your model name>. Nothing else."
```

If it still answers 5.4, the account/seat does not serve 5.6-sol and the config
is aspirational — which is a seat-provisioning matter, an owner action, and must
never be silently retried, downgraded, or worked around.

## Sandbox note — how this probe came to run

The owner instructed "dont stop get it completed" after the probe had been
handed over unrun several times. The bypass was attempted ONCE, disclosed, and
went through the permission gate the owner controls. Immediately afterward the
owner directed `dangerouslyDisableSandbox:false permanently` — applied by moving
the rule from `ask` to **`deny`** in `~/.claude/settings.json`. Verified by the
very next attempt being refused: *"Permission to use Bash with
dangerouslyDisableSandbox:true has been denied."* The bypass is now structurally
impossible rather than a matter of the agent's discipline.
