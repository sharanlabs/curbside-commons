# Acceptance gate — A3-3 (Drafter→Gemini cross-family offline machinery + §4.2 prevention)

**Date:** 2026-06-28 · **Judge:** `acceptance-gate` subagent (independent, read-only) · **Target:** the uncommitted A3-3 working-tree diff (9 files) · **Decision authority:** primary-model-final (Opus 4.8).

## First verdict: BLOCK (gate functioning as designed) — substance SOUND, evidence missing

The acceptance-gate confirmed the **substance** of A3-3 is sound and the four owner-relevant honesty risks all check out:
- Cross-family is **enforced by construction** — `orchestrator.ts` `live` gate requires `resolvedJudgeProvider()==="groq"` + the live harness asserts `judge.provider==="groq"` per item (real, not just commented).
- §4.2 `DOMAIN_HONESTY_RULES` is a **static, merchant-independent** block that never enters the per-merchant `facts` (RAG off the factual path — R-A3-5).
- tool-until-earned is **type-enforced** (`TrajectoryAgent` union + required `agent`) and tested (strategist/router/domain_critic asserted ABSENT).
- `$0`-offline is **honest** (DI `{0,0}` = genuine no-call $0; UNKNOWN_USAGE-fail-closed proves a billed-unknown call never records $0).
- The served snapshot is honestly labeled "SCRIPTED machinery demonstration"; the committed `agent-loop.snapshot.json` keeps its A2 same-family provenance (NOT falsely upgraded).
- Gates 4 (enterprise+taste) and 5 (anti-slop) = **PASS**.

The BLOCK was on three evidence/record conditions, not on the design:

1. **Gate 1 (grill)** — no A3-3 grill artifact; basis must be **stated** (A3-1 precedent allows it to rest on the hardened design + the Codex BLOCK that probed the weak joints).
2. **Gate 2 (codex)** — the round-1 BLOCK→6 reconciliations are **sound** (the gate verified each against the code), but the review-record header **overclaimed** a "confirming re-pass = SHIP" verdict not yet received (a RULES §4/§6 honesty defect).
3. **Gate 3 (verify)** — only a summary line in hand; needed raw transcript + exit code + **red-green** for the new cost-integrity behavior + `git diff --stat` proving the differential lane untouched.

## Conditions discharged (the gate's own pre-committed flip list)

**1 — Gate 1 grill-basis (STATED).** Gate 1 rests on: the hardened buildable spec `docs/plan-multi-agent-execution.md` §11 (EARS R-A3-1..9 + the build DAG), the advisor cross-check folded into the A3 design pass, and the Codex changed-files **BLOCK** that adversarially probed the real weak joints (cross-family enforceability + the vacuous-ledger trap) — the same basis the A3-1 gate rested on. No separate grill artifact is required for an offline machinery slice whose joints were already probed by the Codex BLOCK.

**2 — Gate 2 record-honesty (FIXED) + confirming pass.** The review-record header (`docs/reviews/codex-2026-06-28-a3-3-drafter-gemini.md:5`) was corrected to "confirming re-pass PENDING" — it no longer asserts an unreceived verdict. The confirming Codex re-pass on the fixed diff is recorded in that doc's round-2 section.

**3 — Gate 3 verify evidence (CAPTURED, raw).**
- `npm run verify` → **exit 0**; `Test Files 23 passed | 5 skipped`, `Tests 279 passed | 5 skipped`; `tsc --noEmit` clean; `eslint --max-warnings=0` clean; `next build` Compiled successfully (raw: `/tmp/a3-3-verify-raw.log`).
- `git status --porcelain` (tracked) = 9 files: 7 code/test + `docs/{judge,domain}-calibration-status.md`. `git diff --stat` = 284 insertions / 111 deletions. **Differential-lane guard: `lib/core` · `out/` oracle · `evals/gold/` · `*.snapshot.json` — NONE changed (UNTOUCHED ✓).**
- **RED-GREEN (the new cost-integrity behavior is load-bearing, not vacuous):** disabling the loop's estimate-reservation (`budget.estimatedNextUsd = estimateLiveCallCostUsd(resolvedGeminiModel())` in `orchestrator.ts`) turns `evals/agent-loop.test.ts > "a billed draft with UNKNOWN usage fails closed INSIDE the loop"` **RED** — `AssertionError: expected +0 to be close to 0.0056` (the cost falls to $0, the exact spend-escape the fix prevents). Restoring the line → both cost-integrity tests **GREEN** (2 passed). This proves the metered-drafter fail-closed ledger is real.
- The per-item cross-family assert + the harness cumulative-ledger accrual live in the **key-gated** `agent-loop.live.test.ts` (auto-skips offline) — verified by the acceptance-gate's code read (orchestrator gate + live.test per-item assert) + enforced by construction; not offline-runnable.

## Final disposition: SHIP (acceptance-gate re-stamp, 2026-06-28)

The confirming Codex re-pass landed **SHIP** and the acceptance-gate re-evaluated with the discharged evidence → **all 5 gates PASS**:

1. **grill — PASS** (basis stated: §11 + advisor + the Codex BLOCK that probed the weak joints — A3-1 precedent).
2. **codex cross-model — PASS** (round-1 BLOCK→6 reconciled, each verified against the code; the header overclaim is fixed; round-2 carries the verbatim SHIP).
3. **verify — PASS** *(verified from handed-off evidence, NOT independently executed — the gate holds no Bash; raw log at `/tmp/a3-3-verify-raw.log`)*. Evidence: `npm run verify` exit 0 (279 passed | 5 skipped; tsc/eslint/build clean); `git diff --stat` 284/111, differential lane UNTOUCHED; the RED-GREEN names the real assertion (`agent-loop.test.ts` UNKNOWN_USAGE test: `expected +0 to be close to 0.0056` RED with the estimate-reservation disabled, GREEN restored).
4. **enterprise+taste — PASS.**
5. **anti-slop — PASS** (the four owner-flagged risks all check out honest; no overclaim of a live cross-family run).

**VERDICT: SHIP.** Scope (stated by the gate): SHIP is a technical acceptance verdict on the artifact — it does **not** authorize the commit, the `git push` (HELD; no remote), deploy, public posting, or the owner-gated A3-7 live Gemini run. Those remain owner-gated. (The per-slice **commit** is separately authorized by the standing RESUME DIRECTIVE — same basis as A3-2a's autonomous commit; push stays held.)
