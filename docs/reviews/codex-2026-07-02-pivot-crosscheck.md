# Codex Cross-Check — Reframed Pivot Direction + Council Verdict (2026-07-02)

**Gate:** the mandatory cross-model check on the consequential pivot (CLAUDE.md Operating Doctrine). **Seat:** smoke-tested `SEAT_OK`; run read-only via `codex-guarded` (config default model @ xhigh); raw verdict: `codex-2026-07-02-pivot-crosscheck-raw.md`. **Scope reviewed:** research digest + ADDENDUM + CORRECTION · full council session + synthesis · use-case backlog · suggestions ledger. Codex had NO live web (by sandbox) — instructed to judge inference validity and flag, not assert, on live facts.

## VERDICT: CONFIRM-WITH-AMENDMENTS → reconciled primary-model-final, ALL 12 ACCEPTED

| # | Sev | Finding (short) | Disposition |
| --- | --- | --- | --- |
| 1 | P0 | Crux teardown must be a HARD PRE-BUILD GATE (condition 8), not a "next test" | ACCEPTED — plan §2 condition 8 + §5 gate before W1 |
| 2 | P0 | S-5 "commit as-is" unsafe: require owner acceptance record + final verify + provenance caveat; claim capped at degraded labels | ACCEPTED — plan §5 S0 rewritten |
| 3 | P0 | "Parallelize UC-1" too weak vs the council's own evidence: UC-1 = primary evidence/AI-depth track; UC-2 = bounded frontier demo unless gate 8 confirms the seat | ACCEPTED — plan §2 condition 2 rewritten |
| 4 | P1 | July-16 DCWP owner call gated by a one-page source memo (exact rule text/deadline/dates; digest-vs-addendum effective-date conflict named) | ACCEPTED — plan §9 O4 |
| 5 | P0 | Surface-agnostic underspecified: require ID-mismatch/entity-resolution + modifier-ambiguity drift classes + a label distinguishing synthetic-controlled matching from real-world matching | ACCEPTED — plan §4 C3 amended |
| 6 | P1 | Demo wording: "spec-faithful simulated agent follows a spec-valid but false surface; verifier catches the surface/SOR mismatch" — never "agent gets caught" as headline; Gemini = non-load-bearing color | ACCEPTED — plan §4 C7 + §5 D1 |
| 7 | P1 | Docs coherence gate: suggestions-ledger S-1/S-3 + backlog UC-7 still carry the falsified "seat empty"/"truth layer" framing | ACCEPTED — corrections appended same session |
| 8 | P1 | Split tripwires: pre-build crux gate / 14-day build-slip / 30-day external-signal | ACCEPTED — plan §10 |
| 9 | P0 | The crux is TWO-PART: technical drift persistence AND enforcement/buyer authority ("even if drift persists, UC-2 can be a no-buyer artifact") | ACCEPTED — plan §10 reframed |
| 10 | P1 | Synthesis confidence "medium-high" doesn't follow from its own components → downgrade to MEDIUM, conditional on gate 8 | ACCEPTED — plan + synthesis note |
| 11 | P1 | Venture language must not shape sequencing; optimize for craft showcase + verified pain | ACCEPTED — subsumed by #3 |
| 12 | P2 | Primary-source lockfile (URL · access date · quote · status) required before any public claim | ACCEPTED — plan §11 deliverable |

**Reconciliation note (primary-model-final):** no finding refuted — each tightens the council's verdict using the council's own evidence; none contradicts primary data held. Net effect: RESHAPE-PROCEED stands at **MEDIUM confidence, conditional on the pre-build crux gate (condition 8)**; UC-1 is the program's primary evidence/AI-depth track; UC-2 is a bounded frontier demo pending gate 8. The Codex gate for the PIVOT DIRECTION is DISCHARGED (this review); the per-module build gates (M0/M1/M2) remain ahead.
