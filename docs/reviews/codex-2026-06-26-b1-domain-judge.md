# Codex Cross-Model Review — B1 domain-quality ("Effective"-axis) judge

- **Date:** 2026-06-26
- **Reviewer:** Codex `gpt-5.5` @ `xhigh` (`~/.codex/config.toml` default; ship-gating tier per owner doctrine), read-only, via `~/claude-os/bin/codex-guarded review --base 07e9a55` (CLI 0.136.0; session `019f0571-1c8f-7c53-955c-5f4840114523`).
- **Scope:** the FULL B1 diff `07e9a55..HEAD` (18 files, +3686 / −1) — the rubric, judge, gold set, harness, offline + live + eval-lock calibration tests, the frozen snapshot, and **all the honesty-bearing docs** (spec, calibration-status, the two prior gate records). `07e9a55` verified = the exact parent of the first B1 commit `db72461`, so this single base covers B1a→B1d and **also discharges the B1-offline Codex obligation** (`gate-2026-06-26-b1-offline.md`).
- **Raw verdict (ephemeral):** `/tmp/codex-b1-review-activationops.md` (5195 lines incl. the xhigh reasoning trace; the MCP OAuth errors at the top are Codex's Linear/Figma MCP clients failing to auth — unrelated noise, not part of the review). **This file is the durable record** (per the doctrine fix for the "/tmp-only gate evidence" repeat-failure).

## Verdict: TWO P2 findings, NO P0/P1 — and no honesty / overclaim finding.

Codex's own summary: *"The domain judge adds useful tests and the offline suite passes, but live verdict acceptance can silently miss dimensions and the live gating uses the wrong provider environment namespace. These are actionable correctness issues for the new live domain-judge path."*

Both findings are **latent correctness bugs on the LIVE path** — not the shipped REPLAY/mock path, not `lib/core`, not the gold labels, not the frozen snapshot. **Scope caveat (do NOT over-read the silence):** `codex review` audits **code**, not prose claims — Codex's own summary is *"the new live domain-judge path."* So its silence on the *"directional / synthetic / n=18 / ~100-floor"* framing is **near-zero evidence on the honesty question, NOT corroboration of it.** R-DHON-3's *"Codex gate APPROVEs"* is discharged here as **"ran + no blocking findings"** (sufficient to clear the gate). The word **"calibrated"** is carried by what actually establishes it — the pre-registered-bar clearance on held-out + the eval-lock + the independent **acceptance-gate's** honesty affirmation + the documented **R-DARCH-2 leak-check** — and is held **directional** by R-DHON-1 (the ~100 floor), never by Codex declining to flag docs it was not auditing.

## Findings → reconciliation (primary-model-final)

| # | Sev | Finding | Disposition | Where fixed |
|---|---|---|---|---|
| **B1-1** | **P2** | **Partial-verdict acceptance** (`domain-judge.ts:394`). A schema-valid live verdict with 1–2 dimensions (`DomainVerdictSchema` is `.min(1)`), or duplicates that `normalizeDimensions` collapses to < 3, only rejected the **zero**-dimension case, then computed `domain_defective` from the returned subset → an **omitted failed dimension reads as passing**, so a domain-defective draft could clear the judge (a recall hole in a recall-favoring control). | **ACCEPT → FIX (recall/safety).** Require **all 3** rubric dimensions after normalize; otherwise **fail closed** to the deterministic mock (which always assesses all three) — `mode=FAILED_TO_FALLBACK`, `errorClass=INCOMPLETE_VERDICT`. Locked by 3 tests (omit a dimension, duplicate-collapse, and a does-not-over-reject complete-verdict case). | `lib/agents/domain-judge.ts`; `evals/domain-judge.test.ts` |
| **B1-2** | **P2** | **Wrong env namespace** (`domain-judge.ts:362-370`). Liveness read `judgeLiveEnabled()` → the **faithfulness** judge's `JUDGE_PROVIDER`, while the provider resolves from `DOMAIN_JUDGE_PROVIDER` (`resolvedDomainJudgeProvider()`). So `DOMAIN_JUDGE_PROVIDER=gemini` + `GEMINI_API_KEY` (no Groq key) reported `DOMAIN_JUDGE_LIVE_DISABLED`, while the unrelated `JUDGE_PROVIDER` could flip the path on against its own resolved provider — the documented domain override was unusable / misrouted. | **ACCEPT → FIX.** New `domainJudgeLiveEnabled()` in the env single-source-of-truth (`env-flags.ts`) reading `DOMAIN_JUDGE_PROVIDER`; used by both the liveness default and the real-call provider boundary; the live calibration runner's skip-gate aligned to it. Locked by 4 env-routing tests (Gemini override works without a Groq key; `JUDGE_PROVIDER` does not leak in; default-groq needs `GROQ_API_KEY`; off without `ENABLE_LIVE_AI`). | `lib/server/env-flags.ts`; `lib/agents/domain-judge.ts`; `evals/domain-calibration.live.test.ts`; `evals/domain-judge.test.ts` |

## Impact on the calibration result: NONE

The live calibration ran with explicit `{ live: true }` over the **default-Groq** setup (`GROQ_API_KEY` + `ENABLE_LIVE_AI`) and produced **36/36 LIVE_JUDGE / 0 fallbacks** with complete 3-dimension verdicts. Both fixes only alter the previously-unobserved cases (a partial verdict; a non-default provider) — for the default-Groq calibration path the behavior is byte-identical. The frozen `lib/data/domain-calibration.snapshot.json` and the eval-lock (`evals/domain-calibration.lock.test.ts`) are **untouched and green**, so the cleared pre-registered bar stands.

## Verification (post-fix)

`npm run verify` **green**: typecheck + lint + **250 passed | 4 skipped** + build (28 routes). +7 over the pre-fix 243 = the new lock tests. `lib/core` + the differential oracle + the gold labels + the frozen calibration snapshot **UNTOUCHED**.

## Net

**B1 cross-model gate: RAN (gpt-5.5 @ xhigh, full B1 diff) → 2 P2 code findings · 0 P0/P1 → both reconciled primary-model-final → fixed + test-locked → verify green 250+4.** R-DHON-3's "Codex gate APPROVEs" is discharged as "ran + no blocking findings" for B1 (and this also discharges the B1-offline obligation). The calibration result is unchanged. **Scope of the gate (do not over-claim):** Codex reviewed the **PRE-fix** tree (`07e9a55..HEAD`); the 2 P2 fixes are **test-locked, not themselves cross-model re-reviewed** — a confirming Codex pass on the fixes is **recommended, not blocking** (per the project's 2026-06-20 / 2026-06-22 test-verified-reconciliation precedent: P2 severity, locked by 7 tests). The honest post-gate label is **"calibrated — directional, pending the ~100 validation floor"**: the *methodology* is calibrated (pre-registered bar cleared on held-out + eval-locked + acceptance-gate + leak-checked + cross-model-reviewed), while the *metric* stays directional because the gold is synthetic (n=18 held-out) and below the ~100 floor (R-DHON-1). A code-scoped Codex pass does not make a synthetic-gold number production-grade.
