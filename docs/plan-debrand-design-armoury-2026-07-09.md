# Plan v3.3 — Curbside Commons: full-quality pass (honesty fixes · identity · de-brand · design · CI · enhancements · deploy)
## (2026-07-10 — v3.3: v3 + S0 rounds 1–3 reconciled, ALL findings accepted every round, 0 refuted — raws in `docs/reviews/codex-2026-07-10-unified-review-raw.md`; derives from the unified dual-flagship review + the owner's menu picks)

## S0 disposition checkpoint (decisions fixed BEFORE S1; Codex approval = advice, owner disposition = authority)
1. **Identity = `/legacy/**` path separation** (upgraded from label-as-legacy after the S0 pressure-test — both flagships now converge; full architecture in §Identity disposition below). **OWNER RATIFIES this disposition BEFORE S1/E1a execute** (surfaced at the loop's APPROVED exit; E1a consumes the /legacy/ skeleton, so ratification precedes all identity-bearing work).
2. **AP-09 goldens = current output**: generated templates (render-text/slack/email) migrate to "Curbside Commons" via a versioned template+golden migration (S4b, its own dedicated review); the historical L-2 send record stays immutable.
3. **Package identity**: `package.json` name → `curbside-commons` (nothing external consumes the npm name; recorded in S5).

## Owner picks (structured ask, 2026-07-10, verbatim labels — decision-log row at execution start)
- Identity split → **"Decide at plan v3"** (this plan carries the recommendation; the Codex loop pressure-tests it).
- Tier-3 enhancements → **ALL FOUR** (dashboard · RAG · MCP-approvals simulator · entity-resolution) + owner note verbatim: *"Ensure it is a well built covered in all aspects product."* → adopted as the plan's acceptance principle: every slice ships whole (code + eval + docs + honesty labels + route tags), never a demo-shell.
- CI claim → **"Add real GitHub Actions CI"**.
- Tier-2 → **"Keep all"** four slices.
- Standing: Codex = sol@high (sanctioned level, this workstream) · Haiku OFF · probe-before-heavy · lossless state-doc sync at every boundary · hard stops unchanged (no new live arming · no classifier re-run · freeze edits only via recorded reversal).

## Identity disposition (ACCEPTED at S0 round-1 — supersedes the earlier label-only recommendation; **owner ratifies BEFORE S1/E1a execute**, since E1a consumes the /legacy/ skeleton)
**`/legacy/**` path separation:** the landing is retold for the truth-audit engine (the canonical product per README/PUBLICATION); `/console` `/audit` `/merchant/*` MOVE under `/legacy/**` with their own route-group layout/metadata/footer/provenance; canonical nav = truth-engine surfaces only + one secondary "Legacy activation module" link → `/legacy/console`; merchant breadcrumbs → `/legacy/console`; old root URLs get an explicit tested policy (redirect stubs or 404); `/eval` `/metrics` `/cost` are REPLACED by the truth-engine dashboard (E1a), the legacy records they showed moving behind the /legacy/ destination E1a defines; e2e = dual contracts (canonical-product + legacy why-chain); the export manifest classifies canonical/legacy/error routes. Full removal ranked second; label-only ranked last (S0 round-1 pressure-test, raw on record).

## Slice DAG (order = dependency + urgency; **push policy per §Gates — local commits per slice, PUSH only after the covering batch review reconciles**; per-slice gates in §Gates)
- **S0 — this plan through the codex-review loop until VERDICT: APPROVED** `[route: codex sol@high via codex-guarded | verify: APPROVED recorded in docs/reviews/]`; findings reconciled primary-model-final between iterations; material scope change → owner. Round 1: REVISE, all accepted → v3.1. Round 2: REVISE (7), all accepted → v3.2. Round 3: REVISE (3), all accepted → v3.3. Round 4: **VERDICT: APPROVED** (2026-07-10; raw on record) — S0 CLOSED.
- **S1 — public-doc reconciliation + positioning honesty** (F-01/AP-01 · NEW-02/04/05/06/09/15 · Gate-P2 third class · B-02 README restructure: one-command demo + evals-harness story first): README/PLAIN-ENGLISH/PUBLICATION/WHY/GLOSSARY updated to the earned records (21/21 label, live agents, recorded send, deploy done, 961+7), overclaims scoped ("every DECLARED claim", exclusivity claims → mechanism+rigor positioning per decision-log 2026-06-22), per-module provenance (truth-engine vs classifier vs agents vs legacy replay), CLI help label clarity (bin/check.mjs), glossary refresh. `[route: fable(main)@xhigh — ship-grade public writing | verify: C10 green + claim-by-claim diff review + anti-slop pass + Codex batch (S1-S3)]`
- **S2 — footer/banner semantic disclosure contract** (F-02/AP-02/03 · AP-10 · Gate-P3-2): decision-log freeze-REVERSAL row FIRST → app/layout.tsx footer rewritten for the CURRENT product, scoped to the web replay ("this site initiates no sends; one recorded owner-armed send exists — see SHOWCASE"), non-affiliation sentence kept (e2e asserts it) → banner single-sourced or normalized parity test (copy.ts ↔ ReportView.tsx) → C10 scan set extended over landing + layout footer (+ a mockup claim test). `[route: fable(main)@xhigh | verify: new parity+C10 tests red-green; e2e console spec updated red-green]`
- **S3a — CI workflow only** (NEW-03): `.github/workflows/ci.yml` — `npm ci` + `npm run verify` + `npm run test:legacy` on push/PR (Node 24 pinned, immutable action pins, least permissions, concurrency+timeout); NO claim edits in this slice. Reviewed in batch A → pushed → **first green run observed live**. `[route: fable(main)@high | verify: the workflow's first green run on GitHub]`
- **S3b — CI claim flip (after S3a's green run exists):** README "machine-checked in CI" updated to TRUE wording citing the workflow + quickstart flips to `npm ci` (NEW-13). Rides the next reviewed push. `[route: fable(main)@low | verify: claim text cites the observed green run]`
- **S4 — de-brand with tracked inventory** (AP-08 · Gate-P3-5 · F-13 · NEW-12): inventory committed FIRST — (a) every brand hit classified **edit / factual-attribution-keep / frozen-archive / historical**, with the Gate-P2 rule VERBATIM: *dated public-market prose is genericized; its named, dated citation is preserved and linked in the research digest*; (b) `mockups/README.md` root inventory covering ALL ~54 artifacts by shipped/current/superseded/historical status. Sweep covers site+docs+mockups incl. OpenAI/Stripe/Google/DataSF + `&nbsp;` encodings; final `out/` scan against the allowlist; GitHub About checked explicitly. Never touch legacy/ anchors, history docs, the send record. `[route: fable(main)@high | verify: zero-brand grep vs inventory + verify floor green]`
- **S4b — golden/template identity migration (dedicated, closed BEFORE S5/E1 consume it)** (AP-09 per the S0 disposition): versioned template change render-text/slack/email → "Curbside Commons", sanctioned golden regen with an explicit per-file frozen-allowlist, the historical send record untouched. **Its own dedicated Codex review (batch B), reconciled before any later slice builds on the new strings.** `[route: fable(main)@high | verify: golden regen sanctioned + allowlisted diff exact + verify floor]`
- **E1a — truth-engine observability dashboard, first cut + route skeleton** (Tier-3 #1; BEFORE S5 so identity freezes against the finished surface): replaces /eval /metrics /cost content with the committed eval evidence — lock tests, calibration snapshots + floors, L-1 matrix, $0-enforcement import-graph proofs, run records — per-figure provenance (file+SHA); **defines the `/legacy/**` destination + route skeleton the identity slice needs (legacy records move behind it)**; deployed-SHA/date line: **injected at build time (source SHA + UTC build timestamp, labeled accurately) — implemented and tested here, consumed at S8**; ARIA tabs: **fix the actual `components/report/ReportView.tsx:132-146` defect (NEW-10) here** — full tabs pattern or plain toggle buttons + keyboard coverage. All static/offline. `[route: frontend-specialist@opus xhigh builds; fable judges; data wiring fable(main)@high | verify: figures traced to committed files (spot-audit) + new view tests red-green + verify floor]`
- **S5 — identity resolution execution** (NEW-01/07/11 per the S0 disposition, OWNER-RATIFIED first): `/legacy/**` route-group (own layout/metadata/footer/provenance; console/audit/merchant move; breadcrumbs → /legacy/console), canonical nav = truth-engine only + one secondary legacy link, landing retold (truth-audit story, honest coverage wording from S1), old-root-URL policy implemented + tested (redirect stubs or 404), package.json → `curbside-commons`, **dual e2e contracts: canonical-product (metadata/H1/nav/report/demo/disclosures) + legacy why-chain — PLUS the ARTIFACT-MODE Playwright config (statically serves a recorded `out/`, no dev server, no build) that the release gate runs both contracts under**. `[route: frontend-specialist@opus xhigh; fable judges | verify: both e2e contracts red-green in BOTH modes (dev + artifact) + verify floor]`
- **S6a — Emil VET (HARD GATE — closed before any consumption):** SHA-pin, MIT notice retention, neutralize the scripted-greeting payload, skill-security-auditor pass, **its own dedicated review reconciled and CLOSED before the brief or sample reads a single Emil file** (Gate-P3-3). Estate-side commit in ~/claude-os. `[route: fable(main)@high + skill-security-auditor | verify: VET record + closed review, both on record BEFORE S6b starts]`
- **S6b — design lane** (Tier-2; owner 07-10 direction row): (a) design BRIEF: live study of the two reference pages + 2026 motion sweep distilled to principles (never copied); vertical axis; gallery pure white; icons, micro+macro motion; hold-and-release interaction (owner confirms the reference at brief delivery, or the brief designs our own and SAYS so — **with a non-pointer/keyboard equivalent specified**); fonts decision (self-host or recorded exception, AP-14); canonical fragment matrix (AP-15); dated adopt/reject source matrix (AP-18) `[route: fable(main)@xhigh]` → (b) BUILD `mockups/<name>.html` **+ update the S4 mockups/README inventory (status: current/new) and PROVE the new artifact is picked up by C10's dynamic mockup scan (red-green)** `[route: frontend-specialist@opus xhigh | verify: Fable-equivalence review + C10 dynamic-scan inclusion red-green + zero-external-requests (or the recorded exception) + reduced-motion + contrast ≥4.5:1 + anchor parity + **no-JS readability + keyboard-equivalent operation + focus behavior + semantic-markup checks (incl. the hold-and-release non-pointer path)**]`. Sample only — adoption to the live site is a later owner word.
- **E2 — RAG lane with pre-registered floors** (Tier-3 #2): corpus = committed fee rules/UCP schemas/GLOSSARY; local embeddings (Transformers.js-class, $0 offline) + small-corpus store (in-memory cosine / BM25 hybrid — smallest thing that works) **with a deterministic BM25 baseline comparison**; **provenance pinned: embedding model ID + revision/hash + license + storage dep; a network-denied clean run proves no runtime download**; citation-REQUIRED, abstains below threshold, advisory-only through the existing tool-registry seam (engine still decides); **integration contract decided explicitly: MCP exposure yes/no → registry schemas, tool envelopes, MCP transcript/tool count, SHOWCASE, crew allowlists updated deliberately**; **pre-registration (corpus composition, leakage screen, chunking/version metadata, metrics [retrieval hit-rate · citation precision · unsupported-answer rate · abstention calibration · injection-resistance], floors) committed early and REVIEWED in batch C BEFORE any scoring run**; results reviewed separately (batch D); wired into CI. `[route: ai-engineering-specialist@opus xhigh builds; evals fable-designed | verify: pre-reg commit precedes results commit (git-provable) + floors green or the label honestly defers]`
- **E3 — MCP-Slack approvals SIMULATOR** (Tier-3 #3, offline): signed-action approval flow (request → signed human decision → verified execution) simulated end-to-end offline over the crew's existing human-gate contract; **explicit threat model: signer identity + authorization, nonce/replay-protection, injected clock (expiry testable), tamper/forgery/wrong-actor tests, key handling; import-graph proof it cannot send or mutate live state**; the live interactive lane stays a future owner word. `[route: backend-specialist@opus xhigh; fable judges | verify: threat-model test suite red-green + import-graph $0/no-send proof]`
- **E4 — entity-resolution lane** (Tier-3 #4): adversarial messy-input matching (name variants, unicode, near-misses); **architecture stated up front: ADVISORY (never gating); exact matching stays the protected default; tune/test split; ambiguity → abstain-to-human; false-merge cost named**; offline precision/recall + fail-to-human floors **pre-registered early, reviewed in batch C before scoring** (same regime as E2); label states synthetic scope. `[route: ai-engineering-specialist@opus xhigh | verify: pre-reg-before-results git-provable + floors + verify floor]`
- **E1b — dashboard + public-surface RECONCILIATION refresh** (after E2–E4, before the release gate): dashboard rows for E2/E3/E4 evidence; SHOWCASE + GLOSSARY + limitations updated for the new capabilities; **the exposed-splits FAQ** (neither classifier split may be re-run; new claims need fresh pre-registration — the prepared honest answer); **the history disclosure** (current surfaces de-branded; git history intentionally preserved for provenance); **final public-claim reconciliation: all volatile counts re-derived at the release SHA, dated** (no stale totals). `[route: fable(main)@xhigh — public writing | verify: C10 + claim-by-claim check at the release candidate SHA]`
- **S7 — S-11 wording sync + demo record (AFTER batch D reconciles; reviewed in its own batch E)** (Tier-2; AP-06 · Gate-P3-4 · AP-16): diligence + suggestions-ledger wording → "final non-commercial display brand, no clearance claimed" + current USPTO Trademark Search terms; demo record = a NAMED committed artifact (`docs/demo-record-2026-07.md`: SHOWCASE commands verbatim + captured transcripts + exit codes) **generated against a NAMED source-under-test SHA recorded inside the artifact** (committing the record necessarily advances HEAD — the record names what it tested, never claims to be "the release SHA"). `[route: fable(main)@low-high | verify: exit codes + clean tree + the named SHA inside the record]`
- **HEADER POLICY (decided + committed BEFORE batch E, so batch E reviews it):** Pages header posture checked against current provider docs; any needed `_headers` (or equivalent) is SOURCE-CONTROLLED in this step and rides batch E's review — no preflight-induced source change can enter the candidate post-review. `[route: fable(main)@high | verify: header decision + files in the batch-E diff]`
- **PRE-GATE BUILD + PREFLIGHT (after batch E reconciles — the candidate is FINAL here; source frozen):** Pages project preflight (production branch semantics, current deployment id, wrangler + node/npm tool versions recorded) → **the SOLE build, produced clean-room: fresh isolated checkout at the candidate SHA → `npm ci` → one build with the injected source-SHA + ONE fixed UTC timestamp (E1a mechanism)** → `out/` hashed and IMMUTABLE from this point. Any subsequent source change = a NEW candidate (back through batch review). `[route: fable(main)@high | verify: recorded SHA + tool versions + artifact hash]`
- **RELEASE GATE (over the immutable artifact + its source SHA — explicit NO-BUILD checks only):** independent `acceptance-gate` pass + battery run WITHOUT invoking any build script: `npm run typecheck` + `npm run lint` + the vitest suites + test:legacy + **BOTH e2e contracts in ARTIFACT MODE — a Playwright config that statically serves the recorded `out/` (no `next dev`, no build)** + protected-file manifest check + offline/network-denied tests + **re-hash the immutable `out/` and confirm it matches the pre-gate record**. BLOCK → fix → NEW candidate (rebuild clean-room + re-gate); no deploy on a stale candidate. `[route: acceptance-gate@fable | verify: SHIP recorded in docs/reviews/ attesting SHA + unchanged artifact hash]`
- **S8 — DEPLOY the gated artifact, preview-first (LAST act; NO rebuild)** (F-07/AP-04/12/13 · NEW-08/14): **preview deploy of the SAME immutable `out/`** → full smoke on preview (manifest GENERATED from the actual export vs the expected route set, classified canonical/legacy/error; explicit unknown-path 404 probe; banner served; zero external requests; SHA/timestamp line correct; headers verified) → **production deploy of the same directory with confirmed semantics → re-smoke on the canonical URL**; Cloudflare deployment ID/time recorded; rollback note recorded. `[route: fable(main)@high | verify: preview+production smoke batteries + deployment id + artifact hash unchanged]`
- **WRAP** — state docs synced, decision-log rows, tooling/skills ledger, owner-unknowns surfaced, Law-8 cost report (planned vs actual runs/windows), resume prompt. `[route: fable(main)@high | verify: RULES §9 checklist]`

## Gates (every slice)
- verify floor (≥961+7 at plan time — **all public-facing counts re-derived + dated at the release SHA, never hardcoded forward**) · test:legacy 306+5 · explicit e2e where views change · C10 green (**scan set extended dynamically: every claim-bearing mockup classified current/new in the S4 inventory is covered; historical exclusions explicit**).
- **Protected-path proof (real pathspecs): EMPTY `git diff <slice-start-SHA>..HEAD -- legacy/ evals/ fixtures/`, EXCEPT an explicit per-slice recorded allowlist for intentional additions** (new eval suites, sanctioned S4b golden migration); any modification/deletion of an existing frozen file outside the allowlist FAILS the slice.
- **Push policy: local checkpoint commits per slice; PUSH to the public origin only after the covering Codex batch reconciles + gates re-run** (docs-only state-doc syncs exempt). Freeze/identity migrations (S2, S4b, S5) never push pre-review.
- **Codex batches (each ONE sol@high run, probe-first): A = S1+S2+S3a · B = S4+S4b (dedicated freeze-boundary review, closed before S5) · C = E1a+S5+S6a-gated-S6b + the E2/E4 PRE-REGISTRATIONS (reviewed before scoring; S6a's dedicated vet review closes INSIDE the slice, before S6b) · D = E2+E3+E4 results + E1b · E = S7 + S3b + the HEADER POLICY files + any batch-D residuals.** Then PRE-GATE BUILD+PREFLIGHT (no source changes) → RELEASE GATE (no-build) → S8.
- **CI (S3a/S3b) hardening:** Node 24 pinned + immutable action pins + least permissions + concurrency/timeout; e2e added to CI when the dual contracts land (S5); the claim flip is S3b, strictly after S3a's observed green run.
- Seat events: raw verbatim + one owner-confirmed retry + NO-WAIT inline conversion (standing precedent). Acceptance-gate: post-batch-B identity milestone check + the RELEASE GATE before S8.

## Deferred / parked (owner-word tails, not in this plan)
Email one-shot (builders ready; one arming word) · live MCP-Slack interactive lane · any site adoption of the S6 design sample · domain purchase/USPTO manual (standing tails). *(The E2–E4 dashboard rows now land BEFORE the single deploy via E1b — no second redeploy needed.)*

---
# [SUPERSEDED] Plan v2 — kept as history (the dual verdicts below were absorbed into docs/reviews/unified-review-2026-07-10.md)
## (reworked 2026-07-09 — re-run through the updated doctrine + the 2026-07-09 model lineup)

## Context

The complete-all run finished 2026-07-09 (repo public at `sharanlabs/curbside-commons`, site live, verify 961+7, HEAD 906b522, tree clean). The owner returned with: tail answers, a multi-agentic demo question, a de-brand directive, a hybrid design sample (Swiss base + premium CTA/motion + story-flow narrative), the Emil Kowalski skills repo for the design armoury, a language-calibration brief — then **full delegation** ("use Fable as your judgement… take it forward as you like, no bars; corrections at the end"), then a **rework order**: the doctrine and new models released today must be reviewed and the plan re-run through them.

**Doctrine review result (2026-07-09):** `~/claude-os/docs/MODEL-ROUTING.md` is already current — updated TODAY: GPT-5.6 family GA + live-probed (Codex seat = `gpt-5.6-terra` @ `max` for ship-gating; `gpt-5.6-sol` 400-blocked, retry at dual-flagship pilot entry; `gpt-5.5`@`xhigh` verified fallback); Claude lineup Fable 5 (frontier) / Opus 4.8 (strong) / Sonnet 5 (workhorse) / Haiku 4.5 (economy) matches the live harness; new instruments landed today: `dual-flagship` (advisory whole-project cross-model review — ships nothing, gates nothing until calibrated) + `codex-review` (standalone adversarial PLAN-review loop) + `flagship-evaluator` agent. No doctrine edit needed — the PLAN re-derives from it.

**What v2 changes vs v1:** ① every step now carries the mandatory route tag `[route: model@effort | verify: check]` · ② Codex ship-gating reviews upgrade `xhigh` → **`max` on gpt-5.6-terra** (probed ladder ceiling; fallback gpt-5.5@xhigh, surface any limit error verbatim + skip + defer) · ③ the copy deck moves Opus → **Fable** (matrix: ship-grade writing = FRONTIER; the Opus pick stays only for the visual BUILD, which is both the owner's word and the matrix's coherent-build row) · ④ NEW Slice 0: the plan itself goes through the `codex-review` cross-model loop before execution (doctrine: consequential work — public claims + design + estate adoption — gets a Codex cross-check before it's treated as decided) · ⑤ optional advisory dual-flagship pass at the end (pilot-entry candidate; explicitly non-gating). Slices' CONTENT (A–E) is unchanged from v1.

## Owner's tail answers (recorded intent)
1. Domain purchase → **DECLINED** (free, non-commercial; `curbside-commons.pages.dev` stands).
2. Brand → **Curbside Commons FINAL** (closes S-11).
3. USPTO → **not necessary now** (non-commercial, no ® claim); documented precondition if ever commercialized.
4. Multi-agentic + manual demo → YES with honest nuance; offline/$0 demo path exists (Slice E).

## Delegated judgment calls (the four forks — decided under the owner's delegation, revertible)
| Fork | Decision | Rationale |
|---|---|---|
| Disclaimers name companies? | **Genericize everywhere** ("not affiliated with any delivery marketplace, POS provider, or other named business") | Owner directive explicit; generic stays honest. Decision-log note: named form is the stronger legal convention — revertible. |
| Factual market citations | **Genericize narrative/marketing prose; KEEP factual spec/format attribution** (GLOSSARY UCP co-developers, fixtures "Square-Catalog-API-shaped", code provenance comments) | Falsifying attribution violates RULES §6. |
| Mockup copy | **Story-flow ARC + VOICE re-applied to the CURRENT platform story** (truth-audit + crew + MCP + delivery) | The sample must sell what exists. File:line-cited copy deck (sample-6 precedent). |
| Vendor Emil skills? | **Yes — registry entry AND vendor all 4** behind the EXTERNAL-ADOPTION VET; neutralize the Law-11 scripted-greeting payload | Owner: "add it our design armoury in doctrine model as well." |

## Slices (order: 0 → A → B → C1 → C2 → D → E; per-slice commits under the recorded delegation word)

### Slice 0 — plan cross-check (NEW in v2)
- Record the delegation word + rework order verbatim in `docs/decision-log.md` FIRST, then run the **`codex-review`** loop over this plan (PLAN.md staging, read-only sandbox, same-session iterations until APPROVED or cap). `[route: codex gpt-5.6-terra@max via codex-guarded | verify: VERDICT:APPROVED recorded in docs/reviews/]`
- Findings reconciled primary-model-final (refute what's disprovable, fix what holds). A material scope change = back to the owner; wording/sequencing fixes = fold and go.

### Slice A — decision-log rows + S-11 closure (docs only)
- Rows: delegation authorization · domain DECLINED (supersedes wrap-row tail) · brand FINAL · USPTO deferred-with-precondition · the four forks · the v2 routing re-derivation note. `docs/suggestions-ledger.md`: S-11 → CLOSED. Append-only.
- `[route: fable(main)@low | verify: git diff shows append-only]` — trivial-mode Professional Process line; commit + push.

### Slice B — de-brand public surfaces (product code — FULL gates + redeploy)
- Sweep-and-classify first: `grep -rniE "doordash|uber ?eats|grubhub|square\b|toast\b"` over site + public docs (excl. legacy/, eval/, history docs) → edit / keep-factual / frozen table. `[route: fable(main)@high — wording is judgment-shaped (honesty invariants), NOT equivalence-qualified | verify: the gate battery below]`
- Edit set (confirmed by exploration): `app/page.tsx:355` · `app/layout.tsx:65` · `components/report/ReportView.tsx:115` · `lib/packs/listings/demo/copy.ts:53` · `README.md:7,:59` · `docs/PLAIN-ENGLISH.md:17,:68` · `docs/WHY.md:11,:15` (incumbents argument generically retold) · `docs/PUBLICATION.md:15` · `docs/product-brief.md:5` · GitHub About if branded.
- Invariants: literal `"Not affiliated with"` stays (e2e console.spec.ts:17) · `/simulated/i` in copy.ts + `SIMULATED` in DemoView.tsx (honesty-c10) · `DEMO_CLAIM` byte-untouched · NEVER touch `legacy/` (oracle anchor REFERENCE_PLATFORM_NAME), `eval/` goldens, snapshots, history docs. No golden regen needed (verified).
- Gates: `npm run verify` (961+7) · `npm run test:legacy` (306+5) · `npx vitest run evals/packs` · **explicit** `npx playwright test evals/e2e/console.spec.ts` (e2e NOT in verify) · zero-brand grep on the edit set · Codex changed-files review `[route: codex gpt-5.6-terra@max | verify: review record in docs/reviews/]` · commit + push.
- **Redeploy** (authorized by this plan's approval): `npm run build && npx wrangler pages deploy out/ --project-name curbside-commons --branch main` + smoke (routes 200, banner served, zero external requests).

### Slice C1 — design brief + copy deck (docs only)
- Visual spec: Swiss base (Inter Tight + JetBrains Mono · paper `#fbfbfa` · ink `#16170f` · vermilion `#d6442b` · column rules · numbered markers · gutter `clamp(20px,5vw,88px)` · maxw 1240), type shrunk 1–2 steps (hero cap ~92px · h2 ~44px · body ~19px); premium grafts (gradient CTA card radius-30 + glow + white-primary/glass-ghost buttons + $0.00 replay-note — dark CARD on light ground; the standing no-dark-background rule governs page grounds) · card system + hover lift · reveal-on-scroll · reduced-motion guard.
- Motion law (Emil extraction, MIT, fetched live): micro-interactions `cubic-bezier(0.23,1,0.32,1)`, UI < 300ms, never `ease-in`, never `scale(0)`, transform/opacity only, frequency framework; premium's `cubic-bezier(.16,1,.3,1)` for section reveals only; `review-animations` STANDARDS.md = judge rubric.
- Live 2026 design-reference sweep feeding the brief. `[route: research-specialist@fable xhigh (max-band rendering on Fable) | verify: dated cited digest]`
- Copy deck: 7-beat story-flow arc re-told for the current platform, every claim file:line-cited, dual-register per the owner's language brief, de-branded disclaimer from B, SIMULATED/replay framing prominent, "Run it yourself" receipts block (real offline commands). `[route: writing-specialist@fable xhigh — ship-grade writing = FRONTIER row (v2 change from opus) | verify: claim-by-claim citation check + honesty regex sweep]`

### Slice C2 — build the sample (mockup only)
- New file `mockups/swiss-story-premium.html` (name verified free). Builder: **frontend-specialist@opus xhigh** — owner's word AND the matrix's coherent-build row agree; seat-death fallback: inline on Fable + full Codex chain (accepted precedent). `[route: frontend-specialist@opus xhigh | verify: judge battery below]`
- Judge: **Fable-equivalence review** (main session, FRONTIER) against C1 deck + Emil STANDARDS + latest-references requirement; manual honesty sweep (honesty-c10 BANNED regexes by hand — mockups are outside the CI scan set); external-request grep (Google Fonts only); anchor-id parity; reduced-motion check; Codex changed-files review `[route: codex gpt-5.6-terra@max]`; `npm run verify` unchanged; commit + push. NO deploy — sample awaits owner reaction.

### Slice D — estate wiring (~/claude-os, never mixed into repo commits)
- Registry line in `knowledge/source-registry/design.md` (GitHub-repos section, `_schema.md` format, MIT + pushed-2026-07-09, verified date). `[route: fable(main)@low | verify: line renders, schema-conformant]`
- Vendor 4 skills → `library/_external-skills/emilkowalski-skills/` via the EXTERNAL-ADOPTION VET: Law-11 review, **neutralize the scripted greeting/animations.dev-link instruction** (strip + record), pin commit SHA, MIT note; supersede-pointer in the thin local `emilkowalski-motion`. `[route: fable(main)@high + skill-security-auditor pass | verify: VET record; Codex cross-check rides the Slice-0/B seat]`

### Slice E — manual demo session (no code changes)
- Pre-flight each command green at HEAD, then produce a short watchable record: `npm run demo` · `node bin/check.mjs fees fixtures/synthetic-restaurant/fees/statement.drifted.json` · `npx vitest run evals/crew` (offline crew replay incl. injection defenses) · `npx vitest run evals/mcp` (real stdio server) · pointers to `docs/SHOWCASE.md` + `evals/crew/gold/l1-live-*.json`. `[route: fable(main)@low | verify: exit codes + git status clean after]`
- Answer: YES — honestly multi-agentic (Intake + Reviewer earned "agent"; engine decides; humans approve), fully demonstrable offline/$0.

### Optional tail — dual-flagship advisory pass (pilot-entry candidate; explicitly NON-gating)
- After C2, optionally run `skills/dual-flagship/` over the changed surfaces as the instrument's calibration pilot (retry `gpt-5.6-sol` there per flags.md). Advisory only: ships nothing, gates nothing; findings feed the next session. Skipped without loss if seat/time says no.

## Language calibration — the answer to "am I thinking right? what am I missing?"
**Yes — progressive disclosure across two registers.** Enhancements folded into C1: ① show-don't-tell (real commands/transcripts as receipts — the "Run it yourself" block) · ② a skim layer (numbered markers + kickers + one pullquote per beat) · ③ tell the CURRENT story (story-flow's voice, today's platform) · ④ honesty as brand (SIMULATED/replay framing visible) · ⑤ premium = discipline (sub-300ms, reduced-motion, ≥4.5:1 contrast — the Emil standards) · ⑥ depth links out (SHOWCASE/PLAIN-ENGLISH) rather than crowding the narrative.

## Verification (end-to-end)
- Slice 0: APPROVED verdict on record. B: verify 961+7 · legacy 306+5 · evals/packs · explicit e2e · zero-brand grep · Codex@max record · post-deploy smoke. C2: Fable-equivalence + Emil standards · manual honesty sweep · external-request grep · Codex@max record. D: VET record + neutralization note. E: exit codes + clean tree.
- Wrap: state docs synced (PROJECT_STATE/CURRENT_TASK/HANDOFF/task-log) · paste-ready resume prompt · owner-unknowns surfaced · cost/effort report per Law 8.

---

## EVALUATION VERDICTS (2026-07-09 — both new-doctrine seats ran; NOT yet reconciled; reconcile at next session BEFORE any execution)

### Seat 1 — Codex GPT-5.6 flagship (gpt-5.6-terra @ max, read-only): **VERDICT: REVISE** (5 P1 · 13 P2 · 2 P3)
Full structured findings preserved at /tmp/codex-planv2-verdict-curbside-commons.md (raw stream: /tmp/codex-planv2-stream-curbside-commons.jsonl) — copy into docs/reviews/ at next session. Headlines:
- **AP-01 P1 (public honesty):** the de-brand alone is not a truthful redeploy — README.md:42 still says 947+6; PLAIN-ENGLISH.md:7-11 says "classifier deferred / no agents / no sends"; PUBLICATION.md:31-46 says 20/21-deferred + 743 tests; WHY.md:5 says deploy pending — all contradicted by current records (SHOWCASE.md:35,56-59: earned agent labels, 21/21, one recorded send). Fix: a public-doc reconciliation slice BEFORE Slice B.
- **AP-02 P1:** app/layout.tsx:55-65 footer claims "not … real sends" — now false (one owner-armed send exists). Scope the claim to the web replay.
- **AP-03 P1 (frozen contract):** docs/plan-whole-site-redesign.md:18-24 byte-freezes the SIMULATED banner + footer; Slice B edits both → needs a recorded decision reversal + a semantic disclosure contract + parity test.
- **AP-04 P1 (deploy semantics):** `--branch main` on a direct-upload Pages project may create a PREVIEW, not production; no wrangler config/dependency in package.json. Preflight the project, deploy preview first, confirm production semantics, record wrangler version.
- **AP-05 P1 (design governance):** the Oxblood spec bans gradients/glow/glass/numbered-markers/large radii; the owner's standing rules are "no dark background" + "no grid lines" — the hybrid violates several. Fix: record the sample as a non-canonical exploratory mockup with enumerated exceptions.
- **P2 highlights:** AP-06 TESS is retired (use current USPTO Trademark Search wording; S-11 "final" = non-commercial display brand, no clearance claimed) · AP-07 route tags missing on slices 0/A/B/D/E + RULES §9 doc-closure steps unscheduled · AP-08 grep misses OpenAI/Stripe/Google/DataSF + `Uber&nbsp;Eats` encoding + the three parent mockups' disclaimers; need a tracked inventory + out/ scan · AP-09 "Commerce Truth Audit" still baked in render-text.ts:47 + delivery templates + their goldens — decide historical-vs-current, versioned regen if current · AP-10 single-source the banner or add rendered-text parity; mockup claim test · AP-11 protected-path proof = empty git diff on legacy/** + evals/** at slice end, not test-greens · AP-12 build once, hash out/, e2e against the exact artifact (playwright may reuse a stale dev server) · AP-13 route manifest = 28 intended routes (30 files incl. 404.html/_not-found.html) + 404 probe + rollback path · AP-14 newer mockup precedent = ZERO external requests/system fonts (ultra-modern README) — Google Fonts needs a recorded exception or self-hosting · AP-15 parent anchor-ids are NOT compatible (#main vs #top vs #verify) — define a canonical fragment matrix + a11y/no-JS gates · AP-16 receipts block must add evals/delivery + prerequisites + expected exit-1 semantics + sanctioned label wording · AP-17 SOURCE-INTAKE ORDER: vet/pin emilkowalski/skills (mutable main, no releases, scripted greeting, MIT notice retention) BEFORE C1 consumes it — move D's vet ahead of C1 or use a repo-native motion table · AP-18 the live design sweep needs a dated source matrix (adopted/rejected + licenses).
- **P3:** AP-19 attribution precision (swiss 23px is .lede not body; premium easing is global) · AP-20 the dual-flagship advisory should run BEFORE the production deploy or be dropped from the release path.

### Seat 2 — fresh-context frontier acceptance-gate (Fable, read-only): **SHIP conditional** (1 P2 · 5 P3)
- P2: fork-2 vs edit-set contradiction on DATED FACTUAL market claims (README.md:59, WHY.md:11/:15, PUBLICATION.md:15) — add a third class: genericize prose, retain the named citation in research digests. (Overlaps Codex AP-01 territory.)
- P3s: Slice-B route tag should be @xhigh + anti-slop pass named · state the banner-parity invariant (copy.ts:48-52 ↔ ReportView.tsx:113-118, differ only by &nbsp;) · Slice-D needs a dedicated Codex leg on vendored content · Slice-E artifact unnamed · genericized disclaimers acceptably weaker (recorded, revertible).
- It verified ALL 10 file:line claims true, the site edit set complete for app/+components/, no golden regen needed, e2e genuinely outside verify, all demo commands real.

### Reconciliation status: NOT STARTED (owner ordered wrap). Next session: reconcile primary-model-final (refute-or-fix each AP/P finding), produce plan v3, re-submit to the SAME Codex loop until APPROVED, then execute. The overlap between the two seats (stale-docs honesty, banner freeze, route tags, source-intake order) is high-confidence signal — treat those as accepted-fix candidates first.
