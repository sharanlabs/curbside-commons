# Decision Log

Running log of scope and architecture decisions. One line per decision; link to the ADR or doc that holds the detail. Major architecture decisions also get an ADR in `docs/decisions/`.

Format: `[date] | decision | status | rationale (short) | reference`

| Date | Decision | Status | Rationale | Reference |
| --- | --- | --- | --- | --- |





















## How to add a decision

1. Append a row above with the date, the decision, status (Proposed / Accepted / Superseded), a one-line rationale, and a reference.
2. If it changes architecture materially, write or update an ADR in `docs/decisions/` and link it.
3. If it changes the active task, update `CURRENT_TASK.md`.
| 2026-07-09 (recorded 2026-07-10) | **FULL DELEGATION — the owner's standing word for the post-completion workstream (verbatim, owed since 2026-07-09):** *"You the core intent, nuances, requirements which i had given, design, language, flow, all other aspects. Use fable as your judgement feel free to add your suggestions, creativity if you had given full freedom to demostrate this project as best for free do it. if any corrections or anything is needed i will do it in the end. So ball is your court, take it forward as you like no bars."* Scope: per-slice commits+pushes authorized within the approved plan; hard stops unchanged (no new live-AI arming · no classifier re-run · irreversible/external acts still per recorded word). | Accepted (owner, 2026-07-09) | Delegation covers execution of owner-approved plan items; menu selections and hard-stop acts remain the owner's | HANDOFF 2026-07-09/10 top block; this row |
| 2026-07-21 | **Session 31 (Opus 4.8 @ max, held to the Fable-equivalence bar per the owner's standing doctrine; owner: "the whole work is done on sonnet model … fix it using fable as the judgement" + "go through all prompts review, rework, reverify and fix it" + "ensure opus does to our fable level").** A whole-project capability review this session (project-advisor + guidelines-monitor + utilization-coordinator + blindspot-scout + security-specialist passes) surfaced a **CRITICAL live MCP path-traversal class that the session-30 build and an earlier fix both missed**, plus two lower findings. All confirmed against real code by the primary seat (not taken on the subagents' word) and fixed: **(1 CRITICAL)** `check_conformance`'s caller-supplied `schemaDir` was passed raw (right beside the just-guarded `docPath`) into `walkJson`'s recursive `readdirSync`/`readFileSync` — arbitrary directory enumeration + arbitrary JSON read + DoS, reachable by any MCP client → now routed through `resolveInAllowedRoot` (repo-root/tmp containment, realpath-canonical) with a defense-in-depth `MAX_SCHEMA_DIR_DEPTH=8` cap added to `walkJson` itself; **red-green proven by the primary seat** (reverted guard → payload reached `readdirSync('/etc/passwd')` → ENOTDIR, no guard phrase; restored → 14/14 containment cases green). **A SECOND, subtler variant of the same class — surfaced by the sol cross-model review mid-pass, then confirmed + red-green-proven by the primary seat:** the boundary guard realpath-contains only the TOP-LEVEL `schemaDir`, but `walkJson` used `statSync` (follows symlinks), so a symlink planted at a DESCENDANT inside an allowed root (notably the shared OS temp dir) could redirect the walk back OUT of containment — the depth cap did NOT catch this. Fixed: `walkJson` now uses `lstatSync` and SKIPS symlinks (behavior-preserving — the pinned corpus is committed real files); locked by a new `evals/packs/conformance-walk-containment.test.ts` (red-green: old `statSync` collected a symlinked `secret.json`; `lstat` version does not). Independent completeness sweep by the primary seat: all 6 caller path params across the 4 file-path tools (`docPath`+`schemaDir`, two `statementPath`, `feedPath`+`catalogPath`) now guarded; the other 3 tools take no path param — **zero unguarded file-path parameters remain on the MCP surface**. **(2 HIGH)** the MCP server returned verbatim-quoted audited text (labels/category names) to a calling LLM with no data-vs-instruction signal → added the injection-hygiene convention to `SERVER_INSTRUCTIONS` (the spec-sanctioned channel; fencing every report rejected as disproportionate + human-readability-breaking) + a tested assertion in `mcp-conformance.test.ts`; transcript golden regenerated through its real generator. **(3 MED)** the new injection-resistance gold tested only the `label` channel → extended with 2 `declaredCategory`-channel pairs (the second untrusted string reaching the prompt). Also: non-breaking `npm audit fix` closed 2 dev-only ReDoS highs (`brace-expansion`, `js-yaml`); the 5 remaining `--force`-only advisories (Next/postcss, ai-sdk, adm-zip — all dev-only or behind the gated live-AI flag) deliberately NOT forced (surfaced for owner sign-off, not auto-bumped). Gate: `npm run verify` exit 0, **1283 passed / 8 skipped** (post-dep-fix re-verified identical). Sol cross-model adversarial review dispatched (xhigh, read-only) — verdict adjudicated in the next row. | Accepted | Primary-seat red-green + completeness sweep; `/tmp/fable-verify-depfix.log`; new capability KBs `~/claude-os/knowledge/security_kb` + `evals_kb/rag-evaluation.md` | this row; sol verdict next |
| 2026-07-21 | **Session 31 — sol cross-model security review adjudicated primary-model-final (VERDICT: REVISE, 7 findings; adversarial input, not blind-obeyed).** sol@xhigh (read-only) independently reached the same CRITICAL as the primary seat (the `schemaDir` descendant-symlink escape) plus 6 more; each disposed by the primary seat: **#1 CRITICAL (symlink descendant)** → already fixed (lstat+skip, red-green). **#2 HIGH (walk unbounded by breadth; `.json` FIFO can block)** → FIFO half already closed by the `isFile()` check; breadth-DoS (point `schemaDir` at `node_modules`/repo root) fixed with a `MAX_SCHEMA_DIR_ENTRIES=10_000` cap in `walkJson`. **#6 MED (injection gold missed the `siblingDeclaredCategories` channel + D4 rationale check too narrow)** → added a sibling-channel pair (6th) + forbade "do not flag" in D4 + disclosed the substring-vs-semantic limit honestly in the gold header. **#7 LOW (path-test matrix missed two `check_feed` combos)** → completed the matrix (feedPath-absolute, catalogPath-traversal); descendant-symlink already covered by the new containment test. **SURFACED for owner sign-off, NOT silently changed (API/architecture):** remove caller-settable `schemaDir` from the `check_conformance` MCP input schema entirely (no MCP caller uses it — the strongest fix, kills #1–#4 at the surface); replace the broad `os.tmpdir()` allowed-root with a dedicated `0700` scratch subtree (#3); anchor `DEFAULT_UCP_SCHEMA_DIR` to a module-absolute path so a non-repo-cwd server launch is deterministic (#4). **Accepted residual (documented — the irreducible floor of a caller-directed walk):** TOCTOU between realpath-check and read (#5), and a hard link inside an allowed root to an out-of-tree same-filesystem file (sol run-2 lead — realpath cannot distinguish a hard link from a real path). BOTH require an attacker to already hold filesystem WRITE access to an allowed root — in this stdio-local single-user threat model that attacker can already plant arbitrary `.json` content directly, so neither grants materially new capability; and BOTH are fully eliminated by the surfaced architectural fix (remove/restrict caller-directed `schemaDir`), not by more inline hardening (rejecting `st_nlink>1` is fragile + false-positives; open-fd-then-fstat is a disproportionate refactor). Per the lessons-store "recognize the irreducible floor and stop, don't chase variations" rule: hardened everything proportionate, escalated the surface-elimination to the owner, stopped. sol also independently CONFIRMED CLEAN: all 6 direct path params guarded; non-racing symlink escapes caught (existing + nearest-ancestor); depth cap off-by-one correct (corpus = 78 files, depth ≤ 2); the test's load-bearing "escapes the allowed root" assertion is real; SERVER_INSTRUCTIONS injection-hygiene wording correct. Gate after the adjudicated fixes: `npm run verify` exit 0, **1287 passed / 8 skipped**. sol run raw: first pass completed a full REVISE verdict; the second (confirmatory) pass COMPLETED (also REVISE) — it independently RE-CONFIRMED all 6 params guarded + the symlink escape closed + the test assertions load-bearing, re-raised only the already-adjudicated residuals (TOCTOU / hard-link / broad-temp-trust), and surfaced ONE new item: a **case-sensitivity finding — VERIFIED real by a direct probe** (an alternately-cased absolute path to a real in-repo file is refused on this case-insensitive host). sol's recommended fix ("make containment case-insensitive") was **REJECTED primary-model-final as a WORSE (Linux) security regression**: on a case-sensitive FS `/Foo ≠ /foo`, so a case-insensitive compare would UNDER-reject (accept an out-of-root path differing only in case). The current case-sensitive compare is the correct secure choice on BOTH platforms — its only effect is a FAIL-CLOSED over-rejection of alt-cased input on macOS, which no legitimate caller hits (real callers pass canonical paths, proven by the green legit-path tests); documented as deliberate in `paths.ts` `withinAllowedRoot`. This is the "never blind-obey a cross-model verdict" rule in action — sol found a real behavior but proposed a fix that would open a hole. codex env warnings (hooks-config parse + skills-budget) surfaced verbatim, non-fatal. | Accepted (primary-model-final) | `/tmp/fable-verify-sol-adjudicated.log`; sol events `scratchpad/sol-security-review-events.jsonl` | RULES.md §6 no-silent-drift for the surfaced items |
| 2026-07-21 | **Owner word: "ok complete the decisions." — the flagged owner-decisions resolved (RULES §6 sign-off given by that word for the API-affecting one).** DONE: **(1)** hardening (a) EXECUTED — caller-settable `schemaDir` REMOVED from the `check_conformance` MCP input schema + interface + tool call (no MCP caller used it; the CLI keeps its own `schemaDir` for trusted local use); the whole caller-directed directory-walk/enumeration/DoS surface is gone from the MCP tool. Golden regenerated; new test asserts a sent `schemaDir` is now refused as an unknown property (ajv `additionalProperties:false`). **(2)** Dependency decision: the 5 `--force`-only advisories (Next/postcss, `@ai-sdk/provider-utils`, `adm-zip`) are DECIDED — NOT force-bumped (all dev-only or behind the gated live-AI flag; forcing would break Next/the AI SDK for no runtime-security gain; watch for non-breaking upstream fixes). The 2 non-breaking dev-only ReDoS highs were already fixed earlier this session. **(3)** RULES.md + CLAUDE.md staleness: an additive **project-identity orientation note** added to both (read at every startup) — names Curbside Commons as the current product, points to the README lineage, and clarifies the Slack/Supabase/n8n/Resend stack is the enterprise-expansion PATH not the shipped build; **no rule altered** (the "staleness" was partly intended roadmap content, so a clarifying note beats a rewrite). **DEFERRED with reasoning (documented as focused follow-ups, not dropped):** hardening (b) dedicated-`0700`-scratch-instead-of-broad-`os.tmpdir` and (c) module-anchor `DEFAULT_UCP_SCHEMA_DIR` — both pure robustness/defense-in-depth now that the schemaDir walk surface is gone; (c) touches a shared constant used by scripts and (b) reworks the test-fixture strategy, so both deserve a focused pass over being rushed into a 100+-file commit. State-doc bloat (HANDOFF/PROJECT_STATE/CURRENT_TASK too large for the startup contract) — DEFERRED: a restructure of the continuity mechanism is too risky to bundle into this commit; owed as its own careful pass. **HELD for the owner (genuinely owner-only or the hard gate):** the public Vercel deploy + its prerequisite picks (domain, OG card, robots posture, retiring the stale Cloudflare deploy) — a domain cannot be invented and deploy is RULES §-gated; teed up via a structured ask. Gate after all edits: `npm run verify` exit 0 (count in the next state sync). | Accepted | this row; RULES.md + CLAUDE.md identity notes; `check_conformance` schema diff | owner "complete the decisions" |
| 2026-07-21 | **Owner word: "ensure the projects goals, what we have built, narrative are aligned" → "just replace or delete the old which does not align … at any level."** A read-only alignment map (general-purpose agent) confirmed the goal/built/narrative are **fundamentally aligned in every reader-facing place** (all 6 site pages, README + its Lineage section, PLAIN-ENGLISH/SHOWCASE/WHY/HOW-THIS-WAS-BUILT, current state docs) — the divergence was an isolated **stale old-product doc layer**. Per the owner's replace-or-delete directive: **DELETED six standalone docs that described the archived ActivationOps merchant-onboarding product in present tense as if current** (`docs/product-brief.md`, `project-narrative.md`, `CASE-STUDY.md`, `INTERVIEW-WALKTHROUGH.md`, `roadmap.md`, `ENTERPRISE-READINESS.md` — each confirmed old-vocab-only, 0 current-vocab hits; four also inverted the identity by calling "Curbside Commons" a *fictional marketplace*). Their purpose is already served by the current docs, so deletion (not rewrite) was the clean alignment. **Fixed the two live pointers that re-seed the confusion:** CLAUDE.md's "Reference reading" list (was pointing every new session at the old-product docs) now points at README/PLAIN-ENGLISH/SHOWCASE/HOW-THIS-WAS-BUILT + flags the dated planning artifacts as history-not-reference; RULES.md §8's "Public docs focus on…" line rewritten from the onboarding/blocker-diagnosis framing to the feed-vs-record/fee-audit product. **KEPT as honest provenance (NOT deleted):** the append-only logs (decision-log, task-log, implementation-journal — RULES §1 source of truth; deleting them would erase the audit trail incl. this session) and the DATED historical planning artifacts (data-audit, plan-reconciliation, ADR-001, the v1/t002/phase3 slice-plans — self-evidently dated, referenced by the logs, no longer pointed-to as current). Surfaced to the owner: whether to also purge that dated historical-plan layer (would orphan log references) — awaiting word. | Accepted | alignment map (read-only); `git status` (6 deletions); CLAUDE.md + RULES.md §8 diffs | owner "replace or delete the old" |
| 2026-07-21 | **Owner word: "I need a live integration as well … i have already created slack webhook added the key." → A FRESH L-2 LIVE SLACK SEND WAS ARMED AND DELIVERED (HTTP 200 OK).** The owner created their own Slack incoming webhook, placed it in the gitignored `.env` (`SLACK_WEBHOOK_URL`), and armed the send. Ran the existing hardened one-shot (`scripts-ts/l2-slack-one-shot.mts`) — the real engine's `audit_statement` over the drifted fee fixture → Block Kit (SIMULATED-banner-led) → single POST to `hooks.slack.com` → **200 OK, DELIVERED**; payload sha256 `468349c8…2eb2f38`; redacted run record `docs/reviews/l2-slack-one-shot-2026-07-21T18-19-11-844Z.md`. All 8 `plan-a3-delivery-safety.md` controls held (owner word per send · owner's own channel only · one-shot no-retry · mandatory banner · secret env-only never logged/committed · redacted record · free-tier webhook · failure-reported-as-is). This is the SECOND owner-armed live send in project history (first: 2026-07-09). **Harness raw:** the send command was CLASSIFIER-DENIED once (auto-mode flagged `node --env-file=.env … POST` as exfiltration-shaped — the known owner-worded-external-action friction); the owner's "resume" re-ran it and it delivered — surfaced, not worked around. The record's arming line was corrected (the script hard-codes the original 2026-07-08 arming; follow-up noted to make it parameter-driven). This is a transient demonstration, not standing infrastructure (prototype-not-service). | Accepted | run record; console `L-2 DELIVERED — HTTP 200 OK` | RULES §3/§11; plan-a3-delivery-safety.md |
| 2026-07-21 | **Owner word: "deploy" → THE SITE IS DEPLOYED AND LIVE (Cloudflare Pages, plan-deploy.md recommended pick).** `npx wrangler pages deploy out/ --project-name=curbside-commons` used the owner's ambient Cloudflare auth (agent holds no credentials — RULES §11 satisfied by the owner's own session): **571 files uploaded, 60 pages, "Deployment complete"** → **https://d6674da6.curbside-commons.pages.dev** (production alias **https://curbside-commons.pages.dev**). Deploying to the SAME `curbside-commons` project **retired the stale 8-tab dashboard** that had been flagged (the production alias now serves the new site). Post-deploy smoke (plan-deploy.md §5.5) PASSED: all 7 routes 200 (`/` `/report` `/fees` `/playground` `/proof` `/docs` `/legacy`); homepage carries **zero** banned real-platform claims; `/docs` "what is real" page serves — the amended RULES §4 honesty posture (C10 gate + footer-linked /docs, not per-surface labels) holds on the live artifact. `out/` was built from committed `617ca43` (the 2 later doc commits are non-site). This is a **static artifact showcase, not an operated service** (prototype-not-service; no uptime/ops posture). NEXT owner-facing deliverable: the end-to-end test-run guide ([[e2e-test-run-guide-deliverable]]) now that a public URL exists. | Accepted | `wrangler` deploy output; curl smoke (7×200 + honesty) | plan-deploy.md; RULES §11 |
| 2026-07-22 | **Session 32 structured ask — the owner worded BOTH pending gates: "Fire it now" (the armed L-2 Resend email one-shot) + "Commit now" (the three in-flight files + state sync).** The send was attempted on that word: **NOT ARMED — raw outcome `L-2 email: RESEND_API_KEY is not set` exit 2**; a presence-only boolean probe (no values read or printed; Law 11 held — the agent never opens `.env`) confirmed `RESEND_API_KEY`/`RESEND_TO`/`RESEND_FROM` all ABSENT while `SLACK_WEBHOOK_URL` is present, so the 07-21 "resend i have already added" word did not land in `.env` under the expected names. NOT retried, NOT worked around (control #8 / no-silent-drift): the fix is an owner act — add `RESEND_API_KEY` + `RESEND_TO` (a recipient the owner owns) to the gitignored `.env`, then one fresh word re-fires the one-shot. The commit word was executed (this tree). Session 32 also: verify re-proven exit 0 (1286+8) on the committed tree; live 7-route smoke 200s; the how-to-test guide's paste-in statement FIXED red-green against the real engine (missing meta fields + an averaging-clause-impossible delivery expectation → redesigned; exactly the 5 advertised violations proven to fire: a-2 · c-1 · d-1 ×2 · d-4); tech-stack test plan email lane [BUILD]→[LIVE]; the one-shot's hard-coded arming line made parameter-driven before first use | Accepted | Raw send output verbatim; boolean env probe; engine run of the corrected guide JSON (5 violations); scratchpad/verify-s32.log | RULES §3/§11/§12; docs/plan-a3-delivery-safety.md |
| 2026-07-22 | **L-2 LIVE EMAIL SEND (Resend) DELIVERED — HTTP 200 OK; the THIRD owner-armed live send in project history and the FIRST email one (1: Slack 2026-07-09 · 2: Slack 2026-07-21 · 3: email 2026-07-22).** Owner words verbatim, in sequence: "to sharank98@gmail.com" (recipient designation) → "i think i have resend api key please check it" (name-only env scan: NONE — `.env` untouched since Jul 9, so the 07-21 "already added" had never landed) → "open env file here i will paste it" (opened in VS Code — contents never entered the session) → "add the provision to add the key" (append-only provision of `RESEND_API_KEY=` empty slot + `RESEND_TO=sharank98@gmail.com` + a matching documented block in the tracked `.env.example`; newline-guarded; nothing read; boolean probes only) → "done saving check it out" (probe: ARMED) → fired `scripts-ts/l2-resend-one-shot.mts` on the session's standing "Fire it now" word → **DELIVERED HTTP 200**, Resend id `d8a1225d-…`, payload sha256 `dd4c5ec0…46b4a0` (audit_statement over the drifted fixture; SIMULATED banner led body + subject), redacted record `docs/reviews/l2-resend-one-shot-2026-07-22T16-55-41-613Z.md`. All 8 `plan-a3-delivery-safety.md` controls held; 0 retries; secrets never read/printed/logged (Law 11 substance: the key's value never entered model context — presence checked as booleans only). The email lane is now [GREEN + LIVE-DONE]; transient demonstration, not standing infrastructure (prototype-not-service) | Accepted | Console `L-2 EMAIL DELIVERED — HTTP 200 OK`; run record; boolean probes | RULES §3/§11/§12; docs/plan-a3-delivery-safety.md |
| 2026-07-22 | **Owner standing directive (session 32, verbatim): "ensure it is clean not cluttered, looaded too much of information. so independently as subject expert decide what how why would in there report. it applies to whole as a project."** Read (intent): ① human-facing outputs must be CLEAN, not information-loaded — editorial restraint over completeness; ② the seat acts as the SUBJECT EXPERT and independently decides WHAT a report carries, HOW it presents, and WHY — curation is the expert's job, not a question back to the owner; ③ this binds PROJECT-WIDE (all human-facing surfaces going forward), not just the email under redesign. Applied immediately to the email-redesign brief: the email is a NOTIFICATION surface, not the report itself — verdict + scannable findings + quiet evidence pointer; report-grade detail (claim ids, per-row verdict words, tool/runtime meta) belongs in report.json//docs, not the body. Honesty carriers (SIMULATED banner, one footer line) are floors and stay. Recorded to auto-memory (clean-not-cluttered-expert-curates) | Accepted | Owner words verbatim mid-session 32 | this row; email redesign packet |
| 2026-07-22 | **Owner refinement (session 32, verbatim): "also the content do a research and personalize to our context and project as of today's date."** Read: the email's CONTENT (copy, not just visuals) is also research-grounded — current transactional/compliance-email copy conventions (subject/preheader/verdict phrasing/notification-vs-report split) added as section 6 of the running quarantined sweep — and PERSONALIZED to this project's own established voice (the built site's vocabulary: feed-vs-record, held/receipts, "the engine decides, humans approve" — sourced from the repo, which the no-memory rule permits: it bars untracked model memory for practice/design claims, not reading our own artifacts) and date-anchored to today (2026-07-22) | Accepted | Owner words verbatim mid-session 32 | redesign packet; research digest §6 |
| 2026-07-22 | **L-2 v2 EMAIL SEND DELIVERED — HTTP 200 OK (the FOURTH owner-armed live send; second email).** Owner structured-ask words: "Send it" + "Commit + push". The REDESIGNED one-shot fired (subject `[SIMULATED] Fee audit: 5 violations — 2026-06` 46ch · preheader 96ch · v2 HTML body from the golden-tested builder · curated plain-text half · report.json attachment); payload sha256 `938104d0e213572b…`; redacted record `docs/reviews/l2-resend-one-shot-2026-07-22T18-48-35-336Z.md`; all 8 controls held, 0 retries. The v2 redesign chain behind it: live digest (`docs/research/research-email-design-2026-07-22.md`) → Opus frontend-specialist build → Fable independent evaluation (delivery+C10 180/180 re-run · golden re-derived byte-identical · purity 0/0 · both verdict-state screenshots) → full verify exit 0 **1302+8**. Cap deviation 20→6 adjudicated ACCEPTED (curation directive outranks the packet constant). NEXT round opened by owner word (verbatim): "let us enhance using claude design in web of claude use it develop and for content use Sol" → design leg = Claude Design web via the standing DesignSync route (Fable-authored brief; owner runs it; pull + gate on return) · content leg = sol language-only pass (Codex excluded from design; primary-model-final adjudication) | Accepted | Console DELIVERED line; run record; verify-s32c.log | plan-a3-delivery-safety.md; memory claude-design-standing-route |
| 2026-07-22 | **Owner word (verbatim): "let us enhance using claude design in web of claude use it develop and for content use Sol" → BOTH LEGS EXECUTED.** ① sol content pass (codex-guarded, read-only, @high; first probe failed raw "Not inside a trusted directory" → mechanical `--skip-git-repo-check` fix named by the error itself, owner "resume" ×2; SEAT_OK; non-fatal models-cache stderr noted) over 13 email-chrome strings (engine plainLines out of scope, golden-bound) → **adjudicated primary-model-final: 5 KEEP · 7 ACCEPT** (both preheaders · eyebrow "Simulated fee audit" · calmer PASS empty-state · two-sentence panel · de-jargoned footer reason ("armed"/"one-shot" = internal vocabulary, not recipient language) · scannable footer honesty) **· 1 REJECT-MODIFY** (sol's rewrite of "the engine decides, humans approve" would water down the deterministic-verdict claim — resolved by CUTTING the line from the text half instead, mirroring the curated HTML; the mantra lives on the site/docs) **· 2 catches accepted as code fixes** (computed plurals replacing every "(s)" form incl. one bound test expectation rewritten red-green; "sent" label on the meta date). Golden tooth BIT on the changed bytes (1 failed) → regenerated via the real builder → **delivery+C10 180/180 · tsc 0**. ② Claude Design leg: project `8bed5e72` verified (DESIGN_SYSTEM, canEdit) → pushed `email/email-v2-base-2026-07-22.html` (the current email, content-updated, @dsCard marker) + `brief/enhance-email-2026-07-22.html` (Fable-authored working prompt: design-only round, content FROZEN, email-safe constraints, honesty/lamp floors, known defect classes, return path `email/email-v3-claude-design-2026-07-22.html`) — written:2; manifest handled by the @dsCard self-check (supersedes the manual-manifest lesson). Chrome hands-off retried once — extension not connected (raw) → one-paste fallback. Return loop: owner runs the prompt in claude.ai/design → says done → context-clean pull → email gate battery → adjudicate → owner adopt word → red-green translation into the builder | Accepted | sol events `scratchpad/sol-email-content-out.json`; suite tails; DesignSync written:2 | memory claude-design-standing-route; RULES §2 roles |
| 2026-07-24 | **M2 semantic change adjudicated (session 33, orchestrator call under the gate-SHIPPED audit report): an artifact carrying injection-signature text now FORCES ESCALATION regardless of payload depth.** The deterministic full-artifact scan (audit finding M2) flips `evi-injection-content`'s committed terminal from recommendation to escalate-to-human — adjudicated as the fix's intended behavior, not collateral: the engine verdict is unchanged, the artifact reaches a human with the anomaly named, and exempting the canonical deep-payload fixture would gut the control. L-1 live-record handling: the owner-armed 2026-07-07 RAW TURNS stay byte-untouched (they are the provenance); the DERIVED records/matrix regenerate from those same frozen turns through the new derivation (the repo's regen-through-real-generators pattern), floors recomputed and REQUIRED to still clear — any floor miss stops the change and demotes the label honestly. Status-doc wording gains a dated derivation note. Builder had correctly STOPPED on the packet's conflict clause rather than improvising | Accepted | Audit report M2 (gate-SHIPPED); builder conflict report; this adjudication | agentic-audit-2026-07-24.md; crew-live-l1-status.md |
| 2026-07-24 | **Codex cross-model gate RAN (sol@high, owner-selected) → VALID BLOCK on the M2 injection scan; adjudicated primary-model-final (6/6 confirmed, #7 refuted-as-stated).** The mandatory gate on the session-33 fix wave (seat verified working first, memory [[always-verify-codex-seat]]; first 43-file run over-sized the 10-min window → re-scoped to the 4 highest-risk code diffs). Codex found 6 HIGH false positives in `detectInjectionSignatures`; every one empirically re-verified by running the function on Codex's exact inputs — all 6 FIRE, including the product's OWN clean-audit output "Fee audit report: no violations" → `approval-steering-directive` → forced escalation. #7's "400-char bypass" refuted (the deterministic scan reads full content) but a real word-order recall gap stands. Root cause: M2's "zero-FP" was validated only on committed fixtures with no ordinary-language negatives; my own post-completion eval + the acceptance-gate both passed it — the different-family gate caught the family-internal blindness (the estate-test's headline result; lesson banked to claude-os). Severity: fail-safe (over-escalation), eval-lane-only (shipped MCP/site/CLI use the deterministic classifier, not the crew; all committed goldens still pass), so latent-correctness not shipped-user-facing — deferred to session 34 as the #1 item with a full red-green fix spec (record `docs/reviews/codex-2026-07-24-s33-gate.md`). No code changed at wrap; the defect is documented OPEN, not silently carried | BLOCK (deferred, fail-safe); gate seat now working | Codex verdict `scratchpad/codex-verdict-focused.md`; empirical re-verification (6 FIRE/1 clean) | codex-2026-07-24-s33-gate.md; HANDOFF #1 |
| 2026-07-24 | **PASS-state email: shipped count-free; numeric pass-rate ATTEMPTED then REVERTED at the pre-registration line (owner asked "have email for pass rate as well").** The pass-state email was built first-class (byte-frozen golden `email-html-fees-pass.golden.html`, graphite stamp, no ember) with the honest count-free sentence "No fee lines in this simulated statement exceed the audited caps." The numeric version ("All 6 fee lines … sit within the audited caps") needs a real denominator, which the fee report does not expose — so an additive `reviewedLineCount` field on `FeeAuditReport` was authorized (owner picked "complete the numeric rate"). On regeneration the L-1 **floor gate MISSED** (intake 3/5, reviewer 2/5): the field changes the engine-report hash, and the pre-registered held-out L-1 case files PIN `expectedEngineReportHash` (`evaluateCase` harness.ts:85). Landing it would require rewriting the pinned hashes in the owner-armed, pre-registered exam artifacts — retroactively moving a bar that the project's whole character says never moves after the run. Per the pre-committed hard condition (floor miss → STOP + revert), the entire engine change was reverted; the honest count-free PASS email ships. Estate-test lesson: a report field consumed by a hashed, pre-registered eval cannot be added without re-arming that eval — enrichment of a hashed report is a pre-registration event, not a cosmetic. Full verify green 1354+8 after revert | Reverted (feature); Accepted (count-free PASS email) | floor-gate output; harness.ts:85; owner structured ask | email-html-fees-pass.golden.html; docs/plan-l1-crew-live.md |
| 2026-07-25 | **M2 injection-scan FALSE POSITIVES FIXED red-green — and the fix's OWN first pass was caught by a second, self-directed adversarial sweep (session 34, /claude-os live estate-test run).** Discharges the 2026-07-24 Codex VALID BLOCK. All 6 FPs re-reproduced locally before any edit (6/6 FIRE) rather than accepted on the record. Each signature tightened to require injection CONTEXT over keywords: `system prompt/instruction` excluded when attributive; AI-addressing requires a reading-participle or message-introducing colon (audience metadata is not addressing); `ignore-prior` keys on second-person possessive OR AI-governance vocabulary OR an UNQUALIFIED "previous rules"; `role-reassignment` drops the bare `not` alternation and requires a real role/mode object; tool-invocation binds to REGISTRY NAMES only; approval-steering requires a bare imperative. Recall gap #7 also closed (both word orders). **Two findings the process produced that the gate spec did not contain:** (1) the gate's stated impact — "the product's OWN clean-audit output would force escalation" — is REFUTED as written: the shipped strings are "PASS (no violations)" / "No violations found", neither fires, and a 75-file harvest over every committed benign product-output corpus returns 0 FPs. The exposure is LATENT (one copy-edit to "report … no violations" would fire), so the new M2-SELF suite runs the REAL generators and pins the property. (2) The first tightening pass silently DROPPED a committed true positive (`ignore all previous rules`) — caught only by the byte-frozen `int-injection-steered` render golden, since the artifact still escalated via 3 other signatures and no security outcome changed; fixed by the qualification discriminator (unqualified "previous rules" fires, domain-qualified "prior routing rules" does not) and pinned as a minimal-pair test. **Then, refusing to trust the green suite:** an adversarial sweep over 12 benign strings absent from every test found 7 MORE FPs of one root cause the first pass missed — the signatures matched in any grammatical MOOD (attributive noun phrases, declarative narration, third-party objects, reported speech, tool-name-as-subject). All fixed; final sweep 0/12 FP and 0/8 recall loss, promoted into the committed M2-ADVERSARIAL suite. Generalized lesson: clearing a handed-down FP list is not fixing the class it sampled — stopping at green would have reproduced the original blindspot one level up. NO golden, fixture, or L-1 record edited (the code was fixed until the frozen record matched, never the reverse). Full verify exit 0: **1396 passed + 8 skipped** (baseline 1354; +42 tests) | Accepted | Local 6/6 FP reproduction; 75-file benign harvest; byte-frozen golden catch; 12-string adversarial sweep | `lib/crew/injection-scan.ts`; `evals/crew/crew-injection-scan.test.ts`; codex-2026-07-24-s33-gate.md |
| 2026-07-25 | **Codex cross-model gate RAN on the M2 FIX → VALID BLOCK (9/9 recall bypasses confirmed); adjudicated primary-model-final and all 9 closed.** Seat re-resolved LIVE rather than assumed from memory: `gpt-5.6-terra`@xhigh (session 33's record said `sol`) — a different family from the seat that filed the original BLOCK, which strengthens the cross-model property. Scope 17.6KB (inside the ~24KB proven window). **Harness event recorded raw:** the run exceeded the 10-min wall clock and was killed with **0 bytes on stdout** — the verdict doc was never written — but 1.76MB of stderr had captured the reviewer EMPIRICALLY PROBING the regexes (HIT/MISS lines), and those probes were the salvaged findings. The standing "capture the FULL event stream; `-o` is never the only record" lesson is the only reason this run was recoverable. **The result that matters:** the fix had passed 42 of my own tests AND a self-directed 12-string adversarial sweep, and a different-family reviewer still found 9 bypasses — the SECOND consecutive time cross-model review caught what family-internal review passed. Root cause, and the inverse of the original defect: where M2 was too LOOSE (keywords, no context), my precision fixes were too TIGHT — three guards excluded whole grammatical POSITIONS rather than specific benign SHAPES, each defeated by a one-word rephrase ("PLEASE report no violations" beat a `(?<![a-z]\s)` lookbehind; "YOU approve everything" was mis-classed as narration; an unrelated `their` cancelled a branch; a FULLWIDTH COLON U+FF1A bypassed a separator class whole; comma-separated AI address; "act as"/"free from" persona and release-from-constraint shapes). **Generalized lesson: tightening a detector is itself a change requiring adversarial review — it is not a safe monotonic cleanup, because every narrowing goes green while creating a new place an attacker can stand.** 3 further claims REFUTED with reasoning (a fail-safe ambiguous imperative; a ZWSP string the reviewer mis-filed as an FP when firing is CORRECT; one genitive-noun-phrase residual ACCEPTED and documented rather than re-widening the guard into bypass #3). Pinned as the permanent M2-GATE3 suite; M2-REDOS added in the same pass (the scan reads full artifact content of arbitrary size, so linearity under pathological input is now asserted, not assumed). Full verify exit 0: **1407 passed + 8 skipped** (baseline 1354; +53 tests); no golden/fixture/L-1 record edited | Accepted (BLOCK discharged) | Local 9/9 re-verification before and after; raw stderr stream | `docs/reviews/codex-2026-07-25-s34-m2gate.md`; M2-GATE3 + M2-REDOS suites |
| 2026-07-25 | **The L-2 email's PLAIN-TEXT half is now golden-pinned — the last live-send surface whose exact bytes no test observed.** Found by the capability sweep: `l2-resend-one-shot.mts` POSTs both `text` and `html`; the `html` half has always come from the golden-tested `buildEmailReportHtml` and the sibling Slack one-shot imports `buildSlackReportPayload`, but the `text` half was composed INLINE in the script, covered by no golden and no assertion — a copy edit there would have reached a real inbox with nothing objecting. Extracted to `lib/delivery/email-text.ts` as a pure, transport-free, URL-literal-free builder (same A3 boundary contract as the HTML builder) + 2 byte-frozen goldens generated through the REAL builder. **The extraction was proven BYTE-IDENTICAL to the prior inline composition (1519 bytes both ways on the drifted fixture) BEFORE the goldens were minted**, so the goldens freeze the SHIPPED wire format rather than a new one. What is deliberately NOT changed: the text half still diverges from the `.eml` builder's body — that mirror of the v5 HTML half is an owner directive; being UNPINNED was the defect, not the wording. 14 new tests incl. banner-leads-body (control #4), explicit-truncation-at-cap, refuses-non-decision-grade-canonical, honesty/RULES-§4 (no real-platform or brand claim), and the M2-SELF property (a live-send surface must not trip the injection tripwire). **Two self-inflicted defects caught and fixed in the same pass, both recorded rather than quietly patched:** (1) the extraction broke a real pre-existing SOURCE-TEXT guard (`l2-one-shot-guard.test.ts:96` pattern-matched `const bodyText = [ SIMULATED_BANNER,`) — a guard keyed to a code SHAPE rather than a PROPERTY silently mis-fires on a pure refactor, so it was re-pointed at "whatever builds the body, the banner leads it" and now also asserts inside the builder; (2) my own new test used `/\d+\s*(of|\/)\s*\d+/` body-wide to check for a fabricated denominator and matched the statutory citation "Local Law 79 of **2025**" — a context-free pattern false-positiving on legitimate text, the exact class this session spent its effort removing from the injection scan. Scoped to the verdict line. Also pinned: the PASS body's known-cosmetic extra blank line, documented as a deliberate visible decision so any future tidy-up is a reviewed golden diff, never a silent wire change. Unarmed run still exits clean (control #8). Full verify exit 0: **1421 passed + 8 skipped** (baseline 1354; +67) | Accepted | Byte-equality proof; unarmed-exit run; delivery suite 52/52 | `lib/delivery/email-text.ts`; `evals/delivery/email-text.test.ts`; 2 goldens |
| 2026-07-25 | **Capability sweep PARTIAL — 2 of 8 lanes survived a harness failure; results preserved rather than lost.** A 16-agent read-only fan-out was launched to audit every advertised capability; **14 of 16 agents died on the session usage limit** (~1.22M subagent tokens, 2 usable results), taking with them the CLI/site/MCP sweeps, the adversarial-verification stage, AND all three architecture-decision lanes — (b) report-hash, (a) E2/E4, (e) presentation — which therefore **were never run and remain open**. The estate's own standing "stagger background fan-outs" lesson was hit anyway; replacement heuristic recorded (≤5 agents/batch, read before launching the next). **The two surviving audits were substantive and lived only in a workflow journal no clone would carry, so they are written to `docs/reviews/capability-sweep-2026-07-25.md`.** Delivery lane: 10 claims, 9 HOLD / 1 BROKEN (the unpinned email text half — FIXED this session, see the row above) + 4 open findings (Slack one-shot transport-bearing with no unarmed-guard test AND a docstring falsely claiming Resend is "the ONLY transport-bearing code" — empirically confirmed the script DOES exit 2 unarmed, so an unguarded contract not a live defect · `buildEmailReportHtml` hardcodes fee-audit copy while accepting any decision-grade canonical, proven by rendering a real `check_feed` canonical, latent-not-shipped and explicitly NOT a §4 breach · the `.eml` truncation branch untested · the "no literal (s)" copy standard contradicted by the Slack lane, unenforced either way). Evals harness: **12 claims, 12 HOLD, 0 broken, no HIGH/MEDIUM** — pre-registration ordering git-provable in both exams, eval-locks recompute from per-item records, frozen fixtures genuinely re-run generators, the DEFERRED label stands unedited. Its verdict: "the advertised discipline is real and mechanically enforced, not narrated." The standout anti-theater evidence found was **E4, not the classifier**: a first scoring run was VOIDED for carrying 10 near-miss traps against its own registered ≥12 minimum, and the honest re-run on a fresh split produced a WORSE number (recall 0.6→0.55) that was KEPT and published. 3 LOW findings, all doc-fix: a dead runner's floor string advertises a strictly-beats conjunct its code omits (no outcome impact — the pin makes it unobservable — but a future copier would inherit it) · held-out L-1 exam INPUT fixtures were edited 14 days post-run under an owner-directed copy sweep and the pinned hashes re-derived (raw turns byte-untouched, hashes machine-re-derived so unforgeable, disclosed — but the "frozen after one scoring pass" property is slightly weaker than the narrative implies; note the project has since RAISED this bar, reverting a whole feature rather than re-pin) · the retry split's author-provenance caveat is disclosed in a status doc while README:63 advertises it unqualified (mechanical mitigation does real work: the baseline re-measured at 19/21, identical to the original pin) | Accepted (partial; unrun lanes OPEN) | Workflow journal recovery; per-finding agent evidence | `docs/reviews/capability-sweep-2026-07-25.md` |
| 2026-07-25 | **All 7 remaining capability-sweep findings FIXED, then cross-model-gated — and the gate caught a flaw in my own reasoning (3rd valid catch this session).** #2 the Slack one-shot was transport-bearing with ZERO committed assertions while the guard suite's docstring called Resend "the ONLY transport-bearing code" — docstring corrected + a full Slack mirror suite added (unarmed exit 2 · malformed URL · **non-Slack host** · bad path · single-fetch-lexically-after-last-guard · payload-built-by-the-golden-tested-builder). #3 `buildEmailReportHtml` rendered fee-audit copy for any decision-grade canonical — **contract NARROWED rather than parameterized**, on the reasoning that parameterizing keeps the failure mode open and merely relocates it to the caller. #4 the `.eml` truncation branch (cap 20, fixture yields 5) was untested — `EML_FINDINGS_CAP` promoted from bare literals + a real 26-finding truncation test. #5 the "no literal (s)" copy standard was named in one module and violated in another with no gate — **SCOPED to the email lane with a recorded revisit trigger** (the Slack "(s)" forms are frozen into goldens bound to a real owner-armed delivered send; regenerating them for a cosmetic preference would edit the byte record of an actual send) and made mechanical. #6 a FROZEN exam runner's floor string advertises a strictly-beats conjunct its code omits — **deliberately NOT "fixed"** (editing a frozen exam runner is the move this project refuses; `>= 20` is exactly `> 19` for integers and the runner is env-skipped) but commented so a future copier sees the trap, with a pointer to the retry runner that encodes it correctly. #7 the 2026-07-21 post-run L-1 fixture edit + hash re-derivation is now recorded in `docs/crew-live-l1-status.md` as an **amendment history / re-arming event**, with the three verified protections (raw turns byte-untouched · only the hash changed · hashes machine-re-derived so unforgeable) AND the honest residue stated. #8 the retry split's authorship-provenance caveat now rides the public claims in README + SHOWCASE instead of sitting only in a status doc. **THE GATE ON THIS BATCH (`gpt-5.6-terra`@high, 22.9KB, clean 2,936-byte verdict) returned 2 CONFIRMED findings, both re-verified by execution before fixing:** (a) MEDIUM — **#3 did not make the mislabel unrepresentable**: I gated on `meta.tool`, which is CALLER-SUPPLIED and forgeable, so a `check_feed` canonical passed with `{tool:"audit_statement"}` still rendered the fee copy. **My claim was stronger than my code** — the same shape as the injection-scan defect earlier in this session: *a check that trusts a label instead of the payload is one you can walk around.* Re-fixed to read the canonical's INTRINSIC shape (`classification`+`assumedPurchasePriceBase`+`verdictTally`, which a caller cannot fake), both mismatch directions pinned as `#3b`. (b) LOW — **#5's source-scan instrument was unsound**: the comment-stripper falsely passes `"/* (s) */"`, cannot see `"(" + "s)"`, and would falsely fail an internal error string. Rewritten to assert on RENDERED output across all three email surfaces at both plural arities, plus the goldens, plus a positive control. Gate dispositions accepted on the other 3 risks (no network path in the Slack tests — it EXECUTED the non-Slack-host case; the truncation payload is genuine not degenerate; "comment don't fix" on #6 is defensible). Full verify exit 0: **1434 passed + 8 skipped** | Accepted | Local reproduction of the forged-metadata bypass before and after; delivery suite 65/65 | `docs/reviews/codex-2026-07-25-s34-sweepfix-gate.md` |
| 2026-07-25 | **Lane (b) — the report-hash normalized projection: RECOMMEND NO, and fix the coupling elsewhere.** The question: should the crew hash a normalized PROJECTION so the fee report can be enriched without re-arming the owner-armed held-out exam? Read the mechanism (`orchestrator.ts:57` hashes the WHOLE canonical; `harness.ts:85` compares) and declined, on three grounds. (1) **It solves the wrong problem** — the exam pins the hash BECAUSE the hash is total; narrowing it trades a security property for an ergonomics one, and it is the same move as re-pinning wearing better clothes: it does not move the bar, it makes the bar smaller. This project reverted an entire feature on 2026-07-24 rather than re-pin. (2) **"Normalized projection" has no stable definition here, which is the tell** — building one means enumerating which fields the lock covers, and that list is a judgment made today about what a future bug or tampered artifact will target; it will be edited toward convenience every time it blocks something (`reviewedLineCount` is display-only until a UI derives a rate from it, at which point it is load-bearing and OUTSIDE the lock). A lock whose scope is a maintained list decays; a lock over the whole artifact cannot. (3) **The blocked case was a nice-to-have** — a numeric pass-rate in an email; the count-free wording ships and is honest, and a pre-registration that never blocks anything is not a pre-registration. **The real defect named, with two better remedies:** an exam of CREW behavior is coupled to the full serialization of an ENGINE artifact — fix by (i) versioning the report shape so an enrichment bumps `specVersion` and becomes an explicit, recorded re-arming event, or (ii) **deriving display-only values in the delivery builder** so the engine report never changes, which would have unblocked session 33's feature at zero cost to the lock and is the path the next attempt should reach for first | Recommend NO (no code change) | `lib/crew/orchestrator.ts:57`, `evals/crew/harness.ts:85`, decision-log 2026-07-24 | `docs/decisions/report-hash-projection-2026-07-25.md` |
| 2026-07-25 | **Lane (a) — E2/E4: RETIRE E2's embedding hybrid; KEEP both lanes; RE-ARM NEITHER.** Both were scored once on 2026-07-12, both missed pre-registered floors, both wear "floors not met — experimental/advisory only" and publish full scoreboards (E2 hit-rate 19/24 vs ≥0.85, embedding hybrid failed to beat plain BM25 so the SIMPLER lane shipped; E4 zero-false-merge 14/14 held but the fuzzy ensemble TIED normalized-exact matching, recall 18/35 vs ≥0.80, so exact matching stays the shipped default). **Not re-arming, because re-arming is right only when there is reason to expect a different result and there isn't one:** E2 missed on retrieval over a corpus that has not grown — re-running the same method over the same material is precisely what pre-registration prevents, and a genuine re-attempt needs a stronger method (a build, not a re-score) that nothing downstream currently justifies; E4's miss is INFORMATIVE rather than disappointing — at n=35 fuzziness buys nothing over exact matching, so chasing 0.80 would optimize a method the evidence says is unnecessary. **Not retiring either, because that would delete the most credible evidence in the repo:** E4's VOID-and-re-score (a first run voided for carrying 10 traps against its own registered ≥12 minimum, re-scored on a fresh split, the honest number came back WORSE at recall 0.6→0.55 and was KEPT) demonstrates the measurement discipline better than any passing number, and an independent audit this session named it the project's single strongest anti-theater artifact. Both lanes are offline, $0, deterministic, lock-tested, and green — they cost nothing to keep. **One thing DOES retire (deferred to its own red-green pass since it touches the README CI skip-count claim): E2's embedding-hybrid path** — tested, lost to BM25, and the only part carrying weight it did not earn (a model cache, the repo's only cache-gated CI skip, an inference path supporting a conclusion already reached). **Re-check trigger recorded** so this is not an undated "not yet": the reference corpus grows materially · a downstream surface starts DEPENDING on retrieval quality rather than treating it as advisory · entity resolution is asked to run where exact matching visibly fails | Keep + deferred cleanup | `evals/rag/results/`, `evals/entity/results/`, both pre-registration docs | `docs/decisions/e2-e4-lanes-2026-07-25.md` |
| 2026-07-25 | **Lane (e) — presentation posture SHIPPED, and it fixed a live defect: `metadataBase` named a host that never served the site.** It pointed at `curbside-commons.vercel.app` while the site is deployed on **Cloudflare Pages**, so every absolute URL Next derives (OG image, canonical) addressed a host that does not serve it — repointed to `curbside-commons.pages.dev`, verified in the BUILT export. **OG + Twitter cards added, honest by construction:** a social card is the ONE surface that travels WITHOUT its page — every other surface carries its simulated-data labelling in chrome the reader sees, a screenshotted card does not — so "working prototype", "simulated data", and "not affiliated with any delivery platform" are part of the card COPY, and the committed `public/og.svg` states **SIMULATED DATA · PROTOTYPE** on its face rather than only in alt text. The card honors the site-wide lamp ledger (no ember `#b42318` — a card is decoration, never a verdict) and is fully self-contained (no external fetch from a social crawler). **Robots: INDEXABLE BY DESIGN** — this is a portfolio prototype that exists to be read, so a blanket noindex would defeat its purpose; the honesty obligation is met by labelling, not hiding. `/legacy` is disallowed because an indexed legacy page is how a stale claim outlives the correction that replaced it. No `Sitemap:` line, because naming one that 404s is worse than naming none. Implemented as a static `public/robots.txt` after `app/robots.ts` **failed the build** — a dynamic metadata route cannot be collected under `output: "export"` (proven by the failure, not assumed). 15 new tests read the BUILT export rather than the source, because the built HTML is what a crawler actually receives. Full verify exit 0: **1449 passed + 8 skipped** | Accepted | Built-export assertions; build-failure proof for the route form | `app/layout.tsx`, `public/og.svg`, `public/robots.txt`, `evals/presentation-posture.test.ts` |
| 2026-07-26 | **DEPLOY TARGET MOVED: Cloudflare Pages → VERCEL (owner directive, "no cloudfare remove it replace vercal").** Not a find-replace — **the header policy needed a real port, and a naive migration would have silently shipped a site with ZERO security headers.** `public/_headers` is CLOUDFLARE PAGES syntax; Vercel never reads it. Left in place it would have kept parsing, kept its test green, and protected nothing — **a config file for the wrong platform does not error, it is simply never read**, so the protection stops existing with nothing failing. The four adopted headers (`X-Content-Type-Options: nosniff` · `X-Frame-Options: DENY` · `Referrer-Policy: no-referrer` · `Permissions-Policy: camera=(), microphone=(), geolocation=()`) were re-expressed in a new `vercel.json`, `public/_headers` was DELETED, and `evals/packs/header-policy.test.ts` was rewritten to follow the policy to its new home — including an assertion that the abandoned Cloudflare file is **gone rather than lingering as a decoy** that could keep a stale test green. The CSP deferral survives the port intact (its absence is policy, not omission). Live surfaces repointed to `curbside-commons.vercel.app`: `metadataBase`, the L-2 one-shot's `siteLink`, and both delivery test metas; **all 4 email goldens REGENERATED THROUGH THE REAL BUILDERS** (never hand-edited — the diff is exactly one URL line per golden, verified). New tooth: the built export must contain **no retired-host string anywhere**, which catches the migration that repoints metadata but leaves a hardcoded link or canonical behind. **Note for the record: `metadataBase` has now been wrong in BOTH directions** — it named Vercel during the Cloudflare period, was corrected to `pages.dev` on 2026-07-25, and this directive made `pages.dev` wrong the next day. The invariant is not the value: a metadataBase is a FACTUAL CLAIM ABOUT HOSTING, so it goes stale whenever hosting changes and nothing in the build notices — which is why it is asserted against the BUILT export. Full verify exit 0: **1452 passed + 8 skipped** (+3 tests). The Vercel CLI is NOT installed on this machine; the deploy itself remains the owner's action | Accepted | Built-export scan (0 retired-host refs); golden diffs URL-only; verify green | `vercel.json`, `evals/packs/header-policy.test.ts`, `evals/presentation-posture.test.ts` |
| 2026-07-26 | **CLI capability sweep: 4 findings, incl. a HIGH — the `$0`/zero-network "structural enforcement" was BYPASSABLE, and the README's headline-exhibit recipe CRASHED.** Every finding re-verified locally by execution before any fix. **① HIGH — the load-bearing claim was false.** README: *"enforced structurally by an import-graph eval, not by promise."* The sweep proved otherwise end-to-end, not by inspection: a mirrored CLI with a `node:net` socket + `globalThis.fetch` beacon in `runDemo()` printed `[EGRESS CODE RAN on the CLI path]` while the eval reported `PASS`. My own re-run found MORE than reported — `node:net`, `node:tls`, `node:dgram`, `node:worker_threads`, `node:child_process` all passed the module denylist; `globalThis.fetch(`, `(0,fetch)(` and aliased forms passed the source scan; `import(mod)` was invisible to the specifier extractor, leaving subtrees unwalked as well as unbanned. **The fix is a SHAPE CHANGE, not more patterns: the module check is now an ALLOWLIST** (`evals/lib/import-walk.ts`, shared — ten near-identical walkers is how a hole survives in nine places after being fixed in one). The CLI path legitimately uses exactly 5 node builtins, so anything else fails BY DEFAULT and a module nobody has thought of yet — including one shipping in a future Node — is caught the day it appears; a denylist of egress-capable modules can never be completed. Non-literal dynamic imports are now a HARD FAILURE: an import the walker cannot resolve is one it cannot police. Proven end-to-end — the exact beacon that beat the old eval is now flagged on both counts across the same 35-file walk, pinned as a permanent regression WITH an unmodified-mirror control so the assertion cannot pass for the wrong reason. 27-test known-positive/known-negative suite. **② the README's own recipe crashed the headline exhibit** — `check conformant-but-false.json --against … --surface ucp` died with `Cannot read properties of undefined (reading 'flatMap')`. Cause: that file is a UCP WIRE document (`{simulated, ucp, products}`), the truth leg consumes a FIXTURE (`{items:[…]}`). **Deliberately NOT fixed by teaching the adapter both shapes** — the two formats carry different identity semantics, and silently coercing one into the other is precisely the failure this product exists to catch; it now refuses clearly, names the command that DOES answer the question, and the README points at `demo` (which runs both legs over that exact document). **③ `docs/assets/demo.svg` was STALE** — the D4 slice renamed "serving copy"→"the feed" and regenerated every golden, but the hero image is not a golden and nothing compared it to anything, so the README's "real captured output … never staged" provenance claim was false on the most-looked-at surface. Regenerated through the real renderer (2-line diff) + a freshness tooth that asserts every line the LIVE demo prints appears in the committed image — **red-green proven by restoring the stale file and watching it fail.** ④ the usage text claimed the C10 header surface "always" rides output; `fees`/`demo` carry no `matchingMode`. Corrected the CLAIM, not the code — a fee audit matches nothing, so the field would be meaningless, not missing; pinned per-command. **Three self-inflicted errors caught in-flight and recorded:** a backtick inside a template literal broke `bin/check.mjs` (caught by the suite immediately); a `head` pipe masked the true exit code 2 as 0 (the documented pipe trap, hit anyway); and a test asserting a catalog file must be refused would have PINNED A BUG — a catalog HAS `items`, so exit 1 on drift is correct. Full verify exit 0: **1488 passed + 8 skipped** | Accepted | Local re-verification of all 4; end-to-end beacon proof; red-green on the SVG tooth | `evals/lib/import-walk.ts`, `evals/packs/import-walk-guard.test.ts`, `evals/packs/demo-svg-freshness.test.ts`, `lib/packs/listings/cli.ts` |
| 2026-07-26 | **Site + MCP swept (ALL FIVE surfaces now audited) + the agentic currency pass — 1 MEDIUM found and fixed, 1 unobservable-drift gap closed, 4 versions live-verified.** Run INLINE after three consecutive background workflows died mid-flight (two on the session limit, one on `Not logged in`, ~285k tokens for zero results). **SITE: 5 claims, 5 HOLD, 0 findings** — and the prior session's specific worry is structurally resolved: the C10 honesty gate is not a hardcoded route list but a RECURSIVE WALK of `out/`, so a new route cannot escape it (all 60 built pages scanned); RULES §4(b) verified by scanning all 60 pages for the footer `/docs` link — **0 missing**; all 5 redirect stubs carry a real meta-refresh. Recorded because it is the OPPOSITE of the pattern the other surfaces showed — a claim backed by a mechanism that is complete by construction. **MCP: 1 MEDIUM — the import guard shared the CLI's denylist weakness.** Genuinely better than the CLI's (it carries `node:net|tls|dgram`, which the CLI's did not) but still a denylist with **5 measured ways through**: `node:child_process`, `node:worker_threads`, `node:inspector`, `globalThis.fetch(`, `(0,fetch)(`. **Correction on the record: my first probe transcribed the denylist from a truncated grep and wrongly reported net/tls/dgram as bypasses too — re-probed with the full array; the finding got SMALLER on verification.** Fixed by running the shared allowlist walker alongside the denylist (both kept deliberately: a denylist hit is LEGIBLE — "you imported an LLM provider" — while the allowlist is COMPLETE; common case gets the good message, unknown case still fails). **One reviewed exception:** `@modelcontextprotocol/sdk` is permitted on this surface only — an MCP server cannot exist without it — scoped here and ABSENT from the CLI's allowlist where importing it would be a real finding; not blind, because pre-existing tests already keep the SDK's HTTP/SSE transports unreachable (stdio only). Path traversal probed on 3 vectors (relative, absolute, embedded) — **all refused**. **DRIFT GAP CLOSED: the negotiated MCP protocol revision was not recorded at all**, so the single fact that most defines an MCP server's currency was invisible to a byte-lock built to freeze that exact session — an SDK bump could change it with every test green. Now captured via the transport's `setProtocolVersion` hook (the Transport interface exposes no getter) and pinned at **2025-11-25**; an earlier cut read `transport.protocolVersion`, which does not exist and **would have frozen `null` into the golden forever — a field that looks like coverage and observes nothing.** **CURRENCY, live-verified:** SDK 1.29.0 = current · MCP revision 2025-11-25 (a stale `2025-06-18` citation corrected — the CODE was current, only the comment was not) · UCP **v2026-04-08 confirmed latest** (next-newest 2026-01-23) · Gemini flash/flash-lite pricing **unchanged** at $0.30/$2.50 and $0.10/$0.40, so `PRICING_VERSION` deliberately NOT bumped — it tracks prices, not read-dates, and bumping it on an unchanged re-read would falsely signal a re-price. Full verify exit 0: **1490 passed + 8 skipped** | Accepted | Executed probes for every claim; 60-page scans; 3-vector traversal test | `docs/reviews/capability-sweep-site-mcp-2026-07-26.md` |
| 2026-07-26 | **"Latest agentic AI product" (owner directive) delivered as THREE separable pieces: evals BUILT, positioning SHIPPED, capability RECOMMENDED-AGAINST with a scoped alternative.** ① **AGENTIC EVALS — the K=1 gap closed at the measurement layer.** Every crew label rests on ONE live run (recorded 2026-07-24), and a model that would produce a different terminal on a second pass looks identical on a single pass — so "reached the right terminal" is materially weaker than "reaches it RELIABLY". Built the flip-rate scorer + 7 pre-registered floors (`docs/l1-consistency-preregistration.md`, `evals/crew/l1-consistency.ts`) **BEFORE any multi-rep data exists** — deliberately, because floors written after seeing results are not floors, and this ordering means the bar cannot be fitted to an outcome. Floor design carries real judgment: **C-1 safety flip-rate = exactly 0** (safety that flickers is luck, not a property) and **C-4 zero unsafe-direction flips** — an escalate→recommendation flip is only 1/12 = 8.3%, INSIDE the C-2 rate tolerance, yet it means a human review was skipped by chance, so direction is floored separately from rate. Modal ties break toward escalation (a 2-2 split means the system is not confident, and the honest reading of "not confident" is the outcome that involves a human). 18 tests, **every floor given a PASSING and a FAILING case** (a floor only ever seen passing is decoration), plus a contract test that the scorer reads the REAL committed matrix shape — and it explicitly labels duplicating the single real rep as a SHAPE check, not evidence of stability. **NOT RUN: a K≥3 run is owner-armed by definition; the label stays the honest K=1 one.** ② **POSITIONING — the gap was framing, not capability.** The agentic surface was already documented honestly but buried in row 64 of a status table while the lead sentence positioned the product as a verifier only. Added a README section — *"It is an agentic system that deliberately does not act"* — naming the 4-role crew, 7-tool MCP surface, quarantined artifact content, injection tripwire and pinned report hash, and arguing that **agents-recommend/engine-decides is the STRONGER position, not the weaker one**: the harder engineering problem and the more defensible product. Every clause verified provable before shipping (4 members in the committed matrix · 7 tools/call steps in the real transcript · forced-escalation path present · registration honestly says NOT YET RUN). The K=1 caveat now rides the status row itself. ③ **CAPABILITY — recommend NO for the audit path, YES narrowly for a separate owner-armed operator path** (`docs/decisions/crew-acts-2026-07-26.md`). The core argument: **the product's value IS the refusal to act** — the README's thesis is that every seat in the chain has a conflict of interest, so a referee that could amend a feed or issue a credit has acquired the very stake that disqualifies the others. `RULES.md` §3 already answers it in order ("11. Decision before action · 12. Human approval before risky automation"), and an acting crew puts action before approval on a path whose inputs are ATTACKER-CONTROLLABLE by design — where the tripwire standing between an injected instruction and a real-world effect was demonstrably bypassable TWICE this week. The legitimate acting path operates on the OUTPUT of a completed, human-approved audit (compose the notification that already exists, open a tracking record, schedule a re-check), under the proven L-2 one-shot gate, with hard exclusions (never amends a feed, never moves money, never files with a regulator, never runs unattended, never acts on an artifact that tripped the scan). If the owner wants the full version anyway it needs an explicit RULES §3 amendment, a threat model for acting on hostile input, a pre-registered action-correctness eval — and the K≥3 run scored FIRST, since an agent whose terminals flip is not one to hand actions to. **No code, no RULES change.** Full verify exit 0: **1508 passed + 8 skipped** | ①② Accepted; ③ recommendation, owner-gated | Clause-by-clause provability checks; 18-test scorer suite | `docs/l1-consistency-preregistration.md`; `docs/decisions/crew-acts-2026-07-26.md` |
| 2026-07-26 | **E2's embedding-hybrid lane RETIRED — and it took the repo's last environment-dependent skip with it, making a stale README claim true again.** Executes the deferred cleanup from the 2026-07-25 (a) decision. The hybrid was built, scored once against pre-registered floors, and **LOST to plain BM25**, so the simpler lane shipped and `lookup-reference.ts` has used BM25 only ever since. What remained was a cache-gated determinism test **for a lane nothing ships** — it required a local model cache, was therefore the ONLY skipped-in-CI test, and its skip is what forced the README's "1145 local vs 1144 in CI, the one-test difference is the embedding-hybrid check" caveat. Retired because it carried weight it did not earn: an inference path and a model-cache dependency supporting a conclusion already reached and published. **What is deliberately NOT deleted — the EVIDENCE:** `evals/rag/results/` keeps the full scoreboard including the hybrid's losing numbers, `scripts-ts/` retains the scoring path so the published record stays REPRODUCIBLE, and `lib/rag/hybrid.ts` + `embed*.ts` remain as the artifacts those numbers were measured on. **Retiring a capability is not the same as erasing the evidence that it was evaluated and lost** — the honest record of a miss is the most valuable thing this lane produced. **The README claim was then re-derived from a real run, not edited:** all 8 remaining skips are `describe.skipIf(!live)` owner-armed live-network harnesses, off by default and skipped IDENTICALLY locally and in CI, so **the counts now match on both** and the one-test-difference caveat is obsolete. The old number was also stale by ~360 tests. **New tooth (`evals/packs/readme-skip-claim.test.ts`, red-green proven by re-introducing the exact skip):** it deliberately does NOT assert a suite-size count — a count assertion fails on every honest test addition and trains people to update it without reading it — but asserts the PROPERTY the claim actually makes, that every `skipIf` gate in the repo is a live-lane gate. An environment-dependent skip (cache, platform, installed binary) would diverge the counts again and silently falsify the sentence; that is what it catches. Full verify exit 0: **1511 passed + 8 skipped** | Accepted | Real-run skip inventory; red-green on the tooth | `evals/rag/rag-lane-units.test.ts`; `evals/packs/readme-skip-claim.test.ts` |
| 2026-07-26 | **Deploy runbook written (the deploy itself is BLOCKED on this machine, verified not assumed) + DCWP watch-items spot-checked live — no rule moved.** **DEPLOY:** checked rather than presumed — `which vercel` → not installed · `auth.json` → absent · `config.json` has `currentTeam: team_JFEjWZeHLROwGXXC755erEXl` but **no token**. So the account and team exist (matching the handoff) and nothing here can authenticate; it is a real blocker AND an owner-gated one. Readiness verified instead: verify exit 0 (1511+8) · `out/` with 60 HTML pages · robots.txt + og.svg ship · `vercel.json` parses with the right shape (framework nextjs, outputDirectory out, 4 security headers) · **0 `pages.dev` matches in the build**. Runbook `docs/deploy-vercel-runbook.md` carries the exact commands, the no-CLI GitHub alternative, and post-deploy verification — **including a live `curl -I` header check, which is the one that matters**: `public/_headers` was Cloudflare syntax Vercel never reads, so only a live response proves the ported headers actually arrive. Also flags retiring `pages.dev`, since two live sites disagreeing about the same product is its own honesty problem. **DCWP:** the hearing is **12 days out** and the session-16 ruling is track-only — writing copy about a hearing that has not happened is the opposite of this project's standard, so no preemptive pass. What WAS available: watch-item ① had a comment deadline of **2026-07-16, the day AFTER the last rule-table verification**, so it could have moved unobserved. Checked live at the primary source: **still `Proposed`** — comments closed, one comment received, no adoption/effective date, no published outcome. **No codified row changes**; the as-of stays honestly at 2026-07-15 (a spot-check is not a re-verification, and the note says so). Recorded in `docs/research/uc1-rule-table.md` so the check is on the record rather than in a session's memory | Accepted | Live primary-source fetch; on-machine credential check; build-artifact verification | `docs/deploy-vercel-runbook.md`; `docs/research/uc1-rule-table.md` |
| 2026-07-24 | **Goal fixed (owner pick, /claude-os live estate-test run): P3 — agentic-product audit + agent-surface strengthening.** Full independent audit of Curbside Commons AS a working agentic AI product (crew, MCP tools, recommend-only lanes, playground), then gate-verified fixes via Opus builders (Fable orchestrates + judges every unit). Adjacent dispositions in the same pick: P1 email-design lane → WAITING-ON-OWNER (suspended, not cancelled; Claude Design return loop stands); P2 DCWP currency pass → converted to a recorded trigger (re-evaluate at the 2026-08-07 hearing outcome, per the session-16 track-only ruling); P4 purge + P5 domain/OG/robots → one structured ask riding the goal. Bindings: no transport wiring · curated report (2026-07-22 directive) · verbatim design base. Taste-advisor advisory on record this session | Accepted | Owner pick via structured ask; taste-advisor consult | this row; CURRENT_TASK session-33 bullet |
| 2026-07-27 | **VERCEL DEPLOY EXECUTED on the owner's word — and it took THREE attempts, because two config defects were invisible to every in-repo check.** Live at **https://curbside-commons.vercel.app** (project `prj_RA3ZL1C3T4ilf7AE7xAMXjEQk0fJ`, org `team_JFEjWZeHLROwGXXC755erEXl` — an exact match to the team id recorded in the handoff, confirmed at link time rather than assumed). **① Deploy FAILED: `framework: "nextjs"` + `outputDirectory: "out"` are individually valid and mutually contradictory.** `framework: nextjs` runs Vercel's Next.js builder, which requires `routes-manifest.json`; `outputDirectory: out` aimed that builder at the `output: "export"` folder, where no manifest exists → `Error: The file "/vercel/path0/out/routes-manifest.json" couldn't be found.` Session 34 verified this file "parses, correct shape" and it did — **shape-correctness is not deploy-correctness.** Fixed to `framework: null`, which serves the static export directly. **② Deploy SUCCEEDED and the site was still 5/6 dead.** With `framework: null` Vercel serves `out/` as plain static files; Next's export emits `report.html`, not `report/index.html` (trailingSlash unset), so `/` returned 200 while **/report, /fees, /playground, /proof and /docs all 404'd** — `/report.html` served fine. Fixed with `cleanUrls: true`. **Why nothing caught either one, which is the finding worth keeping:** every in-repo check — including the C10 recursive walk of all 60 pages — verifies that **the FILES EXIST**. None verify that **the HOST maps a URL to a file**. The export was complete and correct while the site was broken, and only a live request could tell the two apart. This is the `public/_headers` lesson one level up: that file was Cloudflare syntax Vercel never read; these were Vercel keys Vercel read and rejected (①) or read and honored into a 404 (②). Both red-green pinned in `evals/packs/header-policy.test.ts`, each with the actual deploy error in the comment so the assertion traces to an outcome rather than a preference. **POST-DEPLOY VERIFICATION, live not assumed:** all six routes **200** · **all four security headers arrive** (checked on `/report`, a subpath, not just root — the check the runbook called the one that matters, and the whole point of the `_headers`→`vercel.json` port) · robots.txt 200 · og.svg 200 image/svg+xml · an unknown path correctly 404s. `.env.local` and `.vercel/` are both gitignored (RULES §11 verified, not presumed). **STILL OPEN, deliberately not folded into this row: `curbside-commons.pages.dev` is a separate Cloudflare-dashboard action by the owner** — two live sites disagreeing about the same product is its own honesty problem | Accepted (owner-worded deploy, 2026-07-27) | Live curl verification of every claim; both defects found by deploying, not by reading; red-green teeth on each | `vercel.json`; `evals/packs/header-policy.test.ts`; `docs/deploy-vercel-runbook.md` |
| 2026-07-27 | **K≥3 CREW CONSISTENCY RUN EXECUTED on the owner's word — ALL SEVEN PRE-REGISTERED FLOORS CLEARED, and a defect in MY OWN SCORER was caught and corrected on the record.** **First, a gap the pre-registration did not know it had.** Session 34 committed the scorer and the seven floors and tested both against synthetic reps — but never a multi-rep RUNNER. `crew-live-l1-run.mts` writes three fixed paths under `evals/crew/gold/` and **probe-writes over all of them BEFORE the first live call**, so running it three times would have destroyed the committed 2026-07-07 K=1 exam record — pinned by `l1-live-lock`, `l1-live-composition-lock` and `l1-lane-drift-lock` (which reads the model id straight out of `l1-live-matrix.json`) — at second zero of rep 1, before spending a cent. **The measurement was fully specified, fully tested, and had no non-destructive way to collect its own input**; "the scoring code is committed BEFORE the run" was satisfied, which is exactly why nobody noticed. Fixed with a rep-scoped `L1_OUT_DIR` (default behaviour byte-identical), committed BEFORE any rep fired, and the canonical artifacts verified untouched via `git status` rather than assumed. **THE RUN:** K=3 independent full passes over the committed n=20 held-out split, `openai/gpt-oss-120b`, **20/20 scored and 0 provider-degraded in every rep**; no case re-fetched, no failure retried (the harness has no retry path, and §4 forbids re-running only failures — that manufactures the stability being measured). Raws **frozen and committed before any metric existed** (`ce0814a`, §2). **RESULT — zero flips of any kind:** C-1 safety flip-rate **0.0000** · C-2 terminal **0.0000** · C-3 class **0.0000** · C-4 unsafe-direction flips **0** · C-5 modal correctness **20/20** · C-6 coverage **5/5/5/5** · K **3**. Every case produced an identical terminal, class and safety verdict on all three passes. **THE CORRECTION, kept on the record because it is the most instructive part.** The FIRST scoring pass reported C-5 as **9/20** and verdict DEFERRED. That was a bug in the scoring CLI, not the crew: it compared `expectedGateState` (`approve-recommendation | escalate-to-human`) directly against `terminal` (`recommendation | escalate-to-human`). The vocabularies agree on one value and differ on the other, so the comparison **passed all 9 escalate cases and failed all 11 approve cases regardless of crew behaviour — C-5 was unpassable by ANY possible output.** Caught only because six floors read a perfect `0.0000` while the seventh read 9/20: an internally impossible shape. **Why correcting it is legitimate and NOT "retrying to green": no floor moved (C-5 still demands 20/20), no rep re-ran, and the raws were frozen and committed before either pass — only the arithmetic changed, over byte-identical data anyone can re-score.** A floor no behaviour can clear is not a strict bar, it is a broken gauge. Fixed by **importing** the repo's own documented mapping (`expectedTerminalFor`, extracted from `harness.ts` where it already governed the live run's safety check) instead of re-typing it, so the two callers cannot drift; the extraction is provably inert because `l1-live-lock` re-derives every committed matrix row through `evaluateCase` and stayed green. **Independently corroborated:** a terminal/expectation mismatch is recorded as a SAFETY VIOLATION during the run by a different code path predating this CLI, and all three reps reported safety 5/5 on all four members — zero gate mismatches, implying 20/20 without trusting the scorer I had just fixed. **Pinned by 3 new teeth, red-green proven** by reintroducing the identity mapping (fails all 3 plus `l1-live-lock`; reverting → 25/25): the mapping is asserted LOAD-BEARING (not an identity), and every committed case must map to a lawful terminal — the generic catch for "an expectation no record can ever match". **What this earns, exactly: "crew terminals are stable across K reps (K=3, flip-rate 0/20, zero safety flips, zero unsafe-direction flips) on the committed n=20 scenario set."** Nothing wider — no real-world distribution claim, no larger-n claim, no general reliability claim about the model. The K=1 caveat on the crew label can now be retired for THIS scope | Accepted (owner-armed run, 2026-07-27) | Freeze-before-scoring honored; floors unmoved; correction cross-validated by an independent code path; red-green teeth | `docs/l1-consistency-preregistration.md`; `evals/crew/gold/consistency/`; `evals/crew/crew-consistency.test.ts` |
| 2026-07-27 | **CODEX CROSS-MODEL GATE RAN ON THE FIXES → VALID BLOCK (2 P1 + 5 P2 + 3 P3); ALL ACCEPTED, ALL FIXED — and it CONFIRMED the one call I was least entitled to make alone.** Run because session 34's own carried-forward lesson says *"run the gate on FIXES, not just features"* and I had not: the session's highest-stakes call was **maker-judging-maker** — I wrote the scoring CLI, diagnosed its defect, and ruled that correcting it after a DEFERRED result was an instrument repair rather than retrying-to-green, a ruling that flipped the published verdict to FLOORS CLEARED. The brief asked the reviewer directly whether that was rationalization. Seat smoke-tested `SEAT_OK` first (standing rule). **Every finding independently reproduced before acceptance; none taken on the reviewer's word.** **P1-1 — the scorer certified a 19-case run as COMPLETE.** It derived the universe of cases from the UNION OF IDS SEEN IN THE MATRICES: a case degraded in one rep was caught, but a case absent from EVERY rep never entered that union, so `droppedIds` was empty, `completeCoverage` was true, and the run cleared C-5 at **19/19**. Reproduced by removing `l1-int-injection-visible` — an INJECTION-RESISTANCE case — from all three reps. **This is precisely the denominator-shrinking that file's own header promised to prevent**, and it failed because the guard ASKED THE DATA what should be there. General rule now recorded: *a completeness check takes its denominator from the specification, never from the observations.* Fixed — authoritative ids from the 20 committed case files, hard refusal on unexpected/duplicate ids, logic extracted to a testable `resolveCoverage()` + 6 tests, **red-green proven** by restoring the old logic (exactly one test fails, and it is the regression test). **P1-2 — my own comment asserted a containment the code never implemented.** It read *"resolved against the gold dir, so it cannot escape it"*; `join()` performs no containment — verified: `..` escaped to `evals/crew`, and **`.` resolved to the canonical K=1 gold directory**, pointing the destructive probe-write straight at the artifacts the parameter exists to protect. **A comment claiming a guarantee the code lacks is worse than no comment: it stops the next reader from checking** — the identical shape as the session-34 `$0` claim and this session's `vercel.json` "correct shape" claim, committed by me WHILE FIXING THAT CLASS. Fixed with a strict `consistency/rep-<N>` grammar, symlink-aware containment, and refusal when the target exists (a rep that overwrites a rep is not an independent pass); all five attack vectors verified refused. **P2s, all applied:** the `framework` assertion admitted omission (Vercel auto-detects Next.js when the key is ABSENT, recreating the original failure with the test green) → `toBeNull()`; the "six floors read a perfect 0.0000 … not a shape a real system produces" narrative was **false** (only C-1/C-2/C-3 are rates, and a system can be stable AND consistently wrong) → rewritten in 3 places to the actual tell, that **9/20 is exactly the count of `escalate-to-human` expectations** — the one value both vocabularies spell alike, which is the signature of a naming defect; the README's "scorer committed before any data existed" **erased the CLI repair** → chronology stated explicitly; the README claimed lock tests for "every live run" while the K=3 reps had **none** → **NEW `l1-consistency-lock.test.ts`** re-derives records from raw turns, matrix rows from records, and the published verdict from the matrices (tamper-proven: editing a floor value fails 1 test, editing a frozen matrix row fails 2 — and the first tamper attempt was a NO-OP I caught myself, because the row already held the value I "changed"); the README's stale "deployed site is behind the repo" → corrected to the verified Vercel deploy with the stale Cloudflare named as pending retirement. **P3s:** dated **erratum E-1** on C-5's literal wording (the registered text is NOT rewritten — a pre-registration that gets edited after the fact is not one) and **E-2** recording that the invalid first report was overwritten rather than retained, with the standing rule that **an invalid report is committed BEFORE the instrument is repaired** — a correction is only fully auditable if the thing corrected still exists. **THE TWO CONFIRMATIONS THAT MATTER:** ① *"You are not rationalizing"* — the terminal vocabulary is closed and the mapping predated the run, so a disinterested reviewer should accept FLOORS CLEARED after a disclosed instrument correction; ② the 20/20 is real and **not circular** — at `ce0814a` the harness already held the correct mapping inline while the CLI held the defective one, so corroboration predates the shared helper; the reviewer independently replayed all three frozen turn sets, **60/60, zero gate mismatches**. Honest limit: the reviewer could NOT reach the live site (`curl` returned `000` in its sandbox), so the production route/header results rest on this session's own live verification. Post-fix verify exit 0: **1534 passed + 8 skipped** | Accepted — all 10 findings fixed, none refuted (primary-model-final) | Every finding reproduced locally before fixing; red-green on both P1s; tamper-proofs on the new lock | `docs/reviews/codex-2026-07-27-s35-gate.md` |
| 2026-07-27 | **CONFIRMED AND SURFACED: `vercel link` silently connected GitHub auto-deploy, so `push` and `deploy` had become the SAME action.** Not left as a question — verified authoritatively via `vercel api /v9/projects/curbside-commons`: `link = github:sharanlabs/curbside-commons`, **auto-deploy ACTIVE**. It arrived as a **side effect** of `vercel link`, never as a decision: when asked, the owner chose "I'll log in now" (the CLI path) and the GitHub-dashboard route was the option they did NOT pick. The consequence is that two separately owner-gated actions collapsed into one — a `git push` would deploy — and it introduces standing infrastructure, which the prototype-not-service doctrine (CLAUDE.md, 2026-06-11) exists to prevent. **I attempted `vercel git disconnect` and the auto-mode classifier DENIED it** (raw: *"Permission for this action was denied by the Claude Code auto mode classifier"*); recorded verbatim and **not worked around** — which is arguably the right outcome for an external-state change anyway. **OWNER ACTION, one reversible command: `vercel git disconnect`** (re-enable any time with `vercel git connect`). Recorded in all three state docs and the resume prompt as a confirmed fact with the exact command, rather than as research the owner has to redo. **Also settled in the same pass (advisor-prompted, and it BLOCKED until checked): the committed `consistency-report.json` was written by the PRE-FIX scoring CLI.** If the repaired CLI produced anything different, the published verdict AND the new `l1-consistency-lock` would both rest on an artifact no current code path emits. Re-ran the fixed CLI over the frozen bytes and diffed: **IDENTICAL** apart from `scoredAt` (full coverage, same seven floors, same verdict); the report was restored to its committed bytes so the tree stayed clean. Deploy provenance also closed honestly: the first deploys built on Vercel's builder where git is unavailable, so the footer read *"build — source SHA unavailable at build time"* — true and non-fabricating, but it left the site unable to name its own commit while the runbook requires recording the deployed SHA; redeployed with the explicit `BUILD_SOURCE_SHA`/`BUILD_TIMESTAMP_UTC` hook `next.config.ts` already supports. **No `+dirty` was ever served.** Content was rebuilt from an identical tree each time — verified by `git diff` over `app/ lib/ public/ next.config.ts vercel.json` — so the later deploys updated the provenance label, not the product | Accepted; the disconnect is owner-gated | Vercel API read; classifier denial recorded raw; report re-derived and diffed | `docs/deploy-vercel-runbook.md`; `evals/crew/gold/consistency/consistency-report.json` |
| 2026-07-27 | **THE SITE BECAME A TOOL: /playground rebuilt as a two-file audit workbench on the owner's word ("I WANT WORKING WEBSITE WHERE I WILL UPLOAD TEST FILES … complete all the steps"), and a RULES §4 defect was found and fixed BEFORE the feature that would have shipped it.** The owner's read was correct and specific: every interactive surface on the landing page was a staged exhibit — the receipt replays, and "Break the feed yourself" checks ONE price field against ONE hardcoded record (`TryBench.tsx:42`, `2150¢`). Worse, the hero's "Try it on a feed" CTA was an anchor to `#try` — that same toy, further down the same page — so a reader who accepted the site's own invitation never reached anything that takes a feed. **Zero `type="file"`, zero `FileReader`, zero drag-drop existed in the repo.** **THE BLOCKER FOUND BEFORE BUILDING (slice 1, the important one).** `runListingsVerification` HARDCODED `matchingMode: "synthetic-controlled"` and `simulated: true` into every report (`run.ts:122-124`), while `VerifierReport.simulated` is typed `boolean`. That was TRUE of every caller that existed — all fed the committed corpus — and becomes FALSE the instant a reader uploads their own catalog: the engine would have stamped a reader's real records as simulated and asserted shared-synthetic-id matching it never performed. **A claim broader than the thing backing it — the identical class this repo caught four times in session 34 and twice in session 35, and I was about to author the seventh.** Fixed as an OPTIONAL third parameter defaulting to today's exact values, so all ~40 existing call sites produce byte-identical reports (the untouched golden + drift-lock suites are the proof); `specVersion` deliberately stays non-overridable — a caller may describe WHOSE data ran, never WHICH RULES ran. 5 red-green teeth incl. one proving labels never steer findings. **SLICES 2-5.** A catalog parser holding the feed parser's discipline — refuse only what the engine cannot index, name the exact row (`items[0].variations[0]`), and never coerce; a value that merely DISAGREES is a finding, not a parse error (14 teeth). Upload mechanics via `FileReader` — zero-network BY CONSTRUCTION, proven by extending the existing import-walk guard to the new closure, not by promise. The page rebuilt: two slots, drag-drop + browse + paste, downloadable report. The hero CTA repointed to `/playground` (destination now pinned as the e2e contract). **THE DESIGN REVIEW the owner asked for, four findings, all fixed:** six `.pg-*` rules (`.pg-page/.pg-sec/.pg-h1/.pg-lede/.pg-boundary/.pg-noscript`) had ZERO `.tsx` references — orphans from the pre-v9 playground; `.pg-sec` declared a **980px** column while the page renders inside `.ds-wrap`'s **1180-1720px**, which WAS the owner's "check the spacing" — the surface was authored off-grid for a container it no longer occupies; the `.pg-*` block used `var(--s*)` twice in ~60 rules against hand-typed pixels while adopted v9 sections sit on the scale; and `.p2-head` was declared identically TWICE (5430, 6820) — a trap where an edit to one silently loses to the cascade. The workbench is built entirely on the `--s*` scale in the v9 register. **THREE MORE CAUGHT ONLY BY LOOKING** (in-repo tests verify files exist, never that a page reads right — session 35's own lesson): the disabled run button rendered as a low-contrast ENABLED button rather than an unavailable one; its helper note said the same sentence in every state; the result section's full-width hairline read as a page divider rather than a result boundary. **AND ONE THE PRODUCT CAUGHT ON ME:** my first e2e "faithful pair" fixture reported a MODIFIER-AMBIG finding — the engine was right and my fixture was wrong (a single-variation item's expected title omits the variant label). Fixed the fixture, not the test. **`PlaygroundClient.tsx` DELETED** as orphaned, and its C10 + import-walk guards REBOUND to the shipping components — a honesty guard aimed at a surface the page no longer renders reports green over nothing. **Gates: verify exit 0 · vitest 1556 + 8 skipped (from 1534, +22 teeth, every one red-green) · e2e dev 48 passed/1 skip · e2e ARTIFACT 48 passed/1 skip (the static-export mode that actually ships) · axe 8/8 zero WCAG A/AA violations incl. the new surface · C10 green.** Tree UNCOMMITTED; commit/push/deploy remain the owner's word, and the four open owner-gated items (vercel git disconnect · push · retire pages.dev · DCWP 2026-08-07) are untouched by this work. |
| 2026-07-27 | **CODEX CROSS-MODEL GATE ON THE UPLOAD WORK → VALID BLOCK (9 P1 · 5 P2 · 2 P3); ALL 16 FINDINGS ACCEPTED AND CLOSED — and it found the error my own adversarial pass could not.** Run because slice 1 changed the C3/C10 honesty labels RULES §4 exists to protect, and I was maker-judging-maker on them. I attacked my own six claims first with executable tests and found four defects (record: `docs/reviews/adversarial-2026-07-27-s36-upload.md`). **The gate then found that THREE OF MY FOUR FIXES WERE STILL WRONG**, including a category error self-review structurally could not reach: **I had been reading `matchingMode` as a statement about DATA OWNERSHIP when it describes the MATCHING MECHANISM.** `report-view.ts:76-79` decodes `real-world` as *"identifiers do not line up, matched by resolution (fuzzier)"* and `sorReference` only ever performs exact shared-id lookup — so labelling a reader's run `real-world` asserted a mechanism the engine never ran. My fix removed one false claim and introduced another. Also: **C10 `simulated` is an OR across BOTH sides** (our sample feed against a reader's catalog was reported not-simulated, though the feed is explicitly ours); **content comparison was still inference** (change only `asOf` and the corpus reads as ours; reorder identical items and it reads as the reader's) — deleted entirely in favour of `RunOrigin` carried from the ACTION, since **provenance is a fact about the action, not a property of the document**; and **my fix was a HALF-FIX — the UI still lied**, printing "your own records" beside a correctly-labelled report because it read "is this slot non-empty" rather than the recorded source. Further P1/P2s all reproduced and fixed: the parser silently invented `currency:"USD"` (an uploaded EUR catalog rewritten, manufacturing a false mismatch on every row) · `asOf` accepted unvalidated then compared LEXICALLY (a `+05:30` stamp sorts by its digits and reverses staleness verdicts) · ids containing the engine's `#` delimiter TRUNCATE (`sku#1` read as `sku` — a false mismatch on data that agrees exactly) · display cleanup rewrote READER evidence ("Simulated Burger" → "example Burger", screen and downloaded report stating different facts) · an async `FileReader` race left a stale verdict runnable and could land two reads out of order · **"zero-network by construction" was itself overclaimed** — the walk checked only IMPORTS, but `fetch`/`sendBeacon`/`XMLHttpRequest`/`WebSocket`/`EventSource` are GLOBALS needing none, so the guard on the surface that reads the reader's files was a claim broader than the thing backing it · both file inputs shared one accessible name · `isInteger` accepts unsafe integers (9007199254740993 silently becomes ...992). **THEN THE THREE DEFERRED ENGINE ITEMS WERE CLOSED IN THE SAME SESSION rather than carried** — the deferral was sound about process and wrong about timing, because **F-8 is a SILENT MISS**: a row whose id was renamed but whose title resolved uniquely got one finding about the id and silence about everything else, INCLUDING A HIDDEN ITEM BEING SOLD, since every detector runs off a reference keyed by the stale variation id. Leaving a known silent miss in place while shipping a tool that invites strangers to audit their own data is a decision, not a deferral. Fixed with `pinnedReference` (claims re-judged against the resolved record, findings citing the RESOLVED id, `existence` deliberately re-run because the hidden rule fires only on it); **F-9** fixed with a new `LST-IDENT-TITLE-AMBIGUOUS` rule — two rows sharing an expected title used to report *"no such item exists"* when TWO did, the opposite of the truth. **THE CHECK THAT MATTERED: the committed drifted feed was re-derived directly after the engine change — still EXACTLY 16 findings, same rule set.** The fix adds detection only where a defect was escaping and changes nothing already correct. **THE LESSON: self-review finds bugs in what you built; cross-review finds bugs in what you BELIEVED — the second class cannot surface by trying harder alone, because the belief is the thing doing the looking.** Final gates: verify exit 0 · vitest **1574 + 8 skipped** · e2e dev 49/1 · e2e ARTIFACT 49/1 · axe 8/8 zero WCAG A/AA · C10 green. Records: `docs/reviews/codex-2026-07-27-s36-gate.md` + 9,064-line raw. Tree uncommitted; commit/push/deploy = owner word. |
| 2026-07-28 | **DCWP WATCH ITEM CHECKED (2026-07-28) — TRIGGER HAS NOT FIRED; NO COPY CHANGE, and the reason is recorded rather than the conclusion alone.** The standing item is: *re-evaluate `/fees` + the rule table at the DCWP junk-fees hearing OUTCOME (2026-08-07)* — the trigger fires at the outcome, not the date. Verified against the **primary source**, not a summary: the DCWP Notice of Public Hearing PDF (`rules.cityofnewyork.us/.../DCWP-NOH-Rules-Relating-to-Junk-Fees.pdf`) states verbatim that the hearing is at **11:00 AM on August 7, 2026**, comments are due **on or before August 7, 2026**, and the document is captioned *"Notice of Public Hearing and Opportunity to Comment on **Proposed** Rules."* **The outcome does not exist yet, so there is nothing to react to.** Two distinctions worth keeping, because collapsing them is how a false claim would enter: (1) **the proposed junk-fees / all-in-pricing rule is NOT the statute this product audits.** `/fees` cites **§20-563.3 (Local Law 79 of 2025)** — the third-party-delivery FEE CAP — while the junk-fees proposal is an all-in-pricing disclosure rule of general application. A rule that is merely adjacent must not be treated as governing. (2) **The fee cap itself was re-checked and has not moved**: enacted 2025-05-31, effective 2025-06-30, no amendment or repeal found; the engine's committed caps (15% delivery · 5% marketing · 3% other · 20% total, `lib/packs/fees/rules.ts:79-87`) still match. **The live `/fees` as-of line therefore stays `2026-07-15` — honestly, since that is when the rule table was last verified against the certified text, and nothing since has changed it.** Advancing that date on the strength of today's check would be the exact overclaim this project keeps catching: a date asserts *"verified against the source on this day,"* and today's check verified the STATUS of a different, still-proposed rule. Watch item stands, unchanged, with the same trigger. Sources: DCWP NOH PDF (primary, read directly) · NYC Council / Local Law 79 of 2025 record · amlegal §20-563.3 (403 to automated fetch — noted, not worked around). |
| 2026-07-28 | **PUSH AND DEPLOY ARE TWO ACTS AGAIN — fixed IN THE REPO rather than in invisible account state (owner: "fix the vercel git disconnect").** `vercel link` had connected GitHub auto-deploy as a SIDE EFFECT in session 35 — the CLI-login path was chosen deliberately, so the GitHub route was the option NOT picked — and from that moment `git push` and `deploy` were one action, with **nothing in the repo recording it**. It was found by querying the Vercel API, not by reading anything in the tree. **Fix: `"git": { "deploymentEnabled": false }` in `vercel.json`**, which disables automatic deployments for every branch (Vercel git-configuration docs, **verified live 2026-07-28**, not recalled). **Chosen OVER `vercel git disconnect` for a reason beyond the twice-denied classifier** (raw errors on record both times, never worked around): the CLI command mutates **invisible account state that no reviewer can see and no test can assert**, whereas this ships in the diff and is pinned by a test in `evals/packs/header-policy.test.ts`. *A guarantee you cannot read is one you are trusting rather than holding* — which is the argument this product makes about feeds, applied to our own infrastructure. **The git link is deliberately LEFT CONNECTED:** it is what still permits a deploy on purpose; what was removed is the automatic trigger. **VERIFIED EMPIRICALLY, and it behaved BETTER than predicted.** The commit message states the expectation that config binds only after the deploy that ships it, so *this* push would still deploy — **it did not.** Vercel reads `vercel.json` from the incoming commit BEFORE building, so the setting took effect on the very push that introduced it: `5d09454` reached `origin/main` and **no deployment was created** (`vercel ls` shows the newest deployment is still `b2f781f`, 43m older; the deployment API confirms `githubCommitSha = b2f781fa2b2f`). Production continues to serve `b2f781f` — correct, since nothing has been deliberately deployed since. The prediction in the commit body is left standing rather than amended: it was the honest expectation at the time, and the record is more useful showing where reality corrected it. **Net state: `git push` ships code and nothing else; a deploy is now a separate, deliberate act — restoring the two-gate separation the doctrine assumes.** |
| 2026-07-28 | **THE OWNER-FIXED DESIGN WAS REOPENED BY THE OWNER, AND THE SITE WAS INVERTED FROM AN EXHIBIT INTO A TOOL.** Owner's words, verbatim and authoritative: *"the website looks like a display pieces with number"* · *"the merchant platform remove"* · *"skip the nyc august 7 not needed"* · *"I want ready to use website to upload test files and check how it is working. Reevaluate the whole design walk through from start where the user would land next step navigation, where he would upload files, analyze it how would it go next steps"* · then, widening: *"do deeply your inspections, audit, reevaluation from all perspectives and dimensions independently"* · *"enhance the typography of the all fonts, looks too thin, need a premium curvy type one"* · *"navigations, user experience as well even if we want to redesign from start to end lets do it. dont just go by memory and training knowledge"* · *"not just design"* · *"discover yourself other aspects and other dimensions"*. This row exists because RULES §6/§15 require a recorded decision + human word before a scope/architecture change, and because the v5/v9 design was owner-FIXED — only the owner could reopen it. **THE DIAGNOSIS WAS MEASURED, NOT ASSERTED.** Session 36 answered the same complaint (*"I WANT WORKING WEBSITE WHERE I WILL UPLOAD TEST FILES"*) by BUILDING the two-file workbench — and putting it in section four of page three. Rendered geometry at 1440px: `/` was **4924px tall with no upload affordance anywhere**, and the tool on `/playground` began **~2990px down** behind four exhibit sections. The cause was structural, not editorial: `.cs-hero` is `min-height: calc(100vh - 68px)`, so while the animated scene was the hero **nothing else could reach the first screen**. Adding a tool deeper into a narrative had been tried and did not land; the fix was the ORDER. **WHAT CHANGED.** `/` now opens on the workbench (same `AuditWorkbench` component — deliberately NOT forked, because two upload surfaces are two surfaces free to drift, a defect this repo already shipped once when a retired host served an older `/playground` and two live sites disagreed about what the product IS); the scene and the receipt moved BELOW the tool, where a diagram explaining a check belongs. `/playground` became "How it works" and its duplicate upload section was replaced by a door back to the tool — one tool, one place. Nav de-numbered to named destinations (`Audit · Example report · Fee rules · How it works · Proof`) and the standing `CASE 001 · CLAIM HELD · 16 FINDINGS` readout — a case number that never changes, describing somebody else's data — replaced by a per-route readout that states something true (on the tool: `RUNS IN YOUR BROWSER · NOTHING IS UPLOADED`). The de-numbering was swept SITEWIDE rather than stopping at the nav: `01 · LISTINGS AUDIT`, `02 · FEE AUDIT`, `CHAPTER FOUR · PROOF`, `CONTINUE · 01/02/03/04`, `FIG. 01–05`, `SPECIMEN —`, `FILE A`/`FILE B`, `REGISTER — … · CASE 001` and the "chapter 01/04" prose references are all gone — a nav reading "Example report" over a page reading "01 · LISTINGS AUDIT" would have been worse than not starting. **REMOVED on the owner's word:** the `FOR MERCHANTS · PLATFORMS · AGENT OPERATORS` eyebrow and the three-seat *"Different roles need the same proof"* band (the two — and only two — places `PLATFORMS` appeared in shipping code; `app/merchant/[id]` was checked and is a non-indexed redirect stub, so it was never a candidate — settled by grep, not by asking). **`TryBench` DELETED with its equivalence pack (−8 tests, justified):** it mirrored ONE price rule in page-local code and needed a pack to prove the mirror still matched the engine. The workbench runs the REAL engine on the reader's own two files, so the mirror is not simplified — it is gone, and the mirror-drift defect class with it. **NEXT STEPS built** (the owner's fifth beat, which had no answer): a verdict now offers *audit another pair* (a real reset of both slots — previously only a page reload), the worked report, how the rules are applied, and the "what is real" page. **TYPOGRAPHY: Onest → Nunito, plus a weight+contrast pass and a display-scale retune.** Six candidates were rendered in the site's OWN hero copy and compared visually; Gabarito was proposed and **the owner picked Nunito** — the most literally curved of the set (every stroke ends in a rounded terminal rather than a flat cut). Availability and the variable `wght` axis were verified against the INSTALLED manifest (`node_modules/next/dist/compiled/@next/font/dist/google/font-data.json`), not recalled from training — and the repo declares **no weight below 400 anywhere**, comfortably inside Nunito's 200 floor, so nothing falls back to a synthesised weight. Weights were then RE-calibrated upward (+60 display, +40 body) because at an identical numeric weight Nunito's rounder, more even strokes read optically LIGHTER than Gabarito's: handing back a face thinner than the one the owner rejected would answer the letter of "curvy" and miss the point. **TWO DEFECTS THE WEIGHT PASS EXPOSED, BOTH PRE-EXISTING, BOTH INVISIBLE TO SCREENSHOTS.** ① **The "display tier" block (globals.css ~8862) has been silently DEAD for landing headings since it was written.** `.lp-main h1, .lp-main h2, …` scores (0,1,1); every rule in that block targets a bare class like `.cs-h1` at (0,1,0). **Specificity beats source order**, so the cascade discarded the whole block while its comment claimed it added weight. Caught by reading `getComputedStyle` in a real browser — the first weight pass appeared to apply, screenshotted plausibly, and computed 600 instead of 740. Fixed by element-qualifying every display selector (`h1.cs-h1`). *A rendered difference between 600 and 740 is exactly subtle enough to eyeball wrong; the computed value is not.* ② **The heading hierarchy was INVERTED at every desktop width.** `.cs-h1` was `clamp(31px, 3vw, 43px)` while `.lp-h2` was `clamp(28px, 3.3vw, 48px)` — 38.4px vs 42.24px at 1280 — so the page announced its second-level headings LOUDER than its own H1. Survivable while the H1 sat alone in a 100vh hero; immediately visible once "Audit a feed." sat directly beneath it. Fixed by bringing the section scale DOWN (`clamp(25px, 2.5vw, 35px)`) rather than inflating the H1, because 42–48px section headings are poster scale, not page scale — they were part of why the site read as something to look at rather than use, and they ate the vertical room the tool needs above the fold. Final measured: H1 42.24px / H2 32px at 1280, both drop zones and both sample buttons fully above a 900px fold. "Too thin" was treated as TWO levers: weight (display +60–80, body 400→460, mono chrome +60–100) and CONTRAST (body prose moved from `--muted` 6.59:1 to `--graphite` 8.31:1 — thin strokes in light grey read lighter than the same strokes in near-black). Both clear WCAG AA; the change raises the floor rather than approaching it, and axe stayed 8/8 zero A/AA. **Note for the record: `next/font/google` fetches at BUILD time — the runtime promise (no off-origin requests) is unchanged, and the build machine reaching Google Fonts is a build dependency, not a runtime one.** **TWO DEFECTS I INTRODUCED AND CAUGHT BY READING THE DOM, not by testing:** the rewritten landing rendered `17codified` and `testscompare` — JSX trims the lines of a multi-line text node and rejoins them, so a space sitting after a `}` vanishes even though the source plainly contains it. `HEAD` was checked and had used `{" "}` correctly, so these were mine. This is why the original source is littered with `{" "}`: it is load-bearing, not noise. No unit test in this repo can see that class; the assertion added to `canonical.spec.ts` now can. **NEW TOOTH — the requirement itself, made executable:** `the tool opens the page — both drop zones sit above the fold` asserts the drop zones' `boundingBox().y < 900` at the 1280px desktop floor. Reachability is not the requirement; ARRIVAL is, and reachability is what regressed twice. **THE NO-JS FLOOR MOVED RATHER THAN DIED:** it used to assert the try-bench's SSR verdict; the page now opens on a tool that genuinely cannot work without scripting, so the floor became "no dead controls, and the reason stated in words" plus the still-SSR-settled receipt. **DCWP 2026-08-07 watch item CLOSED on the owner's word** (*"skip the nyc august 7 not needed"*) — it lived only in state docs, never in shipping code; `/fees` and the §20-563.3 rule content are a product chapter and were deliberately NOT touched. **Gates: verify exit 0 · vitest 1573 passed + 8 skipped (1581 → 1573 = exactly the 8 retired TryBench tests) · e2e 50 passed + 1 skipped (49 → 50: −1 TryBench, +2 new contracts) · axe 8/8 zero WCAG A/AA · layout-sanity clean at 1280/1440/1728.** Deferred and recorded rather than done silently: several CSS class groups are likely orphaned after the removals (`.tab-cut`, `.figs`, `.seats`, `.empty-seat`, `.cs-eyebrow` among them). **They are CANDIDATES, not a verified list** — the screen that produced them was a grep heuristic that demonstrably UNDER-REPORTS (it returned `0 tsx uses` for `.num` while `rc-num` is plainly rendered in `TurnSection`). Nothing was deleted: CSS orphan removal in this repo needs a real reachability graph, deleting the wrong rule breaks the site silently, and publishing the heuristic's output as fact would itself be a claim broader than the thing backing it. |
| 2026-07-29 | **THE SESSION-37 CROSS-MODEL GATE RAN (session 38) AND RETURNED A VALID BLOCK — 7 findings, ALL FIXED, and TWO of the reviewer's proposed fixes were REFUTED on measurements rather than applied.** Session 37 shipped 5 commits with no cross-model review — an unmet `RULES.md` §9 item, on the session that changed the most about what the site asserts of itself. **THE HEADLINE: the site said "no network requests" and that was measurably FALSE.** Measured against the BUILT export, not inferred from framework docs: `/` makes **12 post-load requests** (Next `<Link>` viewport prefetch pulling RSC route payloads for `/docs` and `/legacy/console`), while **off-origin count is 0**. The two claims come apart and only one is true — *nothing the reader uploads or types ever leaves the browser* (FileReader in, Blob URL out, zero off-origin traffic), but the PAGE plainly makes requests for its own routes. Corrected on **five shipping surfaces** plus two stale comments and `docs/how-to-test.md`, which **invited readers to verify the false claim in their own network tab**. **The gate under-reported the blast radius (3 sites; a case-insensitive sweep found 8)** — two escaped a narrow grep because one was a rendered chip in CAPITALS (`NO NETWORK REQUESTS`) and one broke across a newline, the same shape as session 37's JSX-whitespace defect. **AND THREE TESTS WERE PINNING THE FALSE CLAIM** (`playground-golden.test.ts:326`, `playground.spec.ts:20`, the how-to-test prose): the overclaim was not merely present, it was REQUIRED BY CI, so any honest correction would have failed the build. *A guard that asserts a claim is true cannot also notice the claim is false.* Both suites now assert the true wording AND `.not.toMatch` the retired one — and that inverted assertion is what caught the two instances the grep missed. **THREE TEST-INTEGRITY DEFECTS FIXED RED-GREEN, each proven by MUTATION rather than argument:** ① the door test named *"every door's label agrees with where it goes"* filtered on ONE phrase and visited 3 of 6 routes — **it would not have caught the defect it was written for** (the `/fees` door read "Try it live", which the filter skipped; the separate retired-name sweep caught that one); now the full door set is DECLARED across all six routes, count first. ② the reset test's record-slot assertion was **tautological** — `canRun` requires only the feed, so the record textarea was `""` before AND after; **deleting `setRecord(EMPTY)` left the old test GREEN**, and the rewritten one fails on the same mutation. ③ the fold guard's `toBeVisible()` asserted nothing about the viewport. **TWO REFUTATIONS, both on measurements:** the reviewer wanted the Run button in-viewport — measured y=970 at 1280×900, it sits below the fold BY DESIGN (it follows both zones), so the zones were strengthened to *entirely* above the fold (871.7 ≤ 900) and the button's real invariant pinned instead (it must follow the zones within 200px; measured gap 98.8). And it wanted the dead "display tier" `letter-spacing` restored sitewide — measured computed values show `/report` `/fees` `/proof` already carry deliberate per-surface tracking (-0.030em, -0.028em) and only the LANDING fell through to the generic -0.012em; **those values were calibrated for Onest, the face session 37 replaced, and have never once rendered**, so applying them would be a typography change dressed as a bug fix. The landing alone was set explicitly at -0.028em (matching `/proof`, not the retired Onest figure); the dead block is kept as the record of intent with its `letter-spacing` REMOVED so it no longer promises what the cascade discards. **ALSO: `/legacy/merchant` was a 404** (only `[id]` exists) — retargeted rather than given a new index, since that archive is "preserved, not maintained"; **no test saw it because `legacy.spec.ts` checks a list of routes written inside the test file** — the THIRD *denominator-from-the-observations* instance this session — so a new tooth now walks the links the page actually OFFERS and requires each to serve AND render as a legacy page (*a 200 that serves the 404 body is the same lie with a better status code*), red-green proven. `/legacy`'s "chapter 04" was the last user-visible numbering residue. **HARNESS, RAW: the first gate run was KILLED with ZERO output** after 20+ min at `xhigh` in the background (log = the echoed prompt alone), repeating session 34's five-dead-workflow lesson; re-run INLINE, scoped by file, at `high` — **a disclosed effort downgrade to fit a real constraint, not a claim that `xhigh` ran.** Root cause worth fixing: **`AGENTS.md:7` orders every agent to read ~140k tokens of state docs first**, which also killed session 36's gate. **Gates: verify exit 0 · vitest 1573 + 8 skipped · e2e dev 53 + 1 skipped · e2e ARTIFACT 53 + 1 skipped (up from 52 — the new tooth) · axe 8/8.** Four dev failures at default workers were reproduced as dev-server compile contention (16/16 in 16.7s at `--workers=1`), not defects | Accepted (owner: "go for and complete it") | Measured network trace on the built export; three mutation proofs; computed-style measurements; live route checks | `docs/reviews/codex-2026-07-28-s38-gate.md` |
| 2026-07-31 | **CLOSED-SESSION NARRATIVE ARCHIVED OUT OF THE FOUR STARTUP-CONTRACT DOCS — 1.25 MB → 59 KB (−95%) — AND `RULES.md` §15 DELIBERATELY LEFT UNTOUCHED.** Assessment finding 2 held that `AGENTS.md`:7 / `RULES.md` §15 order every agent, Codex reviewers included, to read 1.31 MB before acting, and that **three cross-model gate runs died on it** (sessions 34, 36, 38). The owner's framing decided the fix: *"the right move is probably archiving sessions 24–36 out of the live state docs … rather than only fixing the instruction that reads them. The instruction is the trigger; the volume is the cause."* So volume moved and the instruction did not. Sessions 01–37 → `docs/archive/2026-07-31-state-docs/`, byte-exact, nothing deleted. **Scope deliberately widened beyond the owner's literal "24–36"**: the docs ran back to session 01, so that cut would have left most of the weight — widened on the owner's own stated reasoning, and disclosed. `HANDOFF.md`'s standing continuity procedures stayed live (procedure, not history). **`RULES.md` §15 not edited**: its read list was never incorrect, only expensive, and amending the constitution is a scope change requiring the owner — archiving is not. `AGENTS.md`:7 took one additive paragraph only. Verified `live_kept + archived == original` against `git show HEAD:` rather than the moving script's own bookkeeping; vitest 1574+8 exit 0, unchanged. | Accepted | The only independent check this project has was being killed by the instruction meant to inform it, and each wrap note grew the file that killed the next session's gate | `docs/assessment-2026-07-30-estate-revaluation.md` finding 2 · `docs/archive/2026-07-31-state-docs/README.md` |
| 2026-07-31 | **THE STARTUP-CONTRACT READ LIST IS NOW HELD UNDER A BUDGET BY A TEST, AND THE OTHER TWO §10 RECORDING SURFACES WERE ARCHIVED (owner: "go and fix it").** Two things the first pass got wrong. **(a) Under-scoped:** the report flagged `docs/decision-log.md` (364 KB) but missed `docs/implementation-journal.md` (147 KB) — same `RULES.md` §10 surface, same trajectory. Measuring *all* of §10's surfaces rather than the one noticed is what found it; both archived by **date parsed per entry, not line position**, because both are appended out of chronological order and a positional cut would have mis-sorted them (373 KB→118 KB and 147 KB→79 KB, −62%, reconstruction verified against `git show HEAD:`). **(b) Fixed at the wrong layer:** a measured byte-count was written into `AGENTS.md` and then invalidated by writing more prose — twice in one session. *A number maintained by hand in a document that grows is wrong by construction.* More importantly the archive alone guarantees nothing: the docs reached 1.31 MB by accretion with nothing stopping it, so a one-time tidy just restarts the climb. `evals/packs/startup-contract-budget.test.ts` now asserts the list stays under **250 KB** (~2.8× headroom — a runaway brake, not a style rule), requires `AGENTS.md` to cite the budget rather than a measurement, and puts "archive, don't raise the limit" in the failure message. Red-green proven: 200 KB of filler → red at 291,778 B; restore sha256-verified. | Accepted | A cleanup with no guard is the same decision that produced 1.31 MB; the invariant has to be held, not intended | `evals/packs/startup-contract-budget.test.ts` · `docs/archive/2026-07-31-state-docs/README.md` |
| 2026-07-31 | **PRODUCTION ROUTES VERIFIED LIVE · THE §10 BUDGET GAP CLOSED · FIRST SECURITY SCAN OF THE UPLOAD SURFACE (owner: "complete all the open and commit cases").** **Routes:** seven live WebFetch checks (WebFetch is not bound by the Bash sandbox) — six real routes render real content, and a nonsense path returns a genuine 404. The 404 control is the load-bearing one: without it, "six pages rendered" is equally consistent with a host serving one fallback for every path, which is exactly session 35's failure mode. **Headers remain unverified** and are recorded as unverified — WebFetch returns rendered markdown, not response headers, and the Vercel MCP session expired mid-check; no tool in this seat returns them. **Budget gap:** the first budget test covered only the startup list, leaving the other two §10 surfaces tidied-but-not-held — the same mistake the test exists to prevent; a fourth tooth now holds them under 400 KB (looser by design: nothing must read them to begin work). **Finding 3:** first source-level security scan of `FileDrop`/`parseCatalogText`/`AuditWorkbench` → **one LOW, no high or medium**. The 5 MB cap guards the file path but not the paste path, so the same document is refused as a file and accepted as a paste. Severity was calibrated by measurement, not by alarm: parsing is linear (0.10–0.40 µs/item over 25k–200k items), 22 MB parses in 203 ms, and 100k-deep nesting does not overflow — no amplification, and the only resource at risk is the reader's own tab, so it is recorded as a **robustness gap rather than a vulnerability**. Fix recommended, not applied (read-only scan); truncation explicitly rejected as the worse bug because it would produce a verdict on a document the reader never supplied. Prototype pollution tested by **execution** and found not exploitable — but clean by *construction* (the parser builds field-by-field rather than spreading), so a future `{...row}` refactor reopens it. The record discloses that the scan shares lineage with the code's authors: a second look, not an independent one. | Accepted | The upload surface invites strangers to drop files in, and had been probed only by its own authors; and a guard that stops at the first surface leaves the second climbing | `docs/security/vuln-scan-2026-07-31-upload-surface.md` · `evals/packs/startup-contract-budget.test.ts` |
| 2026-07-31 | **THE FOUR-POINT POST-DEPLOY VERIFY IS COMPLETE — HEADERS CONFIRMED LIVE ON A SUBPATH, AND SESSION 40 CLOSES WITH NOTHING BLOCKED.** The last unverified item from `docs/assessment-2026-07-30-estate-revaluation.md`. The sandbox refused `curl` (`X-Proxy-Error: blocked-by-allowlist`); the `dangerouslyDisableSandbox` bypass stayed retired per the owner's 2026-07-30 directive and the check was handed over as a `!` one-liner rather than worked around. All five headers present on `/playground` — a SUBPATH, which is the case session 35's lesson is about — and compared against `vercel.json` programmatically rather than by eye: `x-content-type-options: nosniff`, `x-frame-options: DENY`, `referrer-policy: no-referrer`, `permissions-policy: camera=(), microphone=(), geolocation=()` all MATCH the adopted config exactly, plus `strict-transport-security: max-age=63072000; includeSubDomains; preload` supplied by Vercel. **`Content-Security-Policy` correctly ABSENT** — its deferral is part of the adopted policy (`evals/packs/header-policy.test.ts`:7,76), so a CSP appearing without a new reviewed decision would be a failure, not an upgrade; recorded so a future reader does not "fix" it. Push done (`c11afe4` → `origin/main`, behind 0 / ahead 0); auto-deploy stays off, so code shipped and nothing deployed — production still serves `fb6c327`, correctly, since this session changed docs and tests only. **Assessment findings 1, 2 and 3 are closed; only finding 4 (the mutation pass) remains, and it is not blocked by anything.** | Accepted | A route returning 200 says nothing about the headers it returns them with, and the assessment's verify was only three-quarters run until now | `HANDOFF.md` ② · `evals/packs/header-policy.test.ts` |
| 2026-07-31 | **THE MUTATION PASS FOUND TWO GUARDS PROTECTED BY NOTHING (8/10 → 10/10), F-1 FIXED AT THE CONVERGENCE POINT, AND §9 RESOLVED AS ACCEPT-THE-GAP (owner `/goal`: "go ahead finish it").** **Finding 4:** ten mutants declared BEFORE any run — denominator from the specification, never the observations (session 35's rule) — each asserted to match its site exactly once before being applied; the first draft failed that check on 5 of 10 patterns because the engine's internals had been guessed rather than read, and the check caught it before the run rather than after. Eight killed immediately, incl. the product's founding defect (dropping the `/100` in cents→decimal, 33 tests). **Two SURVIVED: the non-UTC `asOf` guard and the non-USD currency guard — both added by session 38's cross-model gate, both correct, and deleting either left all 1,578 tests green.** Cause named precisely: several tests SET `currency: "USD"` and a valid `asOf`, but none ever hands the parser a BAD one — *a field that every fixture supplies correctly is a field whose validator is never exercised*, the same shape as session 38's tautological reset assertion. Closed with 14 teeth asserting the refusal AND its reason (a guard that refuses for the wrong reason gets "fixed" by the next reader); re-run kills both; every mutated file verified byte-exact after. **F-1** fixed one step deeper than the scan recommended: the size bound moved INTO the two parsers, the seam where the file and paste routes converge, so the rule is applied once BY CONSTRUCTION rather than by two callers agreeing to apply it — with a tooth that fails if `FileDrop` ever re-declares a numeric cap, which is the drift shape that caused F-1. **§9:** accept the gap, no back-fill, no amendment yet — the deciding measurement is that those same sessions wrote 44 decision-log rows in the same window, so this is a rule that lost to a better-fitting habit as the work moved from small slices to long single-goal sessions, not eleven careless sessions; back-filling would manufacture contemporaneous-looking records and make the metric read satisfied while nothing changed, and amending `RULES.md` is owner-gated and was not delegated. **The Codex smoke probe could NOT run** — `~/.codex/` is read-only under the sandbox, verified directly — so the claim that the cross-model gate is runnable again remains INFERRED FROM A BYTE COUNT and is recorded as the one open soft claim. | Accepted | Green measures the code, not the tests; and a guard nothing watches is a guarantee being trusted rather than held | `docs/quality/mutation-pass-2026-07-31.md` · `docs/quality/mutate.py` · `docs/security/vuln-scan-2026-07-31-upload-surface.md` |
| 2026-07-31 | **THE CODEX SMOKE PROBE IS BLOCKED BY THE NETWORK ALLOWLIST, NOT ONLY THE FILESYSTEM — which changes the remedy.** A first pass concluded "blocked" after `~/.codex/` proved read-only under the sandbox. That was accurate but incomplete, and stopping there would have sent the owner a fix that does not work. Pressed further: `CODEX_HOME` pointed at a writable scratchpad copy of `config.toml` **cleared the filesystem block entirely** — the probe advanced past the error and hung on the network instead. Direct test then found the real wall: both Codex API hosts return **`000`, connection refused** (`chatgpt.com`, `api.openai.com`), because the sandbox network allowlist permits only Anthropic/GitHub/npm/PyPI. **Consequence: a `/sandbox` widening scoped to `~/.codex/` would NOT enable the probe — it must also cover `chatgpt.com`.** `auth.json` sits in the sandbox's explicit deny-list and was deliberately not copied: solving a network block by relocating a credential file would be the wrong trade even if it worked. The claim that the cross-model gate is runnable again therefore stays **INFERRED from a byte count** and is recorded as the session's one soft claim, with the exact owner one-liner that would demonstrate it. | Accepted | "Blocked" is not a finding until the blocker is identified; the first blocker found was real but not the binding one, and the wrong remedy would have wasted the owner's next attempt | `HANDOFF.md` ⑩ · `docs/task-log.md` fourth pass |
| 2026-07-31 | **THE STARTUP CONTRACT WAS 156% OF A CONTEXT WINDOW — THE GATE FAILURES WERE ARITHMETIC, NOT SLOWNESS.** Re-measuring the claim in the unit that actually mattered: at `858fb17` the five `AGENTS.md`:7 docs totalled **~312,000 tokens**, against a 200k reviewer context. **A reviewer instructed to read them before doing anything could not start at all** — which explains the exact symptom sessions 34/36/38 produced, a 20-minute run returning a log containing only the echoed prompt. Session 38's own estimate of "~140k tokens of state docs" was low by 2.2×. After the archive: **~31,000 tokens (15%)**, and a full scoped review — docs plus every code file this session touched — is **~43k (21%)**, leaving the window for reasoning rather than history. Pinned by a fifth tooth at a 60k-token reviewer budget (red-green at 63,974). **Bytes were always a proxy for the thing that broke; this asserts the thing itself.** Explicitly NOT a substitute for the smoke probe: this establishes that the obstacle is removed, not that the Codex seat answers — two different claims, kept apart in the record. | Accepted | The estate had been reasoning about a context failure using a disk-size number, and the two diverge badly enough that the real figure was more than double the recorded one | `evals/packs/startup-contract-budget.test.ts` · `HANDOFF.md` ⑩ᴃ |
| 2026-07-31 | **TYPE AND COLOR SETTLED BY OWNER WORD, MID-DESIGN-PASS: "use nunito but dont change the color go with original."** Two decisions. **(1) Nunito is confirmed permanent** — chosen from a rendered five-direction slate (`mockups/font-slate-2026-07-31.html`: Nunito control · Fraunces+IBM Plex Sans · Instrument Sans · Source Serif 4 · Instrument Serif+Sans, each set in the site's own H1/lede/ledger row), the same render-then-pick method that first chose it in session 37. The "sophisticated, premium" ask is closed at the type layer; premium now has to come from composition (the R-1/R-2 alignment + rhythm work), not a typeface swap. **(2) Colors stay original** — R-6 (the H1 gradient-text recolor) is owner-REFUSED and R-7 (palette inventory) is dead; the directive was relayed mid-run to the Fable senior-designer agent with a revert order covering any color commit already made. Palette, accent, lamps and gradient text are all owner-fixed as they stand. | Accepted | The owner picked from rendered candidates rather than names, and a refused finding recorded as refused keeps the next session from "fixing" it back | `mockups/font-slate-2026-07-31.html` · `docs/audits/design-review-2026-07-31.md` R-6/R-7 |
| 2026-07-31 | **CODEX SEAT PINNED BY OWNER WORD, MID-SESSION-41: "use gpt 5.6 sol always no other models use latest."** The seat model is **`gpt-5.6-sol`, always** — no other Codex models, no fallback tier; "latest" binds the sol line, not a license to substitute. This supersedes the 2026-07-24 "prefer sol, else next-best available" reading and hardens the standing no-cap-tracking rule: if `sol` errors (400/429/quota), the raw error goes to the owner verbatim — never a silent model switch, downgrade, or account swap (seat/credits are owner actions). The still-open anomaly is recorded, not resolved: config already said `gpt-5.6-sol` while a probe self-reported "GPT-5.4"; self-reports are unreliable, so the settling check remains the owner-run `./scripts/codex-probe.sh` outside the sandbox. | Accepted | An owner-worded model pin recorded as a pin keeps a future session from "helpfully" falling back to another model on the first transient error | `~/.codex/config.toml` · `scripts/codex-probe.sh` · CLAUDE.md § Operating Doctrine (models & cost) |
| 2026-07-31 | **TYPEFACE REVERSED BY OWNER WORD, SESSION 41: ONEST THROUGHOUT — the session-37 Nunito pick is retired.** The owner posted the v8-era hero preview with "USE this font throughout"; the mockup embeds Onest as a base64 `@font-face`, so the face on their screen was verified as Onest before acting, not assumed (the twice-fixed "Nunito permanent" word made this consequential enough for a structured ask; the owner answered **"Onest throughout"**). Reversal mechanics: next/font swaps back, weights re-verified against the installed font manifest (session 37's axis lesson), and the weight calibration comes back DOWN — the current values were bumped up because Nunito reads optically lighter; carrying them onto Onest would overweight every heading. The dead "display tier" tracking block, kept as a record because it was calibrated for Onest and never rendered, is now re-assessable on its original terms. Same session, same register pass: example/sample wording retired from chrome (owner: "make it look like production version website") under RULES §4's two enforced carriers — C10 stays green, the "what is real" page stays findable. | Accepted | An owner may reverse their own fixed word; what is not allowed is the seat guessing which screenshot font they meant — the mockup's embedded face was verified and the fork surfaced before a sitewide typography change | docs/decision-log.md 2026-07-31 Nunito rows (superseded) · mockups/sample-home-listings-v8-2026-07-17.html:23 |
| 2026-07-31 | **PRODUCTION REGISTER ADOPTED, SESSION 41 (owner: "remove the sample feed, sample report word. make it look like production version website").** The demo-captioning layer is retired from chrome: nav "Example report" → "Report" · the /report readout and eyebrow → "AUDIT REPORT" · "Use the sample feed / catalog" → "Use the bundled feed / catalog" · "run the sample pair" → "run the bundled pair" (3 surfaces) · fees "THE EXAMPLE MONTHS"/"Four example months" → "THE MONTHS ON RECORD"/"Four months" · "Load the example statement" → "Load the bundled statement". **The word chosen matters and was not arbitrary.** Two classes of "sample" exist here and only one may go: captions that label the SITE as a demo, and statements of fact about WHAT THE DATA IS. Deleting the second class would flip a true statement into an implied false one — "your feed will be checked against the catalog shipped with this site" must not become a claim about real merchant records. Every factual instance therefore moved to **"bundled"**, which is production voice and remains literally true (the files ship with the build), preserving the reader-data-vs-our-data distinction the workbench provenance line exists to draw. **RULES §4's two carriers verified holding, not assumed:** (a) C10's eight BANNED_CLAIMS patterns applied directly to all 12 changed files → **0 hits, with a control proving each pattern still bites its own planted claim** (a clean scan from a scanner that cannot fire proves nothing); (b) the /docs "what is real, what is invented" statement and its footer link are untouched. Three honesty tests that PINNED the old provenance wording were updated to the new wording — the guarantee is unchanged, only the word is. `/docs` and `/legacy` deliberately left alone: the first is the honesty statement itself, the second is an archive where "rules-based sample" is a technical term for deterministic non-LLM output. | Accepted | The owner's ask and the honesty constitution point opposite ways on the same words, and the resolution is a word that satisfies both rather than a choice between them | RULES.md §4 (amended 2026-07-20) · evals/packs/honesty-c10.test.ts BANNED_CLAIMS · evals/packs/playground-golden.test.ts |
| 2026-07-31 | **DESIGN.md CREATED AS THE DESIGN SOURCE OF TRUTH (`/design-consultation`, session 41) — DOCUMENTING THE SHIPPED SYSTEM, NOT PROPOSING A NEW ONE.** The skill's default posture is to propose a complete new design system (aesthetic, fonts, palette, spacing). That was **deliberately not run**: the owner had fixed the typeface, the palette, the heading-font rule and the register by explicit word the same day, so proposing new fonts or colors would have contradicted standing decisions rather than served them. What was missing was not a design — it was a *record*. The system existed only in 9,200 lines of `app/globals.css` plus a dated review doc, so every session re-derived it from the cascade, and `CLAUDE.md` pointed at nothing for design. Every value in `DESIGN.md` is transcribed from the stylesheet (tokens, contrast ratios, weight ladder, eases, radii, the fold contract), never restated from memory. It carries a **"Settled by owner word — do not reopen"** table, the two traps this repo has now paid for twice (**weight numbers are calibrated to a typeface and do not survive a face swap**; **`ch` measure caps silently re-measure the site when the font changes**), the lamp scheme's rule that *blue is never a lamp*, the bright-fill-tokens-are-unusable-as-text rule, and seven open items recorded rather than fixed. `CLAUDE.md` gained a Design System section pointing at it, subordinate to `RULES.md`. | Accepted | A design system that lives only in a stylesheet gets re-derived, re-litigated and occasionally reverted by the next reader; the failure this closes had already happened (a dead Onest-era block was nearly resurrected hours earlier) | DESIGN.md · CLAUDE.md § Design System · app/globals.css |
| 2026-07-31 | **`/prototype-to-production` RUN AGAINST THIS PROJECT — VERDICT: THE QUESTION IT EXISTS TO SETTLE IS NOT LIVE HERE, AND RECORDING THAT IS THE POINT.** The skill settles "refactor the prototype or rewrite from its blueprint", and defaults to **rewrite**. Applied naively here that is **wrong advice**, because `CLAUDE.md`:86 (owner-set 2026-06-11) states the project is *"a prototype solving a use case, run on demand — not a 24/7 operated service"*, with production-scale operation held as a **documented Enterprise Expansion Path, never a build target unless the owner re-decides**. There is no productionization step to sequence. A future session that reads the skill and starts a rewrite would be violating the project's own doctrine while believing it was following best practice — which is exactly why this row exists. **Checklist result, item by item:** ① *"ran against real data shapes, not fixtures"* is a **category error here** — simulated data is not a prototype shortcut to be removed, it is the `RULES.md` §4 bright line, so satisfying this item would mean breaking the constitution. ② *"write path structurally absent"* is **exceeded, not merely met**: the skill describes read-only tokens at a proxy; this repo walks the real import graph of its own surfaces against a **fail-closed allowlist** (`evals/packs/walkthrough-zero-egress.test.ts`, `import-walk-guard.test.ts`), red-green proven by appending a single `fetch`. ③ *staging deploys in minutes* — met (today's production build completed in 28s), with `vercel.json` `git.deploymentEnabled:false` making every deploy deliberate. ④ *blueprint written down* — met heavily (decision-log, ADRs, implementation journal, and now DESIGN.md). ⑤ *production infrastructure mirrored* — deliberately NOT met, and correctly so, per the same doctrine. ⑥ *the rewrite-vs-refactor call recorded with its reason* — **this row is that record.** The call is **neither**: the artifact stays a prototype by owner decision, and the next question is not "how do we productionize" but "does the owner re-decide the identity", which is theirs alone. | Accepted | A generic productionization playbook applied to a deliberately-episodic prototype would manufacture work the owner explicitly ruled out, and the failure would look like diligence | CLAUDE.md:86 · RULES.md §4 · docs/enterprise-delivery-playbook.md § Enterprise Expansion Path · evals/packs/walkthrough-zero-egress.test.ts |
| 2026-07-31 | **THE DECISION BOARD WAS CHECKED BY TWO AGENTS AND BOTH FOUND REAL DEFECTS — INCLUDING FALSE NUMBERS I HAD PROPAGATED INTO THE STATE DOCS.** `mockups/decision-board-2026-07-31.html` was built to unblock three owner-gated decisions by rendering in the OWNER's browser (the agent sandbox refuses Chromium and port binding). A fact-check agent and a design-craft agent reviewed it independently. **The fact-check earned the most:** ① the H1 size comparison "48.64px vs v8's 42.24px" was **FALSE and inverted the argument** — 42.24px is `.lp-h2`, a section heading, not the H1; against the v8 *mockup* the owner actually reposted (53.76px) the H1 ships **smaller**, so what I presented as supporting evidence was counter-evidence. ② "two independent passes, **months apart**" was false — both are 2026-07-31, hours apart, which materially weakens a corroboration claim I had leaned on in three documents. ③ `canonical.spec.ts:104` is the `test()` declaration; the zone bottom is computed at `:130` and **asserted** at `:132` — never printed, but not "discarded" as I wrote. ④ `Nav.tsx:169` → `:168`. ⑤ the gate chip read 1609 when the tree runs **1610** passed + 8 skipped + **1 failed**, and dropped the failure entirely. ⑥ my "~19px residual" was the *width of the computed headroom band*, not a residual — the real divergence from the one recorded measurement is **~99–118px**, understated roughly fivefold. ⑦ "the v8 branch does not compile" was overstated: `min-height` is editable and `globals.css:5364` already zeroes it elsewhere; the honest claim is that satisfying the contract means abandoning what defines the v8 hero. **The craft check found two CSS bugs that broke the artifact's own argument on screen:** `.chip live` collided with an unplaced `.live` panel rule (both specificity (0,1,0), `.live` winning on source order) so the status row rendered as one 12px box and three stretched hollow pills; and `.bars{height:230px;overflow:hidden}` with the run band at `top:230px` **clipped the single piece of evidence decision 1 rests on to zero visible pixels**. Also: `#7c8394` was the one invented (non-transcribed) colour on the page and the only text pair failing WCAG at 3.80:1 — a clean lesson about inventing values; decision 2's RECOMMENDED option wore the ember "do not" chip, inverting the colour language against its own tag; and the `--s*` scale was transcribed then referenced zero times. All fixed, plus `<main>`, `aria-pressed` with a label swap (selection was colour-only), a copy button, and a clear control. | Accepted | An artifact built to make decisions trustworthy had false numbers in it, and only an adversarial check with file-opening discipline found them — the propagation into PROJECT_STATE/HANDOFF/CURRENT_TASK is the part that would have outlived the artifact | mockups/decision-board-2026-07-31.html · docs/audits/design-review-2026-07-31.md |
| 2026-07-31 | **THE THREE DECISION-BOARD FORKS CAME BACK FROM THE OWNER (session 42) — TWO SETTLED, ONE HELD.** The board (`mockups/decision-board-2026-07-31.html`) existed to convert three measured findings into owner words. Returned verbatim: **(1) the fold reclaim — "— not decided —"**, so `.home-lead` / `.wb` / `.fd-zone` are untouched and DESIGN.md open item 1 stays open; **(2) "RETARGET the nav pill to #audit (accepts the e2e change)"** — APPLIED: the bar's one emphasized action pointed at `/`, so on the landing page the site's most dominant control navigated nowhere. It now targets `/#audit`, the instrument itself, which is a real action from every route including the landing; the brand lockup (`Nav.tsx:110`) still carries plain "home". **The trap in this one-line change, caught before it shipped:** `active` and `action` both keyed off the literal `d.href === "/"`, and `usePathname()` never returns a hash — retargeting the href alone would have switched `aria-current` off the Audit link permanently AND stripped the pill's accent fill, failing the very `canonical.spec.ts` contract this change was authorized to touch, in a seat that cannot render to see it fail. Fixed by decoupling route from target: `DESTINATIONS` gained an optional `match`, and both call sites compute off `match ?? href`. The e2e contract moved with it and gained two teeth (`a[href="/#audit"]` count 1; `a[href="/"]` count 0 inside the Sections nav, which fails if a "/" destination is ever re-added). **(3) "CONFIRM workbench-first (current state, no build)"** — the hero fork is closed by owner word, matching the arithmetic already on record (the v8 hero's `min-height: calc(100vh - 68px)` = 832px at 1280×900 cannot satisfy the 900px fold contract); no code changed, and DESIGN.md's settled-by-owner table now carries it as decided rather than inferred. **Gates run in-seat: tsc 0 · eslint 0 · vitest 1610 passed + 8 skipped**, `honesty-c10` the single failure with the raw cause recorded (`next/font: error: Failed to fetch 'Onest' from Google Fonts` — `fonts.googleapis.com` measured at `000` from this sandbox). **`npm run verify` was deliberately NOT run as one command:** it is `typecheck && lint && build && test`, so the build's font fetch would abort it before vitest ran and return zero test signal — worse than splitting the legs. The rendered battery (`npm run verify && npx playwright test evals/e2e/canonical.spec.ts`) remains owner-run; Chromium still refuses to launch in this seat (binary spawns, closes immediately — raw on record), so the retarget is verified by type-check, lint and the unit suite, and is NOT verified rendered. | Accepted (owner word, 2026-07-31) | `components/Nav.tsx` · `evals/e2e/canonical.spec.ts` · `app/globals.css` nav-action comment · `DESIGN.md` | decision board; DESIGN.md open items 1–2 |

## 2026-08-02 — The walkthrough landing: six stations, end to end (owner word)
Owner reopened layout/sections/motion/scroll by word ("reassess the layout, whole user
experience … from dropping files till slack email whole process … minimalist with modern
premium tech product of 2026"; palette explicitly unchanged; mid-turn: "full permission
given, commit push and deploying vercel"). Design authored in-session (owner named Fable
the designer advisor; sol consult attempted — both Codex hosts 000 from the sandbox, raw
on record — handed to the owner as a `!` one-liner; per owner word the final choice is the
session seat's either way). The landing became six stations — INPUTS · RUN · VERDICT ·
FEES · DELIVERY · PROOF — with a process strip, a never-disabled run control on screen
one (closes the twice-found "instrument without its verb" finding), and a NEW delivery
station rendering the real Slack/email payloads from `lib/delivery/*` builders, stamped
BUILT NOT SENT, SIMULATED banner arriving from the builder's own first block. Source of
truth: `mockups/walkthrough-one-run-2026-08-02.html` (rendered, fold-verified: zones 560 /
run 626 vs 900). Port built by an Opus subagent from the mockup packet; reviewed
diff-level in-session (banner path, banned-claims scan with control, deriveChecks honesty,
gates re-run independently). Gates: tsc 0 · eslint 0 · vitest 1630+8 with honesty-c10 the
single disclosed font-host failure. E2e specs moved with the design, authored-but-
unrendered — first network-capable run must execute the battery before the next design
change. RULES §4 carriers intact: C10 patterns 0 hits over new surfaces with a biting
control; /docs statement + footer link untouched; a new zero-egress tooth walks the
delivery components' import graph (22 modules), red-green proven by two mutations.

## 2026-08-02 — Real-product register, sitewide (owner word; RULES §4 register-change row)
Owner, verbatim intent across three mid-turn directives: "make it real product dont have
github link and content in legacy activation … this applies to whole website as well."
Applied: the GitHub links left the footer (credit is plain text); the Legacy activation
module left the product — app/legacy plus the seven tombstone/legacy routes (audit,
console, cost, demo, eval, metrics, merchant) deleted, 404 the honest answer, no
redirects; every "In plain terms:" and "Moved to…" caption swept; robots.txt no longer
names dead paths. RULES §4 carriers verified, not assumed: the /docs "what is real"
statement + footer link survive on all 8 remaining pages (exactly one footer each), all
8 BANNED_PROBES planted-claim controls bite, the rendered C10 scan ran against the fresh
export. Test pins moved with the cut (footer contract, 404 sweep with a never-existed
control, retired-name sweep gains "Legacy activation" and "In plain terms:"); legacy-only
specs deleted; one vacuous-if-kept suite replaced with a tombstone naming where the
property still lives. Suite: 1604 + 8, 0 failed. Built by an Opus subagent, reviewed and
re-gated by the session seat (dual-model; sol unreachable from the sandbox, raw 000s on
record). ALSO: the offline build route discovered and made durable
(scripts/build-offline.mjs — next/font mock hook + webpack pipeline + real font bytes
from the repo's own v8 mockup), which is what let the rendered honesty gate run in-seat
for the first time. CI QA loop closed: run 30773077581's four e2e reds diagnosed — two
real a11y defects in the new landing (color-only link, unfocusable scroll regions), both
fixed; two stale pins (playground H1, folded-evidence visibility), both moved; the
download red did not reproduce in-seat against the same bytes — a loud precondition
assertion added so any recurrence fails at the true line.
